version: '3.8'

services:
  # Router API - FastAPI 入口 (app.main:app)
  router-api:
    build:
      context: .
      dockerfile: docker/Dockerfile.router
    container_name: llm-router-api
    ports:
      - "9000:8000"
    volumes:
      - ./output:/app/output
      - ./data:/app/data
    env_file:
      - .env
    environment:
      - DB_PATH=/app/data/conversations.db
      - GEMINI_API_KEY=${GEMINI_API_KEY}
      - GEMINI_BASE_URL=${GEMINI_BASE_URL}
      - GEMINI_SDK_URL=${GEMINI_SDK_URL}
      - GRAFANA_URL=http://47.89.135.221:9001/
      - GRAFANA_TOKEN=${GRAFANA_TOKEN}
      # 渲染整个 Dashboard
      - GRAFANA_RENDER_PATH=render/d/llm-router-dashboard/dashboard?orgId=1&width=1200&height=800&kiosk
      - REPORT_TIMEZONE=Asia/Shanghai
    volumes:
      - ./output:/app/output
    depends_on:
      - grafana
    restart: unless-stopped

  # Developer Admin - 开发者管理界面
  developer-admin:
    build:
      context: .
      dockerfile: Dockerfile.metrics
    container_name: llm-developer-admin
    ports:
      - "9095:9095"
    volumes:
      - ./data:/app/data
    environment:
      - ADMIN_PORT=9095
      - DB_PATH=/app/data/conversations.db
    command: [ "python", "-u", "developer_admin.py" ]
    restart: unless-stopped

volumes:
  prometheus_data:
  grafana_data:

networks:
  edge-shared:
    external: true
