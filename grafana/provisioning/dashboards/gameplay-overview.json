{
  "annotations": {
    "list": [
      {
        "builtIn": 1,
        "datasource": {
          "type": "grafana",
          "uid": "-- Grafana --"
        },
        "enable": true,
        "hide": true,
        "iconColor": "rgba(0, 211, 255, 1)",
        "name": "Annotations & Alerts",
        "type": "dashboard"
      }
    ]
  },
  "editable": true,
  "fiscalYearStartMonth": 0,
  "graphTooltip": 0,
  "links": [],
  "panels": [
    {
      "collapsed": false,
      "gridPos": { "h": 1, "w": 24, "x": 0, "y": 0 },
      "id": 100,
      "panels": [],
      "title": "总览",
      "type": "row"
    },
    {
      "datasource": { "type": "frser-sqlite-datasource", "uid": "${DS_SQLITE}" },
      "fieldConfig": { "defaults": { "color": { "mode": "thresholds" }, "mappings": [], "thresholds": { "mode": "absolute", "steps": [ { "color": "green", "value": 0 } ] }, "unit": "short" }, "overrides": [] },
      "gridPos": { "h": 4, "w": 6, "x": 0, "y": 1 },
      "id": 1,
      "options": { "colorMode": "value", "graphMode": "none", "justifyMode": "auto", "orientation": "auto", "percentChangeColorMode": "standard", "reduceOptions": { "calcs": [ "lastNotNull" ], "fields": "", "values": false }, "showPercentChange": false, "textMode": "auto", "wideLayout": true },
      "pluginVersion": "12.3.1",
      "targets": [ { "queryText": "SELECT COUNT(*) AS value FROM gameplay_player_summary gps WHERE COALESCE((SELECT us.is_developer FROM user_sessions us WHERE us.user_id = gps.user_id), 0) = 0", "queryType": "table", "rawQueryText": "SELECT COUNT(*) AS value FROM gameplay_player_summary gps WHERE COALESCE((SELECT us.is_developer FROM user_sessions us WHERE us.user_id = gps.user_id), 0) = 0", "refId": "A" } ],
      "title": "玩家数",
      "type": "stat"
    },
    {
      "datasource": { "type": "frser-sqlite-datasource", "uid": "${DS_SQLITE}" },
      "fieldConfig": { "defaults": { "color": { "mode": "thresholds" }, "mappings": [], "thresholds": { "mode": "absolute", "steps": [ { "color": "green", "value": 0 } ] }, "unit": "short" }, "overrides": [] },
      "gridPos": { "h": 4, "w": 6, "x": 6, "y": 1 },
      "id": 2,
      "options": { "colorMode": "value", "graphMode": "none", "justifyMode": "auto", "orientation": "auto", "percentChangeColorMode": "standard", "reduceOptions": { "calcs": [ "lastNotNull" ], "fields": "", "values": false }, "showPercentChange": false, "textMode": "auto", "wideLayout": true },
      "pluginVersion": "12.3.1",
      "targets": [ { "queryText": "WITH base AS (SELECT * FROM gameplay_player_summary gps WHERE COALESCE((SELECT us.is_developer FROM user_sessions us WHERE us.user_id = gps.user_id), 0) = 0), latest AS (SELECT MAX(first_login_date) AS d FROM base) SELECT COUNT(*) AS value FROM base, latest WHERE first_login_date = date(latest.d, '-1 day')", "queryType": "table", "rawQueryText": "WITH base AS (SELECT * FROM gameplay_player_summary gps WHERE COALESCE((SELECT us.is_developer FROM user_sessions us WHERE us.user_id = gps.user_id), 0) = 0), latest AS (SELECT MAX(first_login_date) AS d FROM base) SELECT COUNT(*) AS value FROM base, latest WHERE first_login_date = date(latest.d, '-1 day')", "refId": "A" } ],
      "title": "昨日新增",
      "type": "stat"
    },
    {
      "datasource": { "type": "frser-sqlite-datasource", "uid": "${DS_SQLITE}" },
      "fieldConfig": { "defaults": { "color": { "mode": "thresholds" }, "mappings": [], "thresholds": { "mode": "absolute", "steps": [ { "color": "green", "value": 0 } ] }, "unit": "short" }, "overrides": [] },
      "gridPos": { "h": 4, "w": 6, "x": 12, "y": 1 },
      "id": 3,
      "options": { "colorMode": "value", "graphMode": "none", "justifyMode": "auto", "orientation": "auto", "percentChangeColorMode": "standard", "reduceOptions": { "calcs": [ "lastNotNull" ], "fields": "", "values": false }, "showPercentChange": false, "textMode": "auto", "wideLayout": true },
      "pluginVersion": "12.3.1",
      "targets": [ { "queryText": "WITH base AS (SELECT * FROM gameplay_player_summary gps WHERE COALESCE((SELECT us.is_developer FROM user_sessions us WHERE us.user_id = gps.user_id), 0) = 0), latest AS (SELECT MAX(first_login_date) AS d FROM base) SELECT COUNT(*) AS value FROM base, latest WHERE first_login_date BETWEEN date(latest.d, '-6 day') AND latest.d", "queryType": "table", "rawQueryText": "WITH base AS (SELECT * FROM gameplay_player_summary gps WHERE COALESCE((SELECT us.is_developer FROM user_sessions us WHERE us.user_id = gps.user_id), 0) = 0), latest AS (SELECT MAX(first_login_date) AS d FROM base) SELECT COUNT(*) AS value FROM base, latest WHERE first_login_date BETWEEN date(latest.d, '-6 day') AND latest.d", "refId": "A" } ],
      "title": "7日新增",
      "type": "stat"
    },
    {
      "datasource": { "type": "frser-sqlite-datasource", "uid": "${DS_SQLITE}" },
      "fieldConfig": { "defaults": { "color": { "mode": "thresholds" }, "mappings": [], "thresholds": { "mode": "absolute", "steps": [ { "color": "green", "value": 0 } ] }, "unit": "short" }, "overrides": [] },
      "gridPos": { "h": 4, "w": 6, "x": 18, "y": 1 },
      "id": 4,
      "options": { "colorMode": "value", "graphMode": "none", "justifyMode": "auto", "orientation": "auto", "percentChangeColorMode": "standard", "reduceOptions": { "calcs": [ "lastNotNull" ], "fields": "", "values": false }, "showPercentChange": false, "textMode": "auto", "wideLayout": true },
      "pluginVersion": "12.3.1",
      "targets": [ { "queryText": "WITH base AS (SELECT * FROM gameplay_player_summary gps WHERE COALESCE((SELECT us.is_developer FROM user_sessions us WHERE us.user_id = gps.user_id), 0) = 0), latest AS (SELECT MAX(first_login_date) AS d FROM base) SELECT COUNT(*) AS value FROM base, latest WHERE first_login_date BETWEEN date(latest.d, '-29 day') AND latest.d", "queryType": "table", "rawQueryText": "WITH base AS (SELECT * FROM gameplay_player_summary gps WHERE COALESCE((SELECT us.is_developer FROM user_sessions us WHERE us.user_id = gps.user_id), 0) = 0), latest AS (SELECT MAX(first_login_date) AS d FROM base) SELECT COUNT(*) AS value FROM base, latest WHERE first_login_date BETWEEN date(latest.d, '-29 day') AND latest.d", "refId": "A" } ],
      "title": "30日新增",
      "type": "stat"
    },
    {
      "datasource": { "type": "frser-sqlite-datasource", "uid": "${DS_SQLITE}" },
      "fieldConfig": { "defaults": { "color": { "mode": "thresholds" }, "mappings": [], "thresholds": { "mode": "absolute", "steps": [ { "color": "green", "value": 0 } ] }, "unit": "s" }, "overrides": [] },
      "gridPos": { "h": 4, "w": 4, "x": 0, "y": 5 },
      "id": 5,
      "options": { "colorMode": "value", "graphMode": "none", "justifyMode": "auto", "orientation": "auto", "percentChangeColorMode": "standard", "reduceOptions": { "calcs": [ "lastNotNull" ], "fields": "", "values": false }, "showPercentChange": false, "textMode": "auto", "wideLayout": true },
      "pluginVersion": "12.3.1",
      "targets": [ { "queryText": "WITH ordered AS (SELECT total_play_duration_sec AS v, ROW_NUMBER() OVER (ORDER BY total_play_duration_sec) AS rn, COUNT(*) OVER () AS cnt FROM gameplay_player_summary gps WHERE COALESCE((SELECT us.is_developer FROM user_sessions us WHERE us.user_id = gps.user_id), 0) = 0) SELECT ROUND(AVG(v), 2) AS value FROM ordered WHERE rn IN ((cnt + 1) / 2, (cnt + 2) / 2)", "queryType": "table", "rawQueryText": "WITH ordered AS (SELECT total_play_duration_sec AS v, ROW_NUMBER() OVER (ORDER BY total_play_duration_sec) AS rn, COUNT(*) OVER () AS cnt FROM gameplay_player_summary gps WHERE COALESCE((SELECT us.is_developer FROM user_sessions us WHERE us.user_id = gps.user_id), 0) = 0) SELECT ROUND(AVG(v), 2) AS value FROM ordered WHERE rn IN ((cnt + 1) / 2, (cnt + 2) / 2)", "refId": "A" } ],
      "title": "游玩时长中位数",
      "type": "stat"
    },
    {
      "datasource": { "type": "frser-sqlite-datasource", "uid": "${DS_SQLITE}" },
      "fieldConfig": { "defaults": { "color": { "mode": "thresholds" }, "mappings": [], "thresholds": { "mode": "absolute", "steps": [ { "color": "green", "value": 0 } ] }, "unit": "s" }, "overrides": [] },
      "gridPos": { "h": 4, "w": 4, "x": 4, "y": 5 },
      "id": 6,
      "options": { "colorMode": "value", "graphMode": "none", "justifyMode": "auto", "orientation": "auto", "percentChangeColorMode": "standard", "reduceOptions": { "calcs": [ "lastNotNull" ], "fields": "", "values": false }, "showPercentChange": false, "textMode": "auto", "wideLayout": true },
      "pluginVersion": "12.3.1",
      "targets": [ { "queryText": "SELECT ROUND(AVG(total_play_duration_sec), 2) AS value FROM gameplay_player_summary gps WHERE COALESCE((SELECT us.is_developer FROM user_sessions us WHERE us.user_id = gps.user_id), 0) = 0", "queryType": "table", "rawQueryText": "SELECT ROUND(AVG(total_play_duration_sec), 2) AS value FROM gameplay_player_summary gps WHERE COALESCE((SELECT us.is_developer FROM user_sessions us WHERE us.user_id = gps.user_id), 0) = 0", "refId": "A" } ],
      "title": "游玩时长平均数",
      "type": "stat"
    },
    {
      "datasource": { "type": "frser-sqlite-datasource", "uid": "${DS_SQLITE}" },
      "fieldConfig": { "defaults": { "color": { "mode": "thresholds" }, "mappings": [], "thresholds": { "mode": "absolute", "steps": [ { "color": "green", "value": 0 } ] }, "unit": "short" }, "overrides": [] },
      "gridPos": { "h": 4, "w": 4, "x": 8, "y": 5 },
      "id": 7,
      "options": { "colorMode": "value", "graphMode": "none", "justifyMode": "auto", "orientation": "auto", "percentChangeColorMode": "standard", "reduceOptions": { "calcs": [ "lastNotNull" ], "fields": "", "values": false }, "showPercentChange": false, "textMode": "auto", "wideLayout": true },
      "pluginVersion": "12.3.1",
      "targets": [ { "queryText": "WITH ordered AS (SELECT play_days_total AS v, ROW_NUMBER() OVER (ORDER BY play_days_total) AS rn, COUNT(*) OVER () AS cnt FROM gameplay_player_summary gps WHERE COALESCE((SELECT us.is_developer FROM user_sessions us WHERE us.user_id = gps.user_id), 0) = 0) SELECT ROUND(AVG(v), 2) AS value FROM ordered WHERE rn IN ((cnt + 1) / 2, (cnt + 2) / 2)", "queryType": "table", "rawQueryText": "WITH ordered AS (SELECT play_days_total AS v, ROW_NUMBER() OVER (ORDER BY play_days_total) AS rn, COUNT(*) OVER () AS cnt FROM gameplay_player_summary gps WHERE COALESCE((SELECT us.is_developer FROM user_sessions us WHERE us.user_id = gps.user_id), 0) = 0) SELECT ROUND(AVG(v), 2) AS value FROM ordered WHERE rn IN ((cnt + 1) / 2, (cnt + 2) / 2)", "refId": "A" } ],
      "title": "游玩天数中位数",
      "type": "stat"
    },
    {
      "datasource": { "type": "frser-sqlite-datasource", "uid": "${DS_SQLITE}" },
      "fieldConfig": { "defaults": { "color": { "mode": "thresholds" }, "mappings": [], "thresholds": { "mode": "absolute", "steps": [ { "color": "green", "value": 0 } ] }, "unit": "short" }, "overrides": [] },
      "gridPos": { "h": 4, "w": 4, "x": 12, "y": 5 },
      "id": 8,
      "options": { "colorMode": "value", "graphMode": "none", "justifyMode": "auto", "orientation": "auto", "percentChangeColorMode": "standard", "reduceOptions": { "calcs": [ "lastNotNull" ], "fields": "", "values": false }, "showPercentChange": false, "textMode": "auto", "wideLayout": true },
      "pluginVersion": "12.3.1",
      "targets": [ { "queryText": "SELECT ROUND(AVG(play_days_total), 2) AS value FROM gameplay_player_summary gps WHERE COALESCE((SELECT us.is_developer FROM user_sessions us WHERE us.user_id = gps.user_id), 0) = 0", "queryType": "table", "rawQueryText": "SELECT ROUND(AVG(play_days_total), 2) AS value FROM gameplay_player_summary gps WHERE COALESCE((SELECT us.is_developer FROM user_sessions us WHERE us.user_id = gps.user_id), 0) = 0", "refId": "A" } ],
      "title": "游玩天数平均数",
      "type": "stat"
    },
    {
      "datasource": { "type": "frser-sqlite-datasource", "uid": "${DS_SQLITE}" },
      "fieldConfig": { "defaults": { "color": { "mode": "thresholds" }, "mappings": [], "thresholds": { "mode": "absolute", "steps": [ { "color": "green", "value": 0 } ] }, "unit": "short" }, "overrides": [] },
      "gridPos": { "h": 4, "w": 4, "x": 16, "y": 5 },
      "id": 9,
      "options": { "colorMode": "value", "graphMode": "none", "justifyMode": "auto", "orientation": "auto", "percentChangeColorMode": "standard", "reduceOptions": { "calcs": [ "lastNotNull" ], "fields": "", "values": false }, "showPercentChange": false, "textMode": "auto", "wideLayout": true },
      "pluginVersion": "12.3.1",
      "targets": [ { "queryText": "WITH ordered AS (SELECT island_level_max AS v, ROW_NUMBER() OVER (ORDER BY island_level_max) AS rn, COUNT(*) OVER () AS cnt FROM gameplay_player_summary gps WHERE COALESCE((SELECT us.is_developer FROM user_sessions us WHERE us.user_id = gps.user_id), 0) = 0) SELECT ROUND(AVG(v), 2) AS value FROM ordered WHERE rn IN ((cnt + 1) / 2, (cnt + 2) / 2)", "queryType": "table", "rawQueryText": "WITH ordered AS (SELECT island_level_max AS v, ROW_NUMBER() OVER (ORDER BY island_level_max) AS rn, COUNT(*) OVER () AS cnt FROM gameplay_player_summary gps WHERE COALESCE((SELECT us.is_developer FROM user_sessions us WHERE us.user_id = gps.user_id), 0) = 0) SELECT ROUND(AVG(v), 2) AS value FROM ordered WHERE rn IN ((cnt + 1) / 2, (cnt + 2) / 2)", "refId": "A" } ],
      "title": "岛屿等级中位数",
      "type": "stat"
    },
    {
      "datasource": { "type": "frser-sqlite-datasource", "uid": "${DS_SQLITE}" },
      "fieldConfig": { "defaults": { "color": { "mode": "thresholds" }, "mappings": [], "thresholds": { "mode": "absolute", "steps": [ { "color": "green", "value": 0 } ] }, "unit": "short" }, "overrides": [] },
      "gridPos": { "h": 4, "w": 4, "x": 20, "y": 5 },
      "id": 10,
      "options": { "colorMode": "value", "graphMode": "none", "justifyMode": "auto", "orientation": "auto", "percentChangeColorMode": "standard", "reduceOptions": { "calcs": [ "lastNotNull" ], "fields": "", "values": false }, "showPercentChange": false, "textMode": "auto", "wideLayout": true },
      "pluginVersion": "12.3.1",
      "targets": [ { "queryText": "SELECT ROUND(AVG(island_level_max), 2) AS value FROM gameplay_player_summary gps WHERE COALESCE((SELECT us.is_developer FROM user_sessions us WHERE us.user_id = gps.user_id), 0) = 0", "queryType": "table", "rawQueryText": "SELECT ROUND(AVG(island_level_max), 2) AS value FROM gameplay_player_summary gps WHERE COALESCE((SELECT us.is_developer FROM user_sessions us WHERE us.user_id = gps.user_id), 0) = 0", "refId": "A" } ],
      "title": "岛屿等级平均数",
      "type": "stat"
    },
    {
      "datasource": { "type": "frser-sqlite-datasource", "uid": "${DS_SQLITE}" },
      "fieldConfig": { "defaults": { "color": { "mode": "thresholds" }, "mappings": [], "thresholds": { "mode": "absolute", "steps": [ { "color": "green", "value": 0 } ] }, "unit": "currencyCNY" }, "overrides": [] },
      "gridPos": { "h": 4, "w": 12, "x": 0, "y": 9 },
      "id": 11,
      "options": { "colorMode": "value", "graphMode": "none", "justifyMode": "auto", "orientation": "auto", "percentChangeColorMode": "standard", "reduceOptions": { "calcs": [ "lastNotNull" ], "fields": "", "values": false }, "showPercentChange": false, "textMode": "auto", "wideLayout": true },
      "pluginVersion": "12.3.1",
      "targets": [ { "queryText": "SELECT COALESCE(SUM(total_money), 0) AS value FROM gameplay_player_summary gps WHERE COALESCE((SELECT us.is_developer FROM user_sessions us WHERE us.user_id = gps.user_id), 0) = 0", "queryType": "table", "rawQueryText": "SELECT COALESCE(SUM(total_money), 0) AS value FROM gameplay_player_summary gps WHERE COALESCE((SELECT us.is_developer FROM user_sessions us WHERE us.user_id = gps.user_id), 0) = 0", "refId": "A" } ],
      "title": "总金币数",
      "type": "stat"
    },
    {
      "datasource": { "type": "frser-sqlite-datasource", "uid": "${DS_SQLITE}" },
      "fieldConfig": { "defaults": { "color": { "mode": "thresholds" }, "mappings": [], "thresholds": { "mode": "absolute", "steps": [ { "color": "green", "value": 0 } ] }, "unit": "short" }, "overrides": [] },
      "gridPos": { "h": 4, "w": 12, "x": 12, "y": 9 },
      "id": 12,
      "options": { "colorMode": "value", "graphMode": "none", "justifyMode": "auto", "orientation": "auto", "percentChangeColorMode": "standard", "reduceOptions": { "calcs": [ "lastNotNull" ], "fields": "", "values": false }, "showPercentChange": false, "textMode": "auto", "wideLayout": true },
      "pluginVersion": "12.3.1",
      "targets": [ { "queryText": "SELECT COALESCE(SUM(cats_count), 0) AS value FROM gameplay_player_summary gps WHERE COALESCE((SELECT us.is_developer FROM user_sessions us WHERE us.user_id = gps.user_id), 0) = 0", "queryType": "table", "rawQueryText": "SELECT COALESCE(SUM(cats_count), 0) AS value FROM gameplay_player_summary gps WHERE COALESCE((SELECT us.is_developer FROM user_sessions us WHERE us.user_id = gps.user_id), 0) = 0", "refId": "A" } ],
      "title": "猫总数",
      "type": "stat"
    },
    {
      "datasource": { "type": "frser-sqlite-datasource", "uid": "${DS_SQLITE}" },
      "fieldConfig": { "defaults": { "color": { "mode": "thresholds" }, "custom": { "align": "auto", "cellOptions": { "type": "auto" }, "footer": { "reducers": [] }, "inspect": false }, "mappings": [], "thresholds": { "mode": "absolute", "steps": [ { "color": "green", "value": 0 } ] } }, "overrides": [] },
      "gridPos": { "h": 8, "w": 8, "x": 0, "y": 13 },
      "id": 13,
      "options": { "cellHeight": "sm", "showHeader": true, "sortBy": [ { "desc": true, "displayName": "次数" } ] },
      "pluginVersion": "12.3.1",
      "targets": [ { "queryText": "SELECT COALESCE(NULLIF(item_name, ''), item_id, 'unknown') AS 商品, COUNT(*) AS 次数 FROM gameplay_events WHERE COALESCE(is_dev, 0) = 0 AND event_type = 'shop_purchase' GROUP BY 1 ORDER BY 2 DESC LIMIT 5", "queryType": "table", "rawQueryText": "SELECT COALESCE(NULLIF(item_name, ''), item_id, 'unknown') AS 商品, COUNT(*) AS 次数 FROM gameplay_events WHERE COALESCE(is_dev, 0) = 0 AND event_type = 'shop_purchase' GROUP BY 1 ORDER BY 2 DESC LIMIT 5", "refId": "A" } ],
      "title": "购买最多前5个商品",
      "type": "table"
    },
    {
      "datasource": { "type": "frser-sqlite-datasource", "uid": "${DS_SQLITE}" },
      "fieldConfig": { "defaults": { "color": { "mode": "thresholds" }, "custom": { "align": "auto", "cellOptions": { "type": "auto" }, "footer": { "reducers": [] }, "inspect": false }, "mappings": [], "thresholds": { "mode": "absolute", "steps": [ { "color": "green", "value": 0 } ] } }, "overrides": [] },
      "gridPos": { "h": 8, "w": 8, "x": 8, "y": 13 },
      "id": 14,
      "options": { "cellHeight": "sm", "showHeader": true, "sortBy": [ { "desc": true, "displayName": "次数" } ] },
      "pluginVersion": "12.3.1",
      "targets": [ { "queryText": "SELECT COALESCE(NULLIF(event_type, ''), 'unknown') AS 行为, COUNT(*) AS 次数 FROM gameplay_events WHERE COALESCE(is_dev, 0) = 0 GROUP BY 1 ORDER BY 2 DESC LIMIT 5", "queryType": "table", "rawQueryText": "SELECT COALESCE(NULLIF(event_type, ''), 'unknown') AS 行为, COUNT(*) AS 次数 FROM gameplay_events WHERE COALESCE(is_dev, 0) = 0 GROUP BY 1 ORDER BY 2 DESC LIMIT 5", "refId": "A" } ],
      "title": "进行最多前5个行为",
      "type": "table"
    },
    {
      "datasource": { "type": "frser-sqlite-datasource", "uid": "${DS_SQLITE}" },
      "fieldConfig": { "defaults": { "color": { "mode": "thresholds" }, "custom": { "align": "auto", "cellOptions": { "type": "auto" }, "footer": { "reducers": [] }, "inspect": false }, "mappings": [], "thresholds": { "mode": "absolute", "steps": [ { "color": "green", "value": 0 } ] } }, "overrides": [] },
      "gridPos": { "h": 8, "w": 8, "x": 16, "y": 13 },
      "id": 15,
      "options": { "cellHeight": "sm", "showHeader": true, "sortBy": [ { "desc": true, "displayName": "次数" } ] },
      "pluginVersion": "12.3.1",
      "targets": [ { "queryText": "SELECT COALESCE(NULLIF(seed_name, ''), seed_id, 'unknown') AS 种子, COUNT(*) AS 次数 FROM gameplay_events WHERE COALESCE(is_dev, 0) = 0 AND event_type = 'farming_plant' GROUP BY 1 ORDER BY 2 DESC LIMIT 5", "queryType": "table", "rawQueryText": "SELECT COALESCE(NULLIF(seed_name, ''), seed_id, 'unknown') AS 种子, COUNT(*) AS 次数 FROM gameplay_events WHERE COALESCE(is_dev, 0) = 0 AND event_type = 'farming_plant' GROUP BY 1 ORDER BY 2 DESC LIMIT 5", "refId": "A" } ],
      "title": "种最多的5个种子",
      "type": "table"
    },
    {
      "datasource": { "type": "frser-sqlite-datasource", "uid": "${DS_SQLITE}" },
      "fieldConfig": { "defaults": { "color": { "mode": "thresholds" }, "custom": { "align": "auto", "cellOptions": { "type": "auto" }, "footer": { "reducers": [] }, "inspect": false }, "mappings": [], "thresholds": { "mode": "absolute", "steps": [ { "color": "green", "value": 0 } ] } }, "overrides": [] },
      "gridPos": { "h": 8, "w": 8, "x": 0, "y": 21 },
      "id": 16,
      "options": { "cellHeight": "sm", "showHeader": true, "sortBy": [ { "desc": true, "displayName": "次数" } ] },
      "pluginVersion": "12.3.1",
      "targets": [ { "queryText": "SELECT COALESCE(NULLIF(recipe_name, ''), recipe_id, 'unknown') AS 菜谱, COUNT(*) AS 次数 FROM gameplay_events WHERE COALESCE(is_dev, 0) = 0 AND event_type = 'restaurant_serve' GROUP BY 1 ORDER BY 2 DESC LIMIT 5", "queryType": "table", "rawQueryText": "SELECT COALESCE(NULLIF(recipe_name, ''), recipe_id, 'unknown') AS 菜谱, COUNT(*) AS 次数 FROM gameplay_events WHERE COALESCE(is_dev, 0) = 0 AND event_type = 'restaurant_serve' GROUP BY 1 ORDER BY 2 DESC LIMIT 5", "refId": "A" } ],
      "title": "做最多的5种菜",
      "type": "table"
    },
    {
      "datasource": { "type": "frser-sqlite-datasource", "uid": "${DS_SQLITE}" },
      "fieldConfig": { "defaults": { "color": { "mode": "thresholds" }, "custom": { "align": "auto", "cellOptions": { "type": "auto" }, "footer": { "reducers": [] }, "inspect": false }, "mappings": [], "thresholds": { "mode": "absolute", "steps": [ { "color": "green", "value": 0 } ] } }, "overrides": [] },
      "gridPos": { "h": 8, "w": 8, "x": 8, "y": 21 },
      "id": 17,
      "options": { "cellHeight": "sm", "showHeader": true, "sortBy": [ { "desc": true, "displayName": "体力" } ] },
      "pluginVersion": "12.3.1",
      "targets": [ { "queryText": "SELECT COALESCE(NULLIF(event_type, ''), 'unknown') AS 行为, ROUND(SUM(COALESCE(energy_cost, 0)), 2) AS 体力 FROM gameplay_events WHERE COALESCE(is_dev, 0) = 0 GROUP BY 1 ORDER BY 2 DESC LIMIT 5", "queryType": "table", "rawQueryText": "SELECT COALESCE(NULLIF(event_type, ''), 'unknown') AS 行为, ROUND(SUM(COALESCE(energy_cost, 0)), 2) AS 体力 FROM gameplay_events WHERE COALESCE(is_dev, 0) = 0 GROUP BY 1 ORDER BY 2 DESC LIMIT 5", "refId": "A" } ],
      "title": "花费体力最多的5个行为",
      "type": "table"
    },
    {
      "datasource": { "type": "frser-sqlite-datasource", "uid": "${DS_SQLITE}" },
      "fieldConfig": { "defaults": { "color": { "mode": "thresholds" }, "custom": { "align": "auto", "cellOptions": { "type": "auto" }, "footer": { "reducers": [] }, "inspect": false }, "mappings": [], "thresholds": { "mode": "absolute", "steps": [ { "color": "green", "value": 0 } ] } }, "overrides": [] },
      "gridPos": { "h": 8, "w": 8, "x": 16, "y": 21 },
      "id": 18,
      "options": { "cellHeight": "sm", "showHeader": true, "sortBy": [ { "desc": true, "displayName": "次数" } ] },
      "pluginVersion": "12.3.1",
      "targets": [ { "queryText": "SELECT COALESCE(NULLIF(building_name, ''), building_id, 'unknown') AS 建筑, COUNT(*) AS 次数 FROM gameplay_events WHERE COALESCE(is_dev, 0) = 0 AND event_type IN ('building_construct', 'building_upgrade') GROUP BY 1 ORDER BY 2 DESC LIMIT 5", "queryType": "table", "rawQueryText": "SELECT COALESCE(NULLIF(building_name, ''), building_id, 'unknown') AS 建筑, COUNT(*) AS 次数 FROM gameplay_events WHERE COALESCE(is_dev, 0) = 0 AND event_type IN ('building_construct', 'building_upgrade') GROUP BY 1 ORDER BY 2 DESC LIMIT 5", "refId": "A" } ],
      "title": "建造最多的5种建筑",
      "type": "table"
    },
    {
      "collapsed": false,
      "gridPos": { "h": 1, "w": 24, "x": 0, "y": 29 },
      "id": 101,
      "panels": [],
      "title": "玩家一览",
      "type": "row"
    },
    {
      "datasource": { "type": "frser-sqlite-datasource", "uid": "${DS_SQLITE}" },
      "fieldConfig": {
        "defaults": {
          "color": { "mode": "thresholds" },
          "custom": { "align": "auto", "cellOptions": { "type": "auto" }, "footer": { "reducers": [] }, "inspect": false },
          "mappings": [],
          "thresholds": { "mode": "absolute", "steps": [ { "color": "green", "value": 0 } ] }
        },
        "overrides": [
          {
            "matcher": { "id": "byName", "options": "昵称" },
            "properties": [
              {
                "id": "links",
                "value": [
                  {
                    "title": "打开玩家详情",
                    "url": "/d/gameplay-player-detail/gameplay-player-detail?orgId=1&var-user_id=${__data.fields.user_id}",
                    "targetBlank": false
                  }
                ]
              }
            ]
          }
        ]
      },
      "gridPos": { "h": 12, "w": 24, "x": 0, "y": 30 },
      "id": 19,
      "options": { "cellHeight": "sm", "showHeader": true, "sortBy": [ { "desc": true, "displayName": "游玩时长(秒)" } ] },
      "pluginVersion": "12.3.1",
      "targets": [
        {
          "queryText": "SELECT nickname AS 昵称, user_id, user_id AS ID, ROUND(total_play_duration_sec, 2) AS '游玩时长(秒)', play_days_total AS '游玩天数', island_level_max AS '岛屿等级', first_login_date AS '首次登录日期', total_money AS '总金币数' FROM gameplay_player_summary gps WHERE COALESCE((SELECT us.is_developer FROM user_sessions us WHERE us.user_id = gps.user_id), 0) = 0 ORDER BY total_play_duration_sec DESC",
          "queryType": "table",
          "rawQueryText": "SELECT nickname AS 昵称, user_id, user_id AS ID, ROUND(total_play_duration_sec, 2) AS '游玩时长(秒)', play_days_total AS '游玩天数', island_level_max AS '岛屿等级', first_login_date AS '首次登录日期', total_money AS '总金币数' FROM gameplay_player_summary gps WHERE COALESCE((SELECT us.is_developer FROM user_sessions us WHERE us.user_id = gps.user_id), 0) = 0 ORDER BY total_play_duration_sec DESC",
          "refId": "A"
        }
      ],
      "title": "玩家列表",
      "type": "table"
    }
  ],
  "preload": false,
  "refresh": "5m",
  "schemaVersion": 42,
  "tags": [ "gameplay", "analytics", "sqlite" ],
  "templating": {
    "list": [
      {
        "current": { "text": "SQLite", "value": "SQLite" },
        "includeAll": false,
        "label": "SQLite Datasource",
        "name": "DS_SQLITE",
        "options": [],
        "query": "frser-sqlite-datasource",
        "refresh": 1,
        "regex": "",
        "type": "datasource"
      }
    ]
  },
  "time": { "from": "now-180d", "to": "now" },
  "timepicker": {},
  "timezone": "browser",
  "title": "Gameplay 数据总览",
  "uid": "gameplay-overview",
  "version": 1
}
