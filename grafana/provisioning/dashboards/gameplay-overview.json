{
  "uid": "gameplay-overview",
  "title": "总览",
  "schemaVersion": 39,
  "version": 1,
  "timezone": "Asia/Shanghai",
  "refresh": "1m",
  "time": {
    "from": "now-7d",
    "to": "now"
  },
  "tags": [
    "PawFishing",
    "analytics-v2"
  ],
  "templating": {
    "list": [
      {
        "name": "DS_SQLITE",
        "type": "datasource",
        "query": "frser-sqlite-datasource",
        "current": {
          "text": "SQLite",
          "value": "SQLite"
        },
        "hide": 2
      },
      {
        "name": "client_platform",
        "label": "客户端平台",
        "type": "custom",
        "query": "WebGL : webgl,Windows Player : windows,Unity Editor : editor,其他客户端 : other,未知/历史未上报 : unknown,无客户端归属 : unattributed",
        "multi": true,
        "includeAll": true,
        "allValue": "'__all__'",
        "current": {
          "text": [
            "WebGL",
            "Windows Player",
            "未知/历史未上报"
          ],
          "value": [
            "webgl",
            "windows",
            "unknown"
          ]
        }
      },
      {
        "name": "release_version",
        "label": "发布版本",
        "type": "query",
        "datasource": {
          "type": "frser-sqlite-datasource",
          "uid": "${DS_SQLITE}"
        },
        "query": "SELECT DISTINCT COALESCE(NULLIF(release_version,''),'__empty__') AS __text,COALESCE(NULLIF(release_version,''),'__empty__') AS __value FROM analytics_sessions ORDER BY 1 DESC",
        "refresh": 1,
        "multi": true,
        "includeAll": true,
        "allValue": "'__all__'",
        "current": {
          "text": "All",
          "value": "$__all"
        }
      },
      {
        "name": "test_data",
        "label": "测试数据",
        "type": "custom",
        "query": "按平台自动 : auto,排除 : exclude,包含 : include,仅测试 : only",
        "current": {
          "text": "按平台自动",
          "value": "auto"
        }
      },
      {
        "name": "user_id",
        "label": "玩家 ID（留空=全部）",
        "type": "textbox",
        "current": {
          "text": "",
          "value": ""
        },
        "hide": 2
      },
      {
        "name": "session_id",
        "label": "会话 ID（留空=全部）",
        "type": "textbox",
        "current": {
          "text": "",
          "value": ""
        },
        "hide": 2
      },
      {
        "name": "llm_request_id",
        "label": "AI 请求 ID",
        "type": "textbox",
        "current": {
          "text": "",
          "value": ""
        },
        "hide": 2
      },
      {
        "name": "page",
        "label": "页码（从0起，每页200条）",
        "type": "textbox",
        "current": {
          "text": "0",
          "value": "0"
        }
      },
      {
        "name": "behavior_period",
        "label": "行为范围",
        "type": "custom",
        "query": "历史累计 : all,所选时间 : range",
        "current": {
          "text": "历史累计",
          "value": "all"
        }
      },
      {
        "name": "event_type",
        "label": "事件类型（空=全部）",
        "type": "textbox",
        "current": {
          "text": "",
          "value": ""
        },
        "hide": 2
      },
      {
        "name": "sort_field",
        "label": "排序字段",
        "type": "custom",
        "query": "游玩时长 : play_seconds,游戏日数 : play_days,岛屿等级 : island_level,首次登录 : first_login,最新登录 : latest_login,金币余额 : money",
        "current": {
          "text": "最新登录",
          "value": "latest_login"
        },
        "hide": 2
      },
      {
        "name": "sort_direction",
        "label": "排序方向",
        "type": "custom",
        "query": "倒序 : desc,正序 : asc",
        "current": {
          "text": "倒序",
          "value": "desc"
        },
        "hide": 2
      },
      {
        "name": "theater_event_id",
        "label": "小剧场事件 ID",
        "type": "textbox",
        "current": {
          "text": "",
          "value": ""
        }
      },
      {
        "name": "min_invitations",
        "label": "Top 5 最少邀请数",
        "type": "textbox",
        "current": {
          "text": "10",
          "value": "10"
        }
      }
    ]
  },
  "links": [
    {
      "title": "总览",
      "type": "link",
      "url": "/d/gameplay-overview",
      "includeVars": true,
      "keepTime": true
    },
    {
      "title": "玩家一览",
      "type": "link",
      "url": "/d/gameplay-players",
      "includeVars": true,
      "keepTime": true
    },
    {
      "title": "其他运营报表",
      "type": "link",
      "url": "/d/gameplay-retention",
      "includeVars": true,
      "keepTime": true
    }
  ],
  "panels": [
    {
      "id": 999,
      "title": "统计口径与历史数据",
      "type": "text",
      "options": {
        "mode": "markdown",
        "content": "**累计指标按玩家去重；当前状态按最新快照，空值=未采集。** 游戏日按玩家+存档+游戏日去重，旧数据缺存档标识不猜测。金币为当前余额。行为默认历史累计，可切换期间；未知平台保留。新指标需新版客户端/服务器。体力使用实际扣除事件；旧版失败钓鱼无法补采。"
      },
      "gridPos": {
        "x": 0,
        "y": 0,
        "w": 24,
        "h": 4
      },
      "fieldConfig": {
        "defaults": {
          "noValue": "未采集"
        }
      }
    },
    {
      "id": 100,
      "title": "玩家数与新增",
      "type": "table",
      "datasource": {
        "type": "frser-sqlite-datasource",
        "uid": "${DS_SQLITE}"
      },
      "description": "累计按玩家去重；当前状态取最新快照。缺失显示未采集，数值零保留。行为可切换期间，日期为北京时间。",
      "targets": [
        {
          "refId": "A",
          "queryType": "table",
          "queryText": "WITH e0 AS NOT MATERIALIZED (SELECT * FROM analytics_events WHERE ('__all__' IN (${client_platform:sqlstring}) OR client_platform IN (${client_platform:sqlstring})) AND ('__all__' IN (${release_version:sqlstring}) OR COALESCE(NULLIF(release_version,''),'__empty__') IN (${release_version:sqlstring})) AND ('${test_data}'='include' OR ('${test_data}'='auto' AND (client_platform='editor' OR (is_development_build=0 AND is_developer=0))) OR ('${test_data}'='exclude' AND is_development_build=0 AND is_developer=0 AND client_platform!='editor') OR ('${test_data}'='only' AND (is_development_build=1 OR is_developer=1 OR client_platform='editor')))),\n    s AS MATERIALIZED (SELECT * FROM analytics_sessions WHERE ('__all__' IN (${client_platform:sqlstring}) OR client_platform IN (${client_platform:sqlstring})) AND ('__all__' IN (${release_version:sqlstring}) OR COALESCE(NULLIF(release_version,''),'__empty__') IN (${release_version:sqlstring})) AND ('${test_data}'='include' OR ('${test_data}'='auto' AND (client_platform='editor' OR (is_development_build=0 AND is_developer=0))) OR ('${test_data}'='exclude' AND is_development_build=0 AND is_developer=0 AND client_platform!='editor') OR ('${test_data}'='only' AND (is_development_build=1 OR is_developer=1 OR client_platform='editor')))),\n    ids AS (SELECT user_id FROM s UNION SELECT user_id FROM e0),\n    states AS (SELECT *,ROW_NUMBER() OVER(PARTITION BY user_id ORDER BY julianday(occurred_at) DESC,sequence DESC,event_id DESC) rn\n        FROM e0 WHERE event_type='player_state_snapshot'),\n    legacy AS (SELECT g.*,ROW_NUMBER() OVER(PARTITION BY user_id ORDER BY julianday(real_time_started_iso) DESC,imported_at DESC) rn\n        FROM gameplay_sessions g WHERE EXISTS(SELECT 1 FROM s WHERE s.user_id=g.user_id AND s.session_id=g.session_id)),\n    days AS (SELECT user_id,COUNT(*) play_days FROM\n        (SELECT DISTINCT user_id,archive_id,game_day FROM e0 WHERE NULLIF(archive_id,'') IS NOT NULL AND game_day IS NOT NULL) GROUP BY user_id),\n    p AS (SELECT ids.user_id,COALESCE(NULLIF(json_extract(st.payload_json,'$.nickname'),''),NULLIF(u.nickname,''),NULLIF(u.player_name,''),ids.user_id) nickname,\n        (SELECT SUM(duration_sec) FROM s WHERE s.user_id=ids.user_id) play_seconds,\n        (SELECT first_entry_at FROM analytics_first_entry f WHERE f.user_id=ids.user_id AND f.cohort_scope=CASE WHEN '${test_data}' IN ('include','only') OR ('${test_data}'='auto' AND ('editor' IN (${client_platform:sqlstring}) OR '__all__' IN (${client_platform:sqlstring}))) THEN 'all' ELSE 'production' END) first_login,\n        (SELECT MAX(started_at) FROM s WHERE s.user_id=ids.user_id) latest_login,\n        d.play_days,COALESCE(json_extract(st.payload_json,'$.money'),l.money_end,l.money_start) money,\n        COALESCE(json_extract(st.payload_json,'$.island_level'),l.island_level_max) island_level,\n        json_array_length(json_extract(st.payload_json,'$.cats')) cat_count,\n        st.payload_json state,st.occurred_at state_at,\n        json_extract(st.payload_json,'$.cat_house_count') house_count,\n        json_extract(st.payload_json,'$.cat_house_capacity') house_capacity,\n        json_extract(st.payload_json,'$.cat_house_occupied') house_occupied\n        FROM ids LEFT JOIN user_sessions u USING(user_id) LEFT JOIN states st ON st.user_id=ids.user_id AND st.rn=1\n        LEFT JOIN legacy l ON l.user_id=ids.user_id AND l.rn=1 LEFT JOIN days d USING(user_id)),\n    e AS (SELECT * FROM e0 WHERE ('${behavior_period}'='all' OR julianday(occurred_at) >= julianday(${__from}/1000,'unixepoch') AND julianday(occurred_at) < julianday(${__to}/1000,'unixepoch'))\n        OR (occurred_at IS NULL AND '${behavior_period}'='all')),\n    players AS (SELECT * FROM p WHERE 1),\n    events AS (SELECT * FROM e WHERE 1) SELECT COUNT(*) 总玩家数,\n        SUM(date(first_login,'+8 hours')=date('now','+8 hours','-1 day')) 昨日新增,\n        SUM(date(first_login,'+8 hours')>=date('now','+8 hours','-7 days') AND date(first_login,'+8 hours')<date('now','+8 hours')) 七日新增,\n        SUM(date(first_login,'+8 hours')>=date('now','+8 hours','-30 days') AND date(first_login,'+8 hours')<date('now','+8 hours')) 三十日新增 FROM players",
          "rawQueryText": "WITH e0 AS NOT MATERIALIZED (SELECT * FROM analytics_events WHERE ('__all__' IN (${client_platform:sqlstring}) OR client_platform IN (${client_platform:sqlstring})) AND ('__all__' IN (${release_version:sqlstring}) OR COALESCE(NULLIF(release_version,''),'__empty__') IN (${release_version:sqlstring})) AND ('${test_data}'='include' OR ('${test_data}'='auto' AND (client_platform='editor' OR (is_development_build=0 AND is_developer=0))) OR ('${test_data}'='exclude' AND is_development_build=0 AND is_developer=0 AND client_platform!='editor') OR ('${test_data}'='only' AND (is_development_build=1 OR is_developer=1 OR client_platform='editor')))),\n    s AS MATERIALIZED (SELECT * FROM analytics_sessions WHERE ('__all__' IN (${client_platform:sqlstring}) OR client_platform IN (${client_platform:sqlstring})) AND ('__all__' IN (${release_version:sqlstring}) OR COALESCE(NULLIF(release_version,''),'__empty__') IN (${release_version:sqlstring})) AND ('${test_data}'='include' OR ('${test_data}'='auto' AND (client_platform='editor' OR (is_development_build=0 AND is_developer=0))) OR ('${test_data}'='exclude' AND is_development_build=0 AND is_developer=0 AND client_platform!='editor') OR ('${test_data}'='only' AND (is_development_build=1 OR is_developer=1 OR client_platform='editor')))),\n    ids AS (SELECT user_id FROM s UNION SELECT user_id FROM e0),\n    states AS (SELECT *,ROW_NUMBER() OVER(PARTITION BY user_id ORDER BY julianday(occurred_at) DESC,sequence DESC,event_id DESC) rn\n        FROM e0 WHERE event_type='player_state_snapshot'),\n    legacy AS (SELECT g.*,ROW_NUMBER() OVER(PARTITION BY user_id ORDER BY julianday(real_time_started_iso) DESC,imported_at DESC) rn\n        FROM gameplay_sessions g WHERE EXISTS(SELECT 1 FROM s WHERE s.user_id=g.user_id AND s.session_id=g.session_id)),\n    days AS (SELECT user_id,COUNT(*) play_days FROM\n        (SELECT DISTINCT user_id,archive_id,game_day FROM e0 WHERE NULLIF(archive_id,'') IS NOT NULL AND game_day IS NOT NULL) GROUP BY user_id),\n    p AS (SELECT ids.user_id,COALESCE(NULLIF(json_extract(st.payload_json,'$.nickname'),''),NULLIF(u.nickname,''),NULLIF(u.player_name,''),ids.user_id) nickname,\n        (SELECT SUM(duration_sec) FROM s WHERE s.user_id=ids.user_id) play_seconds,\n        (SELECT first_entry_at FROM analytics_first_entry f WHERE f.user_id=ids.user_id AND f.cohort_scope=CASE WHEN '${test_data}' IN ('include','only') OR ('${test_data}'='auto' AND ('editor' IN (${client_platform:sqlstring}) OR '__all__' IN (${client_platform:sqlstring}))) THEN 'all' ELSE 'production' END) first_login,\n        (SELECT MAX(started_at) FROM s WHERE s.user_id=ids.user_id) latest_login,\n        d.play_days,COALESCE(json_extract(st.payload_json,'$.money'),l.money_end,l.money_start) money,\n        COALESCE(json_extract(st.payload_json,'$.island_level'),l.island_level_max) island_level,\n        json_array_length(json_extract(st.payload_json,'$.cats')) cat_count,\n        st.payload_json state,st.occurred_at state_at,\n        json_extract(st.payload_json,'$.cat_house_count') house_count,\n        json_extract(st.payload_json,'$.cat_house_capacity') house_capacity,\n        json_extract(st.payload_json,'$.cat_house_occupied') house_occupied\n        FROM ids LEFT JOIN user_sessions u USING(user_id) LEFT JOIN states st ON st.user_id=ids.user_id AND st.rn=1\n        LEFT JOIN legacy l ON l.user_id=ids.user_id AND l.rn=1 LEFT JOIN days d USING(user_id)),\n    e AS (SELECT * FROM e0 WHERE ('${behavior_period}'='all' OR julianday(occurred_at) >= julianday(${__from}/1000,'unixepoch') AND julianday(occurred_at) < julianday(${__to}/1000,'unixepoch'))\n        OR (occurred_at IS NULL AND '${behavior_period}'='all')),\n    players AS (SELECT * FROM p WHERE 1),\n    events AS (SELECT * FROM e WHERE 1) SELECT COUNT(*) 总玩家数,\n        SUM(date(first_login,'+8 hours')=date('now','+8 hours','-1 day')) 昨日新增,\n        SUM(date(first_login,'+8 hours')>=date('now','+8 hours','-7 days') AND date(first_login,'+8 hours')<date('now','+8 hours')) 七日新增,\n        SUM(date(first_login,'+8 hours')>=date('now','+8 hours','-30 days') AND date(first_login,'+8 hours')<date('now','+8 hours')) 三十日新增 FROM players"
        }
      ],
      "fieldConfig": {
        "defaults": {
          "unit": "short",
          "custom": {
            "filterable": true
          },
          "noValue": "未采集"
        },
        "overrides": [
          {
            "matcher": {
              "id": "byName",
              "options": "user_id"
            },
            "properties": [
              {
                "id": "links",
                "value": [
                  {
                    "title": "查看玩家旅程",
                    "url": "/d/gameplay-player-detail?${__url_time_range}&${client_platform:queryparam}&${release_version:queryparam}&${test_data:queryparam}&var-user_id=${__data.fields.user_id}&var-user_id=${__value.raw}"
                  }
                ]
              }
            ]
          },
          {
            "matcher": {
              "id": "byName",
              "options": "session_id"
            },
            "properties": [
              {
                "id": "links",
                "value": [
                  {
                    "title": "查看玩家旅程",
                    "url": "/d/gameplay-player-detail?${__url_time_range}&${client_platform:queryparam}&${release_version:queryparam}&${test_data:queryparam}&var-user_id=${__data.fields.user_id}&var-session_id=${__value.raw}"
                  }
                ]
              }
            ]
          },
          {
            "matcher": {
              "id": "byName",
              "options": "llm_request_id"
            },
            "properties": [
              {
                "id": "links",
                "value": [
                  {
                    "title": "查看玩家旅程",
                    "url": "/d/gameplay-player-detail?${__url_time_range}&${client_platform:queryparam}&${release_version:queryparam}&${test_data:queryparam}&var-user_id=${__data.fields.user_id}&var-llm_request_id=${__value.raw}"
                  }
                ]
              }
            ]
          }
        ]
      },
      "options": {
        "showHeader": true,
        "cellHeight": "sm"
      },
      "gridPos": {
        "x": 0,
        "y": 4,
        "w": 24,
        "h": 6
      }
    },
    {
      "id": 101,
      "title": "金币与猫总量",
      "type": "table",
      "datasource": {
        "type": "frser-sqlite-datasource",
        "uid": "${DS_SQLITE}"
      },
      "description": "累计按玩家去重；当前状态取最新快照。缺失显示未采集，数值零保留。行为可切换期间，日期为北京时间。",
      "targets": [
        {
          "refId": "A",
          "queryType": "table",
          "queryText": "WITH e0 AS NOT MATERIALIZED (SELECT * FROM analytics_events WHERE ('__all__' IN (${client_platform:sqlstring}) OR client_platform IN (${client_platform:sqlstring})) AND ('__all__' IN (${release_version:sqlstring}) OR COALESCE(NULLIF(release_version,''),'__empty__') IN (${release_version:sqlstring})) AND ('${test_data}'='include' OR ('${test_data}'='auto' AND (client_platform='editor' OR (is_development_build=0 AND is_developer=0))) OR ('${test_data}'='exclude' AND is_development_build=0 AND is_developer=0 AND client_platform!='editor') OR ('${test_data}'='only' AND (is_development_build=1 OR is_developer=1 OR client_platform='editor')))),\n    s AS MATERIALIZED (SELECT * FROM analytics_sessions WHERE ('__all__' IN (${client_platform:sqlstring}) OR client_platform IN (${client_platform:sqlstring})) AND ('__all__' IN (${release_version:sqlstring}) OR COALESCE(NULLIF(release_version,''),'__empty__') IN (${release_version:sqlstring})) AND ('${test_data}'='include' OR ('${test_data}'='auto' AND (client_platform='editor' OR (is_development_build=0 AND is_developer=0))) OR ('${test_data}'='exclude' AND is_development_build=0 AND is_developer=0 AND client_platform!='editor') OR ('${test_data}'='only' AND (is_development_build=1 OR is_developer=1 OR client_platform='editor')))),\n    ids AS (SELECT user_id FROM s UNION SELECT user_id FROM e0),\n    states AS (SELECT *,ROW_NUMBER() OVER(PARTITION BY user_id ORDER BY julianday(occurred_at) DESC,sequence DESC,event_id DESC) rn\n        FROM e0 WHERE event_type='player_state_snapshot'),\n    legacy AS (SELECT g.*,ROW_NUMBER() OVER(PARTITION BY user_id ORDER BY julianday(real_time_started_iso) DESC,imported_at DESC) rn\n        FROM gameplay_sessions g WHERE EXISTS(SELECT 1 FROM s WHERE s.user_id=g.user_id AND s.session_id=g.session_id)),\n    days AS (SELECT user_id,COUNT(*) play_days FROM\n        (SELECT DISTINCT user_id,archive_id,game_day FROM e0 WHERE NULLIF(archive_id,'') IS NOT NULL AND game_day IS NOT NULL) GROUP BY user_id),\n    p AS (SELECT ids.user_id,COALESCE(NULLIF(json_extract(st.payload_json,'$.nickname'),''),NULLIF(u.nickname,''),NULLIF(u.player_name,''),ids.user_id) nickname,\n        (SELECT SUM(duration_sec) FROM s WHERE s.user_id=ids.user_id) play_seconds,\n        (SELECT first_entry_at FROM analytics_first_entry f WHERE f.user_id=ids.user_id AND f.cohort_scope=CASE WHEN '${test_data}' IN ('include','only') OR ('${test_data}'='auto' AND ('editor' IN (${client_platform:sqlstring}) OR '__all__' IN (${client_platform:sqlstring}))) THEN 'all' ELSE 'production' END) first_login,\n        (SELECT MAX(started_at) FROM s WHERE s.user_id=ids.user_id) latest_login,\n        d.play_days,COALESCE(json_extract(st.payload_json,'$.money'),l.money_end,l.money_start) money,\n        COALESCE(json_extract(st.payload_json,'$.island_level'),l.island_level_max) island_level,\n        json_array_length(json_extract(st.payload_json,'$.cats')) cat_count,\n        st.payload_json state,st.occurred_at state_at,\n        json_extract(st.payload_json,'$.cat_house_count') house_count,\n        json_extract(st.payload_json,'$.cat_house_capacity') house_capacity,\n        json_extract(st.payload_json,'$.cat_house_occupied') house_occupied\n        FROM ids LEFT JOIN user_sessions u USING(user_id) LEFT JOIN states st ON st.user_id=ids.user_id AND st.rn=1\n        LEFT JOIN legacy l ON l.user_id=ids.user_id AND l.rn=1 LEFT JOIN days d USING(user_id)),\n    e AS (SELECT * FROM e0 WHERE ('${behavior_period}'='all' OR julianday(occurred_at) >= julianday(${__from}/1000,'unixepoch') AND julianday(occurred_at) < julianday(${__to}/1000,'unixepoch'))\n        OR (occurred_at IS NULL AND '${behavior_period}'='all')),\n    players AS (SELECT * FROM p WHERE 1),\n    events AS (SELECT * FROM e WHERE 1) SELECT SUM(money) 总金币余额,COUNT(money) 金币有效玩家数,SUM(cat_count) 拥有猫总数,COUNT(cat_count) 猫有效玩家数,COUNT(*) 总玩家数 FROM players",
          "rawQueryText": "WITH e0 AS NOT MATERIALIZED (SELECT * FROM analytics_events WHERE ('__all__' IN (${client_platform:sqlstring}) OR client_platform IN (${client_platform:sqlstring})) AND ('__all__' IN (${release_version:sqlstring}) OR COALESCE(NULLIF(release_version,''),'__empty__') IN (${release_version:sqlstring})) AND ('${test_data}'='include' OR ('${test_data}'='auto' AND (client_platform='editor' OR (is_development_build=0 AND is_developer=0))) OR ('${test_data}'='exclude' AND is_development_build=0 AND is_developer=0 AND client_platform!='editor') OR ('${test_data}'='only' AND (is_development_build=1 OR is_developer=1 OR client_platform='editor')))),\n    s AS MATERIALIZED (SELECT * FROM analytics_sessions WHERE ('__all__' IN (${client_platform:sqlstring}) OR client_platform IN (${client_platform:sqlstring})) AND ('__all__' IN (${release_version:sqlstring}) OR COALESCE(NULLIF(release_version,''),'__empty__') IN (${release_version:sqlstring})) AND ('${test_data}'='include' OR ('${test_data}'='auto' AND (client_platform='editor' OR (is_development_build=0 AND is_developer=0))) OR ('${test_data}'='exclude' AND is_development_build=0 AND is_developer=0 AND client_platform!='editor') OR ('${test_data}'='only' AND (is_development_build=1 OR is_developer=1 OR client_platform='editor')))),\n    ids AS (SELECT user_id FROM s UNION SELECT user_id FROM e0),\n    states AS (SELECT *,ROW_NUMBER() OVER(PARTITION BY user_id ORDER BY julianday(occurred_at) DESC,sequence DESC,event_id DESC) rn\n        FROM e0 WHERE event_type='player_state_snapshot'),\n    legacy AS (SELECT g.*,ROW_NUMBER() OVER(PARTITION BY user_id ORDER BY julianday(real_time_started_iso) DESC,imported_at DESC) rn\n        FROM gameplay_sessions g WHERE EXISTS(SELECT 1 FROM s WHERE s.user_id=g.user_id AND s.session_id=g.session_id)),\n    days AS (SELECT user_id,COUNT(*) play_days FROM\n        (SELECT DISTINCT user_id,archive_id,game_day FROM e0 WHERE NULLIF(archive_id,'') IS NOT NULL AND game_day IS NOT NULL) GROUP BY user_id),\n    p AS (SELECT ids.user_id,COALESCE(NULLIF(json_extract(st.payload_json,'$.nickname'),''),NULLIF(u.nickname,''),NULLIF(u.player_name,''),ids.user_id) nickname,\n        (SELECT SUM(duration_sec) FROM s WHERE s.user_id=ids.user_id) play_seconds,\n        (SELECT first_entry_at FROM analytics_first_entry f WHERE f.user_id=ids.user_id AND f.cohort_scope=CASE WHEN '${test_data}' IN ('include','only') OR ('${test_data}'='auto' AND ('editor' IN (${client_platform:sqlstring}) OR '__all__' IN (${client_platform:sqlstring}))) THEN 'all' ELSE 'production' END) first_login,\n        (SELECT MAX(started_at) FROM s WHERE s.user_id=ids.user_id) latest_login,\n        d.play_days,COALESCE(json_extract(st.payload_json,'$.money'),l.money_end,l.money_start) money,\n        COALESCE(json_extract(st.payload_json,'$.island_level'),l.island_level_max) island_level,\n        json_array_length(json_extract(st.payload_json,'$.cats')) cat_count,\n        st.payload_json state,st.occurred_at state_at,\n        json_extract(st.payload_json,'$.cat_house_count') house_count,\n        json_extract(st.payload_json,'$.cat_house_capacity') house_capacity,\n        json_extract(st.payload_json,'$.cat_house_occupied') house_occupied\n        FROM ids LEFT JOIN user_sessions u USING(user_id) LEFT JOIN states st ON st.user_id=ids.user_id AND st.rn=1\n        LEFT JOIN legacy l ON l.user_id=ids.user_id AND l.rn=1 LEFT JOIN days d USING(user_id)),\n    e AS (SELECT * FROM e0 WHERE ('${behavior_period}'='all' OR julianday(occurred_at) >= julianday(${__from}/1000,'unixepoch') AND julianday(occurred_at) < julianday(${__to}/1000,'unixepoch'))\n        OR (occurred_at IS NULL AND '${behavior_period}'='all')),\n    players AS (SELECT * FROM p WHERE 1),\n    events AS (SELECT * FROM e WHERE 1) SELECT SUM(money) 总金币余额,COUNT(money) 金币有效玩家数,SUM(cat_count) 拥有猫总数,COUNT(cat_count) 猫有效玩家数,COUNT(*) 总玩家数 FROM players"
        }
      ],
      "fieldConfig": {
        "defaults": {
          "unit": "short",
          "custom": {
            "filterable": true
          },
          "noValue": "未采集"
        },
        "overrides": [
          {
            "matcher": {
              "id": "byName",
              "options": "user_id"
            },
            "properties": [
              {
                "id": "links",
                "value": [
                  {
                    "title": "查看玩家旅程",
                    "url": "/d/gameplay-player-detail?${__url_time_range}&${client_platform:queryparam}&${release_version:queryparam}&${test_data:queryparam}&var-user_id=${__data.fields.user_id}&var-user_id=${__value.raw}"
                  }
                ]
              }
            ]
          },
          {
            "matcher": {
              "id": "byName",
              "options": "session_id"
            },
            "properties": [
              {
                "id": "links",
                "value": [
                  {
                    "title": "查看玩家旅程",
                    "url": "/d/gameplay-player-detail?${__url_time_range}&${client_platform:queryparam}&${release_version:queryparam}&${test_data:queryparam}&var-user_id=${__data.fields.user_id}&var-session_id=${__value.raw}"
                  }
                ]
              }
            ]
          },
          {
            "matcher": {
              "id": "byName",
              "options": "llm_request_id"
            },
            "properties": [
              {
                "id": "links",
                "value": [
                  {
                    "title": "查看玩家旅程",
                    "url": "/d/gameplay-player-detail?${__url_time_range}&${client_platform:queryparam}&${release_version:queryparam}&${test_data:queryparam}&var-user_id=${__data.fields.user_id}&var-llm_request_id=${__value.raw}"
                  }
                ]
              }
            ]
          }
        ]
      },
      "options": {
        "showHeader": true,
        "cellHeight": "sm"
      },
      "gridPos": {
        "x": 0,
        "y": 10,
        "w": 12,
        "h": 6
      }
    },
    {
      "id": 102,
      "title": "游玩时长",
      "type": "table",
      "datasource": {
        "type": "frser-sqlite-datasource",
        "uid": "${DS_SQLITE}"
      },
      "description": "累计按玩家去重；当前状态取最新快照。缺失显示未采集，数值零保留。行为可切换期间，日期为北京时间。",
      "targets": [
        {
          "refId": "A",
          "queryType": "table",
          "queryText": "WITH e0 AS NOT MATERIALIZED (SELECT * FROM analytics_events WHERE ('__all__' IN (${client_platform:sqlstring}) OR client_platform IN (${client_platform:sqlstring})) AND ('__all__' IN (${release_version:sqlstring}) OR COALESCE(NULLIF(release_version,''),'__empty__') IN (${release_version:sqlstring})) AND ('${test_data}'='include' OR ('${test_data}'='auto' AND (client_platform='editor' OR (is_development_build=0 AND is_developer=0))) OR ('${test_data}'='exclude' AND is_development_build=0 AND is_developer=0 AND client_platform!='editor') OR ('${test_data}'='only' AND (is_development_build=1 OR is_developer=1 OR client_platform='editor')))),\n    s AS MATERIALIZED (SELECT * FROM analytics_sessions WHERE ('__all__' IN (${client_platform:sqlstring}) OR client_platform IN (${client_platform:sqlstring})) AND ('__all__' IN (${release_version:sqlstring}) OR COALESCE(NULLIF(release_version,''),'__empty__') IN (${release_version:sqlstring})) AND ('${test_data}'='include' OR ('${test_data}'='auto' AND (client_platform='editor' OR (is_development_build=0 AND is_developer=0))) OR ('${test_data}'='exclude' AND is_development_build=0 AND is_developer=0 AND client_platform!='editor') OR ('${test_data}'='only' AND (is_development_build=1 OR is_developer=1 OR client_platform='editor')))),\n    ids AS (SELECT user_id FROM s UNION SELECT user_id FROM e0),\n    states AS (SELECT *,ROW_NUMBER() OVER(PARTITION BY user_id ORDER BY julianday(occurred_at) DESC,sequence DESC,event_id DESC) rn\n        FROM e0 WHERE event_type='player_state_snapshot'),\n    legacy AS (SELECT g.*,ROW_NUMBER() OVER(PARTITION BY user_id ORDER BY julianday(real_time_started_iso) DESC,imported_at DESC) rn\n        FROM gameplay_sessions g WHERE EXISTS(SELECT 1 FROM s WHERE s.user_id=g.user_id AND s.session_id=g.session_id)),\n    days AS (SELECT user_id,COUNT(*) play_days FROM\n        (SELECT DISTINCT user_id,archive_id,game_day FROM e0 WHERE NULLIF(archive_id,'') IS NOT NULL AND game_day IS NOT NULL) GROUP BY user_id),\n    p AS (SELECT ids.user_id,COALESCE(NULLIF(json_extract(st.payload_json,'$.nickname'),''),NULLIF(u.nickname,''),NULLIF(u.player_name,''),ids.user_id) nickname,\n        (SELECT SUM(duration_sec) FROM s WHERE s.user_id=ids.user_id) play_seconds,\n        (SELECT first_entry_at FROM analytics_first_entry f WHERE f.user_id=ids.user_id AND f.cohort_scope=CASE WHEN '${test_data}' IN ('include','only') OR ('${test_data}'='auto' AND ('editor' IN (${client_platform:sqlstring}) OR '__all__' IN (${client_platform:sqlstring}))) THEN 'all' ELSE 'production' END) first_login,\n        (SELECT MAX(started_at) FROM s WHERE s.user_id=ids.user_id) latest_login,\n        d.play_days,COALESCE(json_extract(st.payload_json,'$.money'),l.money_end,l.money_start) money,\n        COALESCE(json_extract(st.payload_json,'$.island_level'),l.island_level_max) island_level,\n        json_array_length(json_extract(st.payload_json,'$.cats')) cat_count,\n        st.payload_json state,st.occurred_at state_at,\n        json_extract(st.payload_json,'$.cat_house_count') house_count,\n        json_extract(st.payload_json,'$.cat_house_capacity') house_capacity,\n        json_extract(st.payload_json,'$.cat_house_occupied') house_occupied\n        FROM ids LEFT JOIN user_sessions u USING(user_id) LEFT JOIN states st ON st.user_id=ids.user_id AND st.rn=1\n        LEFT JOIN legacy l ON l.user_id=ids.user_id AND l.rn=1 LEFT JOIN days d USING(user_id)),\n    e AS (SELECT * FROM e0 WHERE ('${behavior_period}'='all' OR julianday(occurred_at) >= julianday(${__from}/1000,'unixepoch') AND julianday(occurred_at) < julianday(${__to}/1000,'unixepoch'))\n        OR (occurred_at IS NULL AND '${behavior_period}'='all')),\n    players AS (SELECT * FROM p WHERE 1),\n    events AS (SELECT * FROM e WHERE 1) SELECT ROUND(AVG(v)/60.0,2) 平均数,\n            ROUND(AVG(CASE WHEN rn IN ((n+1)/2,(n+2)/2) THEN v END)/60.0,2) 中位数,COUNT(*) 有效玩家数\n            FROM (SELECT play_seconds v,ROW_NUMBER() OVER(ORDER BY play_seconds) rn,COUNT(*) OVER() n FROM players WHERE play_seconds IS NOT NULL)",
          "rawQueryText": "WITH e0 AS NOT MATERIALIZED (SELECT * FROM analytics_events WHERE ('__all__' IN (${client_platform:sqlstring}) OR client_platform IN (${client_platform:sqlstring})) AND ('__all__' IN (${release_version:sqlstring}) OR COALESCE(NULLIF(release_version,''),'__empty__') IN (${release_version:sqlstring})) AND ('${test_data}'='include' OR ('${test_data}'='auto' AND (client_platform='editor' OR (is_development_build=0 AND is_developer=0))) OR ('${test_data}'='exclude' AND is_development_build=0 AND is_developer=0 AND client_platform!='editor') OR ('${test_data}'='only' AND (is_development_build=1 OR is_developer=1 OR client_platform='editor')))),\n    s AS MATERIALIZED (SELECT * FROM analytics_sessions WHERE ('__all__' IN (${client_platform:sqlstring}) OR client_platform IN (${client_platform:sqlstring})) AND ('__all__' IN (${release_version:sqlstring}) OR COALESCE(NULLIF(release_version,''),'__empty__') IN (${release_version:sqlstring})) AND ('${test_data}'='include' OR ('${test_data}'='auto' AND (client_platform='editor' OR (is_development_build=0 AND is_developer=0))) OR ('${test_data}'='exclude' AND is_development_build=0 AND is_developer=0 AND client_platform!='editor') OR ('${test_data}'='only' AND (is_development_build=1 OR is_developer=1 OR client_platform='editor')))),\n    ids AS (SELECT user_id FROM s UNION SELECT user_id FROM e0),\n    states AS (SELECT *,ROW_NUMBER() OVER(PARTITION BY user_id ORDER BY julianday(occurred_at) DESC,sequence DESC,event_id DESC) rn\n        FROM e0 WHERE event_type='player_state_snapshot'),\n    legacy AS (SELECT g.*,ROW_NUMBER() OVER(PARTITION BY user_id ORDER BY julianday(real_time_started_iso) DESC,imported_at DESC) rn\n        FROM gameplay_sessions g WHERE EXISTS(SELECT 1 FROM s WHERE s.user_id=g.user_id AND s.session_id=g.session_id)),\n    days AS (SELECT user_id,COUNT(*) play_days FROM\n        (SELECT DISTINCT user_id,archive_id,game_day FROM e0 WHERE NULLIF(archive_id,'') IS NOT NULL AND game_day IS NOT NULL) GROUP BY user_id),\n    p AS (SELECT ids.user_id,COALESCE(NULLIF(json_extract(st.payload_json,'$.nickname'),''),NULLIF(u.nickname,''),NULLIF(u.player_name,''),ids.user_id) nickname,\n        (SELECT SUM(duration_sec) FROM s WHERE s.user_id=ids.user_id) play_seconds,\n        (SELECT first_entry_at FROM analytics_first_entry f WHERE f.user_id=ids.user_id AND f.cohort_scope=CASE WHEN '${test_data}' IN ('include','only') OR ('${test_data}'='auto' AND ('editor' IN (${client_platform:sqlstring}) OR '__all__' IN (${client_platform:sqlstring}))) THEN 'all' ELSE 'production' END) first_login,\n        (SELECT MAX(started_at) FROM s WHERE s.user_id=ids.user_id) latest_login,\n        d.play_days,COALESCE(json_extract(st.payload_json,'$.money'),l.money_end,l.money_start) money,\n        COALESCE(json_extract(st.payload_json,'$.island_level'),l.island_level_max) island_level,\n        json_array_length(json_extract(st.payload_json,'$.cats')) cat_count,\n        st.payload_json state,st.occurred_at state_at,\n        json_extract(st.payload_json,'$.cat_house_count') house_count,\n        json_extract(st.payload_json,'$.cat_house_capacity') house_capacity,\n        json_extract(st.payload_json,'$.cat_house_occupied') house_occupied\n        FROM ids LEFT JOIN user_sessions u USING(user_id) LEFT JOIN states st ON st.user_id=ids.user_id AND st.rn=1\n        LEFT JOIN legacy l ON l.user_id=ids.user_id AND l.rn=1 LEFT JOIN days d USING(user_id)),\n    e AS (SELECT * FROM e0 WHERE ('${behavior_period}'='all' OR julianday(occurred_at) >= julianday(${__from}/1000,'unixepoch') AND julianday(occurred_at) < julianday(${__to}/1000,'unixepoch'))\n        OR (occurred_at IS NULL AND '${behavior_period}'='all')),\n    players AS (SELECT * FROM p WHERE 1),\n    events AS (SELECT * FROM e WHERE 1) SELECT ROUND(AVG(v)/60.0,2) 平均数,\n            ROUND(AVG(CASE WHEN rn IN ((n+1)/2,(n+2)/2) THEN v END)/60.0,2) 中位数,COUNT(*) 有效玩家数\n            FROM (SELECT play_seconds v,ROW_NUMBER() OVER(ORDER BY play_seconds) rn,COUNT(*) OVER() n FROM players WHERE play_seconds IS NOT NULL)"
        }
      ],
      "fieldConfig": {
        "defaults": {
          "unit": "short",
          "custom": {
            "filterable": true
          },
          "noValue": "未采集"
        },
        "overrides": [
          {
            "matcher": {
              "id": "byName",
              "options": "user_id"
            },
            "properties": [
              {
                "id": "links",
                "value": [
                  {
                    "title": "查看玩家旅程",
                    "url": "/d/gameplay-player-detail?${__url_time_range}&${client_platform:queryparam}&${release_version:queryparam}&${test_data:queryparam}&var-user_id=${__data.fields.user_id}&var-user_id=${__value.raw}"
                  }
                ]
              }
            ]
          },
          {
            "matcher": {
              "id": "byName",
              "options": "session_id"
            },
            "properties": [
              {
                "id": "links",
                "value": [
                  {
                    "title": "查看玩家旅程",
                    "url": "/d/gameplay-player-detail?${__url_time_range}&${client_platform:queryparam}&${release_version:queryparam}&${test_data:queryparam}&var-user_id=${__data.fields.user_id}&var-session_id=${__value.raw}"
                  }
                ]
              }
            ]
          },
          {
            "matcher": {
              "id": "byName",
              "options": "llm_request_id"
            },
            "properties": [
              {
                "id": "links",
                "value": [
                  {
                    "title": "查看玩家旅程",
                    "url": "/d/gameplay-player-detail?${__url_time_range}&${client_platform:queryparam}&${release_version:queryparam}&${test_data:queryparam}&var-user_id=${__data.fields.user_id}&var-llm_request_id=${__value.raw}"
                  }
                ]
              }
            ]
          }
        ]
      },
      "options": {
        "showHeader": true,
        "cellHeight": "sm"
      },
      "gridPos": {
        "x": 12,
        "y": 10,
        "w": 12,
        "h": 6
      }
    },
    {
      "id": 103,
      "title": "游玩天数（实际参与游戏日）",
      "type": "table",
      "datasource": {
        "type": "frser-sqlite-datasource",
        "uid": "${DS_SQLITE}"
      },
      "description": "累计按玩家去重；当前状态取最新快照。缺失显示未采集，数值零保留。行为可切换期间，日期为北京时间。",
      "targets": [
        {
          "refId": "A",
          "queryType": "table",
          "queryText": "WITH e0 AS NOT MATERIALIZED (SELECT * FROM analytics_events WHERE ('__all__' IN (${client_platform:sqlstring}) OR client_platform IN (${client_platform:sqlstring})) AND ('__all__' IN (${release_version:sqlstring}) OR COALESCE(NULLIF(release_version,''),'__empty__') IN (${release_version:sqlstring})) AND ('${test_data}'='include' OR ('${test_data}'='auto' AND (client_platform='editor' OR (is_development_build=0 AND is_developer=0))) OR ('${test_data}'='exclude' AND is_development_build=0 AND is_developer=0 AND client_platform!='editor') OR ('${test_data}'='only' AND (is_development_build=1 OR is_developer=1 OR client_platform='editor')))),\n    s AS MATERIALIZED (SELECT * FROM analytics_sessions WHERE ('__all__' IN (${client_platform:sqlstring}) OR client_platform IN (${client_platform:sqlstring})) AND ('__all__' IN (${release_version:sqlstring}) OR COALESCE(NULLIF(release_version,''),'__empty__') IN (${release_version:sqlstring})) AND ('${test_data}'='include' OR ('${test_data}'='auto' AND (client_platform='editor' OR (is_development_build=0 AND is_developer=0))) OR ('${test_data}'='exclude' AND is_development_build=0 AND is_developer=0 AND client_platform!='editor') OR ('${test_data}'='only' AND (is_development_build=1 OR is_developer=1 OR client_platform='editor')))),\n    ids AS (SELECT user_id FROM s UNION SELECT user_id FROM e0),\n    states AS (SELECT *,ROW_NUMBER() OVER(PARTITION BY user_id ORDER BY julianday(occurred_at) DESC,sequence DESC,event_id DESC) rn\n        FROM e0 WHERE event_type='player_state_snapshot'),\n    legacy AS (SELECT g.*,ROW_NUMBER() OVER(PARTITION BY user_id ORDER BY julianday(real_time_started_iso) DESC,imported_at DESC) rn\n        FROM gameplay_sessions g WHERE EXISTS(SELECT 1 FROM s WHERE s.user_id=g.user_id AND s.session_id=g.session_id)),\n    days AS (SELECT user_id,COUNT(*) play_days FROM\n        (SELECT DISTINCT user_id,archive_id,game_day FROM e0 WHERE NULLIF(archive_id,'') IS NOT NULL AND game_day IS NOT NULL) GROUP BY user_id),\n    p AS (SELECT ids.user_id,COALESCE(NULLIF(json_extract(st.payload_json,'$.nickname'),''),NULLIF(u.nickname,''),NULLIF(u.player_name,''),ids.user_id) nickname,\n        (SELECT SUM(duration_sec) FROM s WHERE s.user_id=ids.user_id) play_seconds,\n        (SELECT first_entry_at FROM analytics_first_entry f WHERE f.user_id=ids.user_id AND f.cohort_scope=CASE WHEN '${test_data}' IN ('include','only') OR ('${test_data}'='auto' AND ('editor' IN (${client_platform:sqlstring}) OR '__all__' IN (${client_platform:sqlstring}))) THEN 'all' ELSE 'production' END) first_login,\n        (SELECT MAX(started_at) FROM s WHERE s.user_id=ids.user_id) latest_login,\n        d.play_days,COALESCE(json_extract(st.payload_json,'$.money'),l.money_end,l.money_start) money,\n        COALESCE(json_extract(st.payload_json,'$.island_level'),l.island_level_max) island_level,\n        json_array_length(json_extract(st.payload_json,'$.cats')) cat_count,\n        st.payload_json state,st.occurred_at state_at,\n        json_extract(st.payload_json,'$.cat_house_count') house_count,\n        json_extract(st.payload_json,'$.cat_house_capacity') house_capacity,\n        json_extract(st.payload_json,'$.cat_house_occupied') house_occupied\n        FROM ids LEFT JOIN user_sessions u USING(user_id) LEFT JOIN states st ON st.user_id=ids.user_id AND st.rn=1\n        LEFT JOIN legacy l ON l.user_id=ids.user_id AND l.rn=1 LEFT JOIN days d USING(user_id)),\n    e AS (SELECT * FROM e0 WHERE ('${behavior_period}'='all' OR julianday(occurred_at) >= julianday(${__from}/1000,'unixepoch') AND julianday(occurred_at) < julianday(${__to}/1000,'unixepoch'))\n        OR (occurred_at IS NULL AND '${behavior_period}'='all')),\n    players AS (SELECT * FROM p WHERE 1),\n    events AS (SELECT * FROM e WHERE 1) SELECT ROUND(AVG(v)/1.0,2) 平均数,\n            ROUND(AVG(CASE WHEN rn IN ((n+1)/2,(n+2)/2) THEN v END)/1.0,2) 中位数,COUNT(*) 有效玩家数\n            FROM (SELECT play_days v,ROW_NUMBER() OVER(ORDER BY play_days) rn,COUNT(*) OVER() n FROM players WHERE play_days IS NOT NULL)",
          "rawQueryText": "WITH e0 AS NOT MATERIALIZED (SELECT * FROM analytics_events WHERE ('__all__' IN (${client_platform:sqlstring}) OR client_platform IN (${client_platform:sqlstring})) AND ('__all__' IN (${release_version:sqlstring}) OR COALESCE(NULLIF(release_version,''),'__empty__') IN (${release_version:sqlstring})) AND ('${test_data}'='include' OR ('${test_data}'='auto' AND (client_platform='editor' OR (is_development_build=0 AND is_developer=0))) OR ('${test_data}'='exclude' AND is_development_build=0 AND is_developer=0 AND client_platform!='editor') OR ('${test_data}'='only' AND (is_development_build=1 OR is_developer=1 OR client_platform='editor')))),\n    s AS MATERIALIZED (SELECT * FROM analytics_sessions WHERE ('__all__' IN (${client_platform:sqlstring}) OR client_platform IN (${client_platform:sqlstring})) AND ('__all__' IN (${release_version:sqlstring}) OR COALESCE(NULLIF(release_version,''),'__empty__') IN (${release_version:sqlstring})) AND ('${test_data}'='include' OR ('${test_data}'='auto' AND (client_platform='editor' OR (is_development_build=0 AND is_developer=0))) OR ('${test_data}'='exclude' AND is_development_build=0 AND is_developer=0 AND client_platform!='editor') OR ('${test_data}'='only' AND (is_development_build=1 OR is_developer=1 OR client_platform='editor')))),\n    ids AS (SELECT user_id FROM s UNION SELECT user_id FROM e0),\n    states AS (SELECT *,ROW_NUMBER() OVER(PARTITION BY user_id ORDER BY julianday(occurred_at) DESC,sequence DESC,event_id DESC) rn\n        FROM e0 WHERE event_type='player_state_snapshot'),\n    legacy AS (SELECT g.*,ROW_NUMBER() OVER(PARTITION BY user_id ORDER BY julianday(real_time_started_iso) DESC,imported_at DESC) rn\n        FROM gameplay_sessions g WHERE EXISTS(SELECT 1 FROM s WHERE s.user_id=g.user_id AND s.session_id=g.session_id)),\n    days AS (SELECT user_id,COUNT(*) play_days FROM\n        (SELECT DISTINCT user_id,archive_id,game_day FROM e0 WHERE NULLIF(archive_id,'') IS NOT NULL AND game_day IS NOT NULL) GROUP BY user_id),\n    p AS (SELECT ids.user_id,COALESCE(NULLIF(json_extract(st.payload_json,'$.nickname'),''),NULLIF(u.nickname,''),NULLIF(u.player_name,''),ids.user_id) nickname,\n        (SELECT SUM(duration_sec) FROM s WHERE s.user_id=ids.user_id) play_seconds,\n        (SELECT first_entry_at FROM analytics_first_entry f WHERE f.user_id=ids.user_id AND f.cohort_scope=CASE WHEN '${test_data}' IN ('include','only') OR ('${test_data}'='auto' AND ('editor' IN (${client_platform:sqlstring}) OR '__all__' IN (${client_platform:sqlstring}))) THEN 'all' ELSE 'production' END) first_login,\n        (SELECT MAX(started_at) FROM s WHERE s.user_id=ids.user_id) latest_login,\n        d.play_days,COALESCE(json_extract(st.payload_json,'$.money'),l.money_end,l.money_start) money,\n        COALESCE(json_extract(st.payload_json,'$.island_level'),l.island_level_max) island_level,\n        json_array_length(json_extract(st.payload_json,'$.cats')) cat_count,\n        st.payload_json state,st.occurred_at state_at,\n        json_extract(st.payload_json,'$.cat_house_count') house_count,\n        json_extract(st.payload_json,'$.cat_house_capacity') house_capacity,\n        json_extract(st.payload_json,'$.cat_house_occupied') house_occupied\n        FROM ids LEFT JOIN user_sessions u USING(user_id) LEFT JOIN states st ON st.user_id=ids.user_id AND st.rn=1\n        LEFT JOIN legacy l ON l.user_id=ids.user_id AND l.rn=1 LEFT JOIN days d USING(user_id)),\n    e AS (SELECT * FROM e0 WHERE ('${behavior_period}'='all' OR julianday(occurred_at) >= julianday(${__from}/1000,'unixepoch') AND julianday(occurred_at) < julianday(${__to}/1000,'unixepoch'))\n        OR (occurred_at IS NULL AND '${behavior_period}'='all')),\n    players AS (SELECT * FROM p WHERE 1),\n    events AS (SELECT * FROM e WHERE 1) SELECT ROUND(AVG(v)/1.0,2) 平均数,\n            ROUND(AVG(CASE WHEN rn IN ((n+1)/2,(n+2)/2) THEN v END)/1.0,2) 中位数,COUNT(*) 有效玩家数\n            FROM (SELECT play_days v,ROW_NUMBER() OVER(ORDER BY play_days) rn,COUNT(*) OVER() n FROM players WHERE play_days IS NOT NULL)"
        }
      ],
      "fieldConfig": {
        "defaults": {
          "unit": "short",
          "custom": {
            "filterable": true
          },
          "noValue": "未采集"
        },
        "overrides": [
          {
            "matcher": {
              "id": "byName",
              "options": "user_id"
            },
            "properties": [
              {
                "id": "links",
                "value": [
                  {
                    "title": "查看玩家旅程",
                    "url": "/d/gameplay-player-detail?${__url_time_range}&${client_platform:queryparam}&${release_version:queryparam}&${test_data:queryparam}&var-user_id=${__data.fields.user_id}&var-user_id=${__value.raw}"
                  }
                ]
              }
            ]
          },
          {
            "matcher": {
              "id": "byName",
              "options": "session_id"
            },
            "properties": [
              {
                "id": "links",
                "value": [
                  {
                    "title": "查看玩家旅程",
                    "url": "/d/gameplay-player-detail?${__url_time_range}&${client_platform:queryparam}&${release_version:queryparam}&${test_data:queryparam}&var-user_id=${__data.fields.user_id}&var-session_id=${__value.raw}"
                  }
                ]
              }
            ]
          },
          {
            "matcher": {
              "id": "byName",
              "options": "llm_request_id"
            },
            "properties": [
              {
                "id": "links",
                "value": [
                  {
                    "title": "查看玩家旅程",
                    "url": "/d/gameplay-player-detail?${__url_time_range}&${client_platform:queryparam}&${release_version:queryparam}&${test_data:queryparam}&var-user_id=${__data.fields.user_id}&var-llm_request_id=${__value.raw}"
                  }
                ]
              }
            ]
          }
        ]
      },
      "options": {
        "showHeader": true,
        "cellHeight": "sm"
      },
      "gridPos": {
        "x": 0,
        "y": 16,
        "w": 12,
        "h": 6
      }
    },
    {
      "id": 104,
      "title": "岛屿等级",
      "type": "table",
      "datasource": {
        "type": "frser-sqlite-datasource",
        "uid": "${DS_SQLITE}"
      },
      "description": "累计按玩家去重；当前状态取最新快照。缺失显示未采集，数值零保留。行为可切换期间，日期为北京时间。",
      "targets": [
        {
          "refId": "A",
          "queryType": "table",
          "queryText": "WITH e0 AS NOT MATERIALIZED (SELECT * FROM analytics_events WHERE ('__all__' IN (${client_platform:sqlstring}) OR client_platform IN (${client_platform:sqlstring})) AND ('__all__' IN (${release_version:sqlstring}) OR COALESCE(NULLIF(release_version,''),'__empty__') IN (${release_version:sqlstring})) AND ('${test_data}'='include' OR ('${test_data}'='auto' AND (client_platform='editor' OR (is_development_build=0 AND is_developer=0))) OR ('${test_data}'='exclude' AND is_development_build=0 AND is_developer=0 AND client_platform!='editor') OR ('${test_data}'='only' AND (is_development_build=1 OR is_developer=1 OR client_platform='editor')))),\n    s AS MATERIALIZED (SELECT * FROM analytics_sessions WHERE ('__all__' IN (${client_platform:sqlstring}) OR client_platform IN (${client_platform:sqlstring})) AND ('__all__' IN (${release_version:sqlstring}) OR COALESCE(NULLIF(release_version,''),'__empty__') IN (${release_version:sqlstring})) AND ('${test_data}'='include' OR ('${test_data}'='auto' AND (client_platform='editor' OR (is_development_build=0 AND is_developer=0))) OR ('${test_data}'='exclude' AND is_development_build=0 AND is_developer=0 AND client_platform!='editor') OR ('${test_data}'='only' AND (is_development_build=1 OR is_developer=1 OR client_platform='editor')))),\n    ids AS (SELECT user_id FROM s UNION SELECT user_id FROM e0),\n    states AS (SELECT *,ROW_NUMBER() OVER(PARTITION BY user_id ORDER BY julianday(occurred_at) DESC,sequence DESC,event_id DESC) rn\n        FROM e0 WHERE event_type='player_state_snapshot'),\n    legacy AS (SELECT g.*,ROW_NUMBER() OVER(PARTITION BY user_id ORDER BY julianday(real_time_started_iso) DESC,imported_at DESC) rn\n        FROM gameplay_sessions g WHERE EXISTS(SELECT 1 FROM s WHERE s.user_id=g.user_id AND s.session_id=g.session_id)),\n    days AS (SELECT user_id,COUNT(*) play_days FROM\n        (SELECT DISTINCT user_id,archive_id,game_day FROM e0 WHERE NULLIF(archive_id,'') IS NOT NULL AND game_day IS NOT NULL) GROUP BY user_id),\n    p AS (SELECT ids.user_id,COALESCE(NULLIF(json_extract(st.payload_json,'$.nickname'),''),NULLIF(u.nickname,''),NULLIF(u.player_name,''),ids.user_id) nickname,\n        (SELECT SUM(duration_sec) FROM s WHERE s.user_id=ids.user_id) play_seconds,\n        (SELECT first_entry_at FROM analytics_first_entry f WHERE f.user_id=ids.user_id AND f.cohort_scope=CASE WHEN '${test_data}' IN ('include','only') OR ('${test_data}'='auto' AND ('editor' IN (${client_platform:sqlstring}) OR '__all__' IN (${client_platform:sqlstring}))) THEN 'all' ELSE 'production' END) first_login,\n        (SELECT MAX(started_at) FROM s WHERE s.user_id=ids.user_id) latest_login,\n        d.play_days,COALESCE(json_extract(st.payload_json,'$.money'),l.money_end,l.money_start) money,\n        COALESCE(json_extract(st.payload_json,'$.island_level'),l.island_level_max) island_level,\n        json_array_length(json_extract(st.payload_json,'$.cats')) cat_count,\n        st.payload_json state,st.occurred_at state_at,\n        json_extract(st.payload_json,'$.cat_house_count') house_count,\n        json_extract(st.payload_json,'$.cat_house_capacity') house_capacity,\n        json_extract(st.payload_json,'$.cat_house_occupied') house_occupied\n        FROM ids LEFT JOIN user_sessions u USING(user_id) LEFT JOIN states st ON st.user_id=ids.user_id AND st.rn=1\n        LEFT JOIN legacy l ON l.user_id=ids.user_id AND l.rn=1 LEFT JOIN days d USING(user_id)),\n    e AS (SELECT * FROM e0 WHERE ('${behavior_period}'='all' OR julianday(occurred_at) >= julianday(${__from}/1000,'unixepoch') AND julianday(occurred_at) < julianday(${__to}/1000,'unixepoch'))\n        OR (occurred_at IS NULL AND '${behavior_period}'='all')),\n    players AS (SELECT * FROM p WHERE 1),\n    events AS (SELECT * FROM e WHERE 1) SELECT ROUND(AVG(v)/1.0,2) 平均数,\n            ROUND(AVG(CASE WHEN rn IN ((n+1)/2,(n+2)/2) THEN v END)/1.0,2) 中位数,COUNT(*) 有效玩家数\n            FROM (SELECT island_level v,ROW_NUMBER() OVER(ORDER BY island_level) rn,COUNT(*) OVER() n FROM players WHERE island_level IS NOT NULL)",
          "rawQueryText": "WITH e0 AS NOT MATERIALIZED (SELECT * FROM analytics_events WHERE ('__all__' IN (${client_platform:sqlstring}) OR client_platform IN (${client_platform:sqlstring})) AND ('__all__' IN (${release_version:sqlstring}) OR COALESCE(NULLIF(release_version,''),'__empty__') IN (${release_version:sqlstring})) AND ('${test_data}'='include' OR ('${test_data}'='auto' AND (client_platform='editor' OR (is_development_build=0 AND is_developer=0))) OR ('${test_data}'='exclude' AND is_development_build=0 AND is_developer=0 AND client_platform!='editor') OR ('${test_data}'='only' AND (is_development_build=1 OR is_developer=1 OR client_platform='editor')))),\n    s AS MATERIALIZED (SELECT * FROM analytics_sessions WHERE ('__all__' IN (${client_platform:sqlstring}) OR client_platform IN (${client_platform:sqlstring})) AND ('__all__' IN (${release_version:sqlstring}) OR COALESCE(NULLIF(release_version,''),'__empty__') IN (${release_version:sqlstring})) AND ('${test_data}'='include' OR ('${test_data}'='auto' AND (client_platform='editor' OR (is_development_build=0 AND is_developer=0))) OR ('${test_data}'='exclude' AND is_development_build=0 AND is_developer=0 AND client_platform!='editor') OR ('${test_data}'='only' AND (is_development_build=1 OR is_developer=1 OR client_platform='editor')))),\n    ids AS (SELECT user_id FROM s UNION SELECT user_id FROM e0),\n    states AS (SELECT *,ROW_NUMBER() OVER(PARTITION BY user_id ORDER BY julianday(occurred_at) DESC,sequence DESC,event_id DESC) rn\n        FROM e0 WHERE event_type='player_state_snapshot'),\n    legacy AS (SELECT g.*,ROW_NUMBER() OVER(PARTITION BY user_id ORDER BY julianday(real_time_started_iso) DESC,imported_at DESC) rn\n        FROM gameplay_sessions g WHERE EXISTS(SELECT 1 FROM s WHERE s.user_id=g.user_id AND s.session_id=g.session_id)),\n    days AS (SELECT user_id,COUNT(*) play_days FROM\n        (SELECT DISTINCT user_id,archive_id,game_day FROM e0 WHERE NULLIF(archive_id,'') IS NOT NULL AND game_day IS NOT NULL) GROUP BY user_id),\n    p AS (SELECT ids.user_id,COALESCE(NULLIF(json_extract(st.payload_json,'$.nickname'),''),NULLIF(u.nickname,''),NULLIF(u.player_name,''),ids.user_id) nickname,\n        (SELECT SUM(duration_sec) FROM s WHERE s.user_id=ids.user_id) play_seconds,\n        (SELECT first_entry_at FROM analytics_first_entry f WHERE f.user_id=ids.user_id AND f.cohort_scope=CASE WHEN '${test_data}' IN ('include','only') OR ('${test_data}'='auto' AND ('editor' IN (${client_platform:sqlstring}) OR '__all__' IN (${client_platform:sqlstring}))) THEN 'all' ELSE 'production' END) first_login,\n        (SELECT MAX(started_at) FROM s WHERE s.user_id=ids.user_id) latest_login,\n        d.play_days,COALESCE(json_extract(st.payload_json,'$.money'),l.money_end,l.money_start) money,\n        COALESCE(json_extract(st.payload_json,'$.island_level'),l.island_level_max) island_level,\n        json_array_length(json_extract(st.payload_json,'$.cats')) cat_count,\n        st.payload_json state,st.occurred_at state_at,\n        json_extract(st.payload_json,'$.cat_house_count') house_count,\n        json_extract(st.payload_json,'$.cat_house_capacity') house_capacity,\n        json_extract(st.payload_json,'$.cat_house_occupied') house_occupied\n        FROM ids LEFT JOIN user_sessions u USING(user_id) LEFT JOIN states st ON st.user_id=ids.user_id AND st.rn=1\n        LEFT JOIN legacy l ON l.user_id=ids.user_id AND l.rn=1 LEFT JOIN days d USING(user_id)),\n    e AS (SELECT * FROM e0 WHERE ('${behavior_period}'='all' OR julianday(occurred_at) >= julianday(${__from}/1000,'unixepoch') AND julianday(occurred_at) < julianday(${__to}/1000,'unixepoch'))\n        OR (occurred_at IS NULL AND '${behavior_period}'='all')),\n    players AS (SELECT * FROM p WHERE 1),\n    events AS (SELECT * FROM e WHERE 1) SELECT ROUND(AVG(v)/1.0,2) 平均数,\n            ROUND(AVG(CASE WHEN rn IN ((n+1)/2,(n+2)/2) THEN v END)/1.0,2) 中位数,COUNT(*) 有效玩家数\n            FROM (SELECT island_level v,ROW_NUMBER() OVER(ORDER BY island_level) rn,COUNT(*) OVER() n FROM players WHERE island_level IS NOT NULL)"
        }
      ],
      "fieldConfig": {
        "defaults": {
          "unit": "short",
          "custom": {
            "filterable": true
          },
          "noValue": "未采集"
        },
        "overrides": [
          {
            "matcher": {
              "id": "byName",
              "options": "user_id"
            },
            "properties": [
              {
                "id": "links",
                "value": [
                  {
                    "title": "查看玩家旅程",
                    "url": "/d/gameplay-player-detail?${__url_time_range}&${client_platform:queryparam}&${release_version:queryparam}&${test_data:queryparam}&var-user_id=${__data.fields.user_id}&var-user_id=${__value.raw}"
                  }
                ]
              }
            ]
          },
          {
            "matcher": {
              "id": "byName",
              "options": "session_id"
            },
            "properties": [
              {
                "id": "links",
                "value": [
                  {
                    "title": "查看玩家旅程",
                    "url": "/d/gameplay-player-detail?${__url_time_range}&${client_platform:queryparam}&${release_version:queryparam}&${test_data:queryparam}&var-user_id=${__data.fields.user_id}&var-session_id=${__value.raw}"
                  }
                ]
              }
            ]
          },
          {
            "matcher": {
              "id": "byName",
              "options": "llm_request_id"
            },
            "properties": [
              {
                "id": "links",
                "value": [
                  {
                    "title": "查看玩家旅程",
                    "url": "/d/gameplay-player-detail?${__url_time_range}&${client_platform:queryparam}&${release_version:queryparam}&${test_data:queryparam}&var-user_id=${__data.fields.user_id}&var-llm_request_id=${__value.raw}"
                  }
                ]
              }
            ]
          }
        ]
      },
      "options": {
        "showHeader": true,
        "cellHeight": "sm"
      },
      "gridPos": {
        "x": 12,
        "y": 16,
        "w": 12,
        "h": 6
      }
    },
    {
      "id": 105,
      "title": "各职业平均等级",
      "type": "table",
      "datasource": {
        "type": "frser-sqlite-datasource",
        "uid": "${DS_SQLITE}"
      },
      "description": "累计按玩家去重；当前状态取最新快照。缺失显示未采集，数值零保留。行为可切换期间，日期为北京时间。",
      "targets": [
        {
          "refId": "A",
          "queryType": "table",
          "queryText": "WITH e0 AS NOT MATERIALIZED (SELECT * FROM analytics_events WHERE ('__all__' IN (${client_platform:sqlstring}) OR client_platform IN (${client_platform:sqlstring})) AND ('__all__' IN (${release_version:sqlstring}) OR COALESCE(NULLIF(release_version,''),'__empty__') IN (${release_version:sqlstring})) AND ('${test_data}'='include' OR ('${test_data}'='auto' AND (client_platform='editor' OR (is_development_build=0 AND is_developer=0))) OR ('${test_data}'='exclude' AND is_development_build=0 AND is_developer=0 AND client_platform!='editor') OR ('${test_data}'='only' AND (is_development_build=1 OR is_developer=1 OR client_platform='editor')))),\n    s AS MATERIALIZED (SELECT * FROM analytics_sessions WHERE ('__all__' IN (${client_platform:sqlstring}) OR client_platform IN (${client_platform:sqlstring})) AND ('__all__' IN (${release_version:sqlstring}) OR COALESCE(NULLIF(release_version,''),'__empty__') IN (${release_version:sqlstring})) AND ('${test_data}'='include' OR ('${test_data}'='auto' AND (client_platform='editor' OR (is_development_build=0 AND is_developer=0))) OR ('${test_data}'='exclude' AND is_development_build=0 AND is_developer=0 AND client_platform!='editor') OR ('${test_data}'='only' AND (is_development_build=1 OR is_developer=1 OR client_platform='editor')))),\n    ids AS (SELECT user_id FROM s UNION SELECT user_id FROM e0),\n    states AS (SELECT *,ROW_NUMBER() OVER(PARTITION BY user_id ORDER BY julianday(occurred_at) DESC,sequence DESC,event_id DESC) rn\n        FROM e0 WHERE event_type='player_state_snapshot'),\n    legacy AS (SELECT g.*,ROW_NUMBER() OVER(PARTITION BY user_id ORDER BY julianday(real_time_started_iso) DESC,imported_at DESC) rn\n        FROM gameplay_sessions g WHERE EXISTS(SELECT 1 FROM s WHERE s.user_id=g.user_id AND s.session_id=g.session_id)),\n    days AS (SELECT user_id,COUNT(*) play_days FROM\n        (SELECT DISTINCT user_id,archive_id,game_day FROM e0 WHERE NULLIF(archive_id,'') IS NOT NULL AND game_day IS NOT NULL) GROUP BY user_id),\n    p AS (SELECT ids.user_id,COALESCE(NULLIF(json_extract(st.payload_json,'$.nickname'),''),NULLIF(u.nickname,''),NULLIF(u.player_name,''),ids.user_id) nickname,\n        (SELECT SUM(duration_sec) FROM s WHERE s.user_id=ids.user_id) play_seconds,\n        (SELECT first_entry_at FROM analytics_first_entry f WHERE f.user_id=ids.user_id AND f.cohort_scope=CASE WHEN '${test_data}' IN ('include','only') OR ('${test_data}'='auto' AND ('editor' IN (${client_platform:sqlstring}) OR '__all__' IN (${client_platform:sqlstring}))) THEN 'all' ELSE 'production' END) first_login,\n        (SELECT MAX(started_at) FROM s WHERE s.user_id=ids.user_id) latest_login,\n        d.play_days,COALESCE(json_extract(st.payload_json,'$.money'),l.money_end,l.money_start) money,\n        COALESCE(json_extract(st.payload_json,'$.island_level'),l.island_level_max) island_level,\n        json_array_length(json_extract(st.payload_json,'$.cats')) cat_count,\n        st.payload_json state,st.occurred_at state_at,\n        json_extract(st.payload_json,'$.cat_house_count') house_count,\n        json_extract(st.payload_json,'$.cat_house_capacity') house_capacity,\n        json_extract(st.payload_json,'$.cat_house_occupied') house_occupied\n        FROM ids LEFT JOIN user_sessions u USING(user_id) LEFT JOIN states st ON st.user_id=ids.user_id AND st.rn=1\n        LEFT JOIN legacy l ON l.user_id=ids.user_id AND l.rn=1 LEFT JOIN days d USING(user_id)),\n    e AS (SELECT * FROM e0 WHERE ('${behavior_period}'='all' OR julianday(occurred_at) >= julianday(${__from}/1000,'unixepoch') AND julianday(occurred_at) < julianday(${__to}/1000,'unixepoch'))\n        OR (occurred_at IS NULL AND '${behavior_period}'='all')),\n    players AS (SELECT * FROM p WHERE 1),\n    events AS (SELECT * FROM e WHERE 1) SELECT j.key 职业,AVG(j.value) 平均等级,COUNT(j.value) 有效玩家数 FROM players,json_each(state,'$.careers') j GROUP BY j.key",
          "rawQueryText": "WITH e0 AS NOT MATERIALIZED (SELECT * FROM analytics_events WHERE ('__all__' IN (${client_platform:sqlstring}) OR client_platform IN (${client_platform:sqlstring})) AND ('__all__' IN (${release_version:sqlstring}) OR COALESCE(NULLIF(release_version,''),'__empty__') IN (${release_version:sqlstring})) AND ('${test_data}'='include' OR ('${test_data}'='auto' AND (client_platform='editor' OR (is_development_build=0 AND is_developer=0))) OR ('${test_data}'='exclude' AND is_development_build=0 AND is_developer=0 AND client_platform!='editor') OR ('${test_data}'='only' AND (is_development_build=1 OR is_developer=1 OR client_platform='editor')))),\n    s AS MATERIALIZED (SELECT * FROM analytics_sessions WHERE ('__all__' IN (${client_platform:sqlstring}) OR client_platform IN (${client_platform:sqlstring})) AND ('__all__' IN (${release_version:sqlstring}) OR COALESCE(NULLIF(release_version,''),'__empty__') IN (${release_version:sqlstring})) AND ('${test_data}'='include' OR ('${test_data}'='auto' AND (client_platform='editor' OR (is_development_build=0 AND is_developer=0))) OR ('${test_data}'='exclude' AND is_development_build=0 AND is_developer=0 AND client_platform!='editor') OR ('${test_data}'='only' AND (is_development_build=1 OR is_developer=1 OR client_platform='editor')))),\n    ids AS (SELECT user_id FROM s UNION SELECT user_id FROM e0),\n    states AS (SELECT *,ROW_NUMBER() OVER(PARTITION BY user_id ORDER BY julianday(occurred_at) DESC,sequence DESC,event_id DESC) rn\n        FROM e0 WHERE event_type='player_state_snapshot'),\n    legacy AS (SELECT g.*,ROW_NUMBER() OVER(PARTITION BY user_id ORDER BY julianday(real_time_started_iso) DESC,imported_at DESC) rn\n        FROM gameplay_sessions g WHERE EXISTS(SELECT 1 FROM s WHERE s.user_id=g.user_id AND s.session_id=g.session_id)),\n    days AS (SELECT user_id,COUNT(*) play_days FROM\n        (SELECT DISTINCT user_id,archive_id,game_day FROM e0 WHERE NULLIF(archive_id,'') IS NOT NULL AND game_day IS NOT NULL) GROUP BY user_id),\n    p AS (SELECT ids.user_id,COALESCE(NULLIF(json_extract(st.payload_json,'$.nickname'),''),NULLIF(u.nickname,''),NULLIF(u.player_name,''),ids.user_id) nickname,\n        (SELECT SUM(duration_sec) FROM s WHERE s.user_id=ids.user_id) play_seconds,\n        (SELECT first_entry_at FROM analytics_first_entry f WHERE f.user_id=ids.user_id AND f.cohort_scope=CASE WHEN '${test_data}' IN ('include','only') OR ('${test_data}'='auto' AND ('editor' IN (${client_platform:sqlstring}) OR '__all__' IN (${client_platform:sqlstring}))) THEN 'all' ELSE 'production' END) first_login,\n        (SELECT MAX(started_at) FROM s WHERE s.user_id=ids.user_id) latest_login,\n        d.play_days,COALESCE(json_extract(st.payload_json,'$.money'),l.money_end,l.money_start) money,\n        COALESCE(json_extract(st.payload_json,'$.island_level'),l.island_level_max) island_level,\n        json_array_length(json_extract(st.payload_json,'$.cats')) cat_count,\n        st.payload_json state,st.occurred_at state_at,\n        json_extract(st.payload_json,'$.cat_house_count') house_count,\n        json_extract(st.payload_json,'$.cat_house_capacity') house_capacity,\n        json_extract(st.payload_json,'$.cat_house_occupied') house_occupied\n        FROM ids LEFT JOIN user_sessions u USING(user_id) LEFT JOIN states st ON st.user_id=ids.user_id AND st.rn=1\n        LEFT JOIN legacy l ON l.user_id=ids.user_id AND l.rn=1 LEFT JOIN days d USING(user_id)),\n    e AS (SELECT * FROM e0 WHERE ('${behavior_period}'='all' OR julianday(occurred_at) >= julianday(${__from}/1000,'unixepoch') AND julianday(occurred_at) < julianday(${__to}/1000,'unixepoch'))\n        OR (occurred_at IS NULL AND '${behavior_period}'='all')),\n    players AS (SELECT * FROM p WHERE 1),\n    events AS (SELECT * FROM e WHERE 1) SELECT j.key 职业,AVG(j.value) 平均等级,COUNT(j.value) 有效玩家数 FROM players,json_each(state,'$.careers') j GROUP BY j.key"
        }
      ],
      "fieldConfig": {
        "defaults": {
          "unit": "short",
          "custom": {
            "filterable": true
          },
          "noValue": "未采集"
        },
        "overrides": [
          {
            "matcher": {
              "id": "byName",
              "options": "user_id"
            },
            "properties": [
              {
                "id": "links",
                "value": [
                  {
                    "title": "查看玩家旅程",
                    "url": "/d/gameplay-player-detail?${__url_time_range}&${client_platform:queryparam}&${release_version:queryparam}&${test_data:queryparam}&var-user_id=${__data.fields.user_id}&var-user_id=${__value.raw}"
                  }
                ]
              }
            ]
          },
          {
            "matcher": {
              "id": "byName",
              "options": "session_id"
            },
            "properties": [
              {
                "id": "links",
                "value": [
                  {
                    "title": "查看玩家旅程",
                    "url": "/d/gameplay-player-detail?${__url_time_range}&${client_platform:queryparam}&${release_version:queryparam}&${test_data:queryparam}&var-user_id=${__data.fields.user_id}&var-session_id=${__value.raw}"
                  }
                ]
              }
            ]
          },
          {
            "matcher": {
              "id": "byName",
              "options": "llm_request_id"
            },
            "properties": [
              {
                "id": "links",
                "value": [
                  {
                    "title": "查看玩家旅程",
                    "url": "/d/gameplay-player-detail?${__url_time_range}&${client_platform:queryparam}&${release_version:queryparam}&${test_data:queryparam}&var-user_id=${__data.fields.user_id}&var-llm_request_id=${__value.raw}"
                  }
                ]
              }
            ]
          }
        ]
      },
      "options": {
        "showHeader": true,
        "cellHeight": "sm"
      },
      "gridPos": {
        "x": 0,
        "y": 22,
        "w": 24,
        "h": 6
      }
    },
    {
      "id": 120,
      "title": "购买最多商品 Top 5",
      "type": "table",
      "datasource": {
        "type": "frser-sqlite-datasource",
        "uid": "${DS_SQLITE}"
      },
      "description": "累计按玩家去重；当前状态取最新快照。缺失显示未采集，数值零保留。行为可切换期间，日期为北京时间。",
      "targets": [
        {
          "refId": "A",
          "queryType": "table",
          "queryText": "WITH e0 AS NOT MATERIALIZED (SELECT * FROM analytics_events WHERE ('__all__' IN (${client_platform:sqlstring}) OR client_platform IN (${client_platform:sqlstring})) AND ('__all__' IN (${release_version:sqlstring}) OR COALESCE(NULLIF(release_version,''),'__empty__') IN (${release_version:sqlstring})) AND ('${test_data}'='include' OR ('${test_data}'='auto' AND (client_platform='editor' OR (is_development_build=0 AND is_developer=0))) OR ('${test_data}'='exclude' AND is_development_build=0 AND is_developer=0 AND client_platform!='editor') OR ('${test_data}'='only' AND (is_development_build=1 OR is_developer=1 OR client_platform='editor')))),\n    s AS MATERIALIZED (SELECT * FROM analytics_sessions WHERE ('__all__' IN (${client_platform:sqlstring}) OR client_platform IN (${client_platform:sqlstring})) AND ('__all__' IN (${release_version:sqlstring}) OR COALESCE(NULLIF(release_version,''),'__empty__') IN (${release_version:sqlstring})) AND ('${test_data}'='include' OR ('${test_data}'='auto' AND (client_platform='editor' OR (is_development_build=0 AND is_developer=0))) OR ('${test_data}'='exclude' AND is_development_build=0 AND is_developer=0 AND client_platform!='editor') OR ('${test_data}'='only' AND (is_development_build=1 OR is_developer=1 OR client_platform='editor')))),\n    ids AS (SELECT user_id FROM s UNION SELECT user_id FROM e0),\n    states AS (SELECT *,ROW_NUMBER() OVER(PARTITION BY user_id ORDER BY julianday(occurred_at) DESC,sequence DESC,event_id DESC) rn\n        FROM e0 WHERE event_type='player_state_snapshot'),\n    legacy AS (SELECT g.*,ROW_NUMBER() OVER(PARTITION BY user_id ORDER BY julianday(real_time_started_iso) DESC,imported_at DESC) rn\n        FROM gameplay_sessions g WHERE EXISTS(SELECT 1 FROM s WHERE s.user_id=g.user_id AND s.session_id=g.session_id)),\n    days AS (SELECT user_id,COUNT(*) play_days FROM\n        (SELECT DISTINCT user_id,archive_id,game_day FROM e0 WHERE NULLIF(archive_id,'') IS NOT NULL AND game_day IS NOT NULL) GROUP BY user_id),\n    p AS (SELECT ids.user_id,COALESCE(NULLIF(json_extract(st.payload_json,'$.nickname'),''),NULLIF(u.nickname,''),NULLIF(u.player_name,''),ids.user_id) nickname,\n        (SELECT SUM(duration_sec) FROM s WHERE s.user_id=ids.user_id) play_seconds,\n        (SELECT first_entry_at FROM analytics_first_entry f WHERE f.user_id=ids.user_id AND f.cohort_scope=CASE WHEN '${test_data}' IN ('include','only') OR ('${test_data}'='auto' AND ('editor' IN (${client_platform:sqlstring}) OR '__all__' IN (${client_platform:sqlstring}))) THEN 'all' ELSE 'production' END) first_login,\n        (SELECT MAX(started_at) FROM s WHERE s.user_id=ids.user_id) latest_login,\n        d.play_days,COALESCE(json_extract(st.payload_json,'$.money'),l.money_end,l.money_start) money,\n        COALESCE(json_extract(st.payload_json,'$.island_level'),l.island_level_max) island_level,\n        json_array_length(json_extract(st.payload_json,'$.cats')) cat_count,\n        st.payload_json state,st.occurred_at state_at,\n        json_extract(st.payload_json,'$.cat_house_count') house_count,\n        json_extract(st.payload_json,'$.cat_house_capacity') house_capacity,\n        json_extract(st.payload_json,'$.cat_house_occupied') house_occupied\n        FROM ids LEFT JOIN user_sessions u USING(user_id) LEFT JOIN states st ON st.user_id=ids.user_id AND st.rn=1\n        LEFT JOIN legacy l ON l.user_id=ids.user_id AND l.rn=1 LEFT JOIN days d USING(user_id)),\n    e AS (SELECT * FROM e0 WHERE ('${behavior_period}'='all' OR julianday(occurred_at) >= julianday(${__from}/1000,'unixepoch') AND julianday(occurred_at) < julianday(${__to}/1000,'unixepoch'))\n        OR (occurred_at IS NULL AND '${behavior_period}'='all')),\n    players AS (SELECT * FROM p WHERE 1),\n    events AS (SELECT * FROM e WHERE 1) SELECT COALESCE(NULLIF(COALESCE(json_extract(payload_json,'$.item_name'),json_extract(payload_json,'$.item.item_name')),''),'未采集名称') 名称,COUNT(*) 次数,COUNT(DISTINCT user_id) 参与玩家数,SUM(COALESCE(json_extract(payload_json,'$.quantity'),1)) 件数 FROM events WHERE event_type='shop_purchase' AND actor_is_player=1 AND 1 GROUP BY COALESCE(json_extract(payload_json,'$.item_id'),COALESCE(json_extract(payload_json,'$.item_name'),json_extract(payload_json,'$.item.item_name'))) ORDER BY 次数 DESC,名称 LIMIT 5",
          "rawQueryText": "WITH e0 AS NOT MATERIALIZED (SELECT * FROM analytics_events WHERE ('__all__' IN (${client_platform:sqlstring}) OR client_platform IN (${client_platform:sqlstring})) AND ('__all__' IN (${release_version:sqlstring}) OR COALESCE(NULLIF(release_version,''),'__empty__') IN (${release_version:sqlstring})) AND ('${test_data}'='include' OR ('${test_data}'='auto' AND (client_platform='editor' OR (is_development_build=0 AND is_developer=0))) OR ('${test_data}'='exclude' AND is_development_build=0 AND is_developer=0 AND client_platform!='editor') OR ('${test_data}'='only' AND (is_development_build=1 OR is_developer=1 OR client_platform='editor')))),\n    s AS MATERIALIZED (SELECT * FROM analytics_sessions WHERE ('__all__' IN (${client_platform:sqlstring}) OR client_platform IN (${client_platform:sqlstring})) AND ('__all__' IN (${release_version:sqlstring}) OR COALESCE(NULLIF(release_version,''),'__empty__') IN (${release_version:sqlstring})) AND ('${test_data}'='include' OR ('${test_data}'='auto' AND (client_platform='editor' OR (is_development_build=0 AND is_developer=0))) OR ('${test_data}'='exclude' AND is_development_build=0 AND is_developer=0 AND client_platform!='editor') OR ('${test_data}'='only' AND (is_development_build=1 OR is_developer=1 OR client_platform='editor')))),\n    ids AS (SELECT user_id FROM s UNION SELECT user_id FROM e0),\n    states AS (SELECT *,ROW_NUMBER() OVER(PARTITION BY user_id ORDER BY julianday(occurred_at) DESC,sequence DESC,event_id DESC) rn\n        FROM e0 WHERE event_type='player_state_snapshot'),\n    legacy AS (SELECT g.*,ROW_NUMBER() OVER(PARTITION BY user_id ORDER BY julianday(real_time_started_iso) DESC,imported_at DESC) rn\n        FROM gameplay_sessions g WHERE EXISTS(SELECT 1 FROM s WHERE s.user_id=g.user_id AND s.session_id=g.session_id)),\n    days AS (SELECT user_id,COUNT(*) play_days FROM\n        (SELECT DISTINCT user_id,archive_id,game_day FROM e0 WHERE NULLIF(archive_id,'') IS NOT NULL AND game_day IS NOT NULL) GROUP BY user_id),\n    p AS (SELECT ids.user_id,COALESCE(NULLIF(json_extract(st.payload_json,'$.nickname'),''),NULLIF(u.nickname,''),NULLIF(u.player_name,''),ids.user_id) nickname,\n        (SELECT SUM(duration_sec) FROM s WHERE s.user_id=ids.user_id) play_seconds,\n        (SELECT first_entry_at FROM analytics_first_entry f WHERE f.user_id=ids.user_id AND f.cohort_scope=CASE WHEN '${test_data}' IN ('include','only') OR ('${test_data}'='auto' AND ('editor' IN (${client_platform:sqlstring}) OR '__all__' IN (${client_platform:sqlstring}))) THEN 'all' ELSE 'production' END) first_login,\n        (SELECT MAX(started_at) FROM s WHERE s.user_id=ids.user_id) latest_login,\n        d.play_days,COALESCE(json_extract(st.payload_json,'$.money'),l.money_end,l.money_start) money,\n        COALESCE(json_extract(st.payload_json,'$.island_level'),l.island_level_max) island_level,\n        json_array_length(json_extract(st.payload_json,'$.cats')) cat_count,\n        st.payload_json state,st.occurred_at state_at,\n        json_extract(st.payload_json,'$.cat_house_count') house_count,\n        json_extract(st.payload_json,'$.cat_house_capacity') house_capacity,\n        json_extract(st.payload_json,'$.cat_house_occupied') house_occupied\n        FROM ids LEFT JOIN user_sessions u USING(user_id) LEFT JOIN states st ON st.user_id=ids.user_id AND st.rn=1\n        LEFT JOIN legacy l ON l.user_id=ids.user_id AND l.rn=1 LEFT JOIN days d USING(user_id)),\n    e AS (SELECT * FROM e0 WHERE ('${behavior_period}'='all' OR julianday(occurred_at) >= julianday(${__from}/1000,'unixepoch') AND julianday(occurred_at) < julianday(${__to}/1000,'unixepoch'))\n        OR (occurred_at IS NULL AND '${behavior_period}'='all')),\n    players AS (SELECT * FROM p WHERE 1),\n    events AS (SELECT * FROM e WHERE 1) SELECT COALESCE(NULLIF(COALESCE(json_extract(payload_json,'$.item_name'),json_extract(payload_json,'$.item.item_name')),''),'未采集名称') 名称,COUNT(*) 次数,COUNT(DISTINCT user_id) 参与玩家数,SUM(COALESCE(json_extract(payload_json,'$.quantity'),1)) 件数 FROM events WHERE event_type='shop_purchase' AND actor_is_player=1 AND 1 GROUP BY COALESCE(json_extract(payload_json,'$.item_id'),COALESCE(json_extract(payload_json,'$.item_name'),json_extract(payload_json,'$.item.item_name'))) ORDER BY 次数 DESC,名称 LIMIT 5"
        }
      ],
      "fieldConfig": {
        "defaults": {
          "unit": "short",
          "custom": {
            "filterable": true
          },
          "noValue": "未采集"
        },
        "overrides": [
          {
            "matcher": {
              "id": "byName",
              "options": "user_id"
            },
            "properties": [
              {
                "id": "links",
                "value": [
                  {
                    "title": "查看玩家旅程",
                    "url": "/d/gameplay-player-detail?${__url_time_range}&${client_platform:queryparam}&${release_version:queryparam}&${test_data:queryparam}&var-user_id=${__data.fields.user_id}&var-user_id=${__value.raw}"
                  }
                ]
              }
            ]
          },
          {
            "matcher": {
              "id": "byName",
              "options": "session_id"
            },
            "properties": [
              {
                "id": "links",
                "value": [
                  {
                    "title": "查看玩家旅程",
                    "url": "/d/gameplay-player-detail?${__url_time_range}&${client_platform:queryparam}&${release_version:queryparam}&${test_data:queryparam}&var-user_id=${__data.fields.user_id}&var-session_id=${__value.raw}"
                  }
                ]
              }
            ]
          },
          {
            "matcher": {
              "id": "byName",
              "options": "llm_request_id"
            },
            "properties": [
              {
                "id": "links",
                "value": [
                  {
                    "title": "查看玩家旅程",
                    "url": "/d/gameplay-player-detail?${__url_time_range}&${client_platform:queryparam}&${release_version:queryparam}&${test_data:queryparam}&var-user_id=${__data.fields.user_id}&var-llm_request_id=${__value.raw}"
                  }
                ]
              }
            ]
          }
        ]
      },
      "options": {
        "showHeader": true,
        "cellHeight": "sm"
      },
      "gridPos": {
        "x": 0,
        "y": 28,
        "w": 12,
        "h": 9
      }
    },
    {
      "id": 121,
      "title": "最常招募的猫 Top 5",
      "type": "table",
      "datasource": {
        "type": "frser-sqlite-datasource",
        "uid": "${DS_SQLITE}"
      },
      "description": "累计按玩家去重；当前状态取最新快照。缺失显示未采集，数值零保留。行为可切换期间，日期为北京时间。",
      "targets": [
        {
          "refId": "A",
          "queryType": "table",
          "queryText": "WITH e0 AS NOT MATERIALIZED (SELECT * FROM analytics_events WHERE ('__all__' IN (${client_platform:sqlstring}) OR client_platform IN (${client_platform:sqlstring})) AND ('__all__' IN (${release_version:sqlstring}) OR COALESCE(NULLIF(release_version,''),'__empty__') IN (${release_version:sqlstring})) AND ('${test_data}'='include' OR ('${test_data}'='auto' AND (client_platform='editor' OR (is_development_build=0 AND is_developer=0))) OR ('${test_data}'='exclude' AND is_development_build=0 AND is_developer=0 AND client_platform!='editor') OR ('${test_data}'='only' AND (is_development_build=1 OR is_developer=1 OR client_platform='editor')))),\n    s AS MATERIALIZED (SELECT * FROM analytics_sessions WHERE ('__all__' IN (${client_platform:sqlstring}) OR client_platform IN (${client_platform:sqlstring})) AND ('__all__' IN (${release_version:sqlstring}) OR COALESCE(NULLIF(release_version,''),'__empty__') IN (${release_version:sqlstring})) AND ('${test_data}'='include' OR ('${test_data}'='auto' AND (client_platform='editor' OR (is_development_build=0 AND is_developer=0))) OR ('${test_data}'='exclude' AND is_development_build=0 AND is_developer=0 AND client_platform!='editor') OR ('${test_data}'='only' AND (is_development_build=1 OR is_developer=1 OR client_platform='editor')))),\n    ids AS (SELECT user_id FROM s UNION SELECT user_id FROM e0),\n    states AS (SELECT *,ROW_NUMBER() OVER(PARTITION BY user_id ORDER BY julianday(occurred_at) DESC,sequence DESC,event_id DESC) rn\n        FROM e0 WHERE event_type='player_state_snapshot'),\n    legacy AS (SELECT g.*,ROW_NUMBER() OVER(PARTITION BY user_id ORDER BY julianday(real_time_started_iso) DESC,imported_at DESC) rn\n        FROM gameplay_sessions g WHERE EXISTS(SELECT 1 FROM s WHERE s.user_id=g.user_id AND s.session_id=g.session_id)),\n    days AS (SELECT user_id,COUNT(*) play_days FROM\n        (SELECT DISTINCT user_id,archive_id,game_day FROM e0 WHERE NULLIF(archive_id,'') IS NOT NULL AND game_day IS NOT NULL) GROUP BY user_id),\n    p AS (SELECT ids.user_id,COALESCE(NULLIF(json_extract(st.payload_json,'$.nickname'),''),NULLIF(u.nickname,''),NULLIF(u.player_name,''),ids.user_id) nickname,\n        (SELECT SUM(duration_sec) FROM s WHERE s.user_id=ids.user_id) play_seconds,\n        (SELECT first_entry_at FROM analytics_first_entry f WHERE f.user_id=ids.user_id AND f.cohort_scope=CASE WHEN '${test_data}' IN ('include','only') OR ('${test_data}'='auto' AND ('editor' IN (${client_platform:sqlstring}) OR '__all__' IN (${client_platform:sqlstring}))) THEN 'all' ELSE 'production' END) first_login,\n        (SELECT MAX(started_at) FROM s WHERE s.user_id=ids.user_id) latest_login,\n        d.play_days,COALESCE(json_extract(st.payload_json,'$.money'),l.money_end,l.money_start) money,\n        COALESCE(json_extract(st.payload_json,'$.island_level'),l.island_level_max) island_level,\n        json_array_length(json_extract(st.payload_json,'$.cats')) cat_count,\n        st.payload_json state,st.occurred_at state_at,\n        json_extract(st.payload_json,'$.cat_house_count') house_count,\n        json_extract(st.payload_json,'$.cat_house_capacity') house_capacity,\n        json_extract(st.payload_json,'$.cat_house_occupied') house_occupied\n        FROM ids LEFT JOIN user_sessions u USING(user_id) LEFT JOIN states st ON st.user_id=ids.user_id AND st.rn=1\n        LEFT JOIN legacy l ON l.user_id=ids.user_id AND l.rn=1 LEFT JOIN days d USING(user_id)),\n    e AS (SELECT * FROM e0 WHERE ('${behavior_period}'='all' OR julianday(occurred_at) >= julianday(${__from}/1000,'unixepoch') AND julianday(occurred_at) < julianday(${__to}/1000,'unixepoch'))\n        OR (occurred_at IS NULL AND '${behavior_period}'='all')),\n    players AS (SELECT * FROM p WHERE 1),\n    events AS (SELECT * FROM e WHERE 1) SELECT COALESCE(NULLIF(COALESCE(json_extract(payload_json,'$.cat.breed'),json_extract(payload_json,'$.cat.skin')),''),'未采集名称') 名称,COUNT(*) 次数,COUNT(DISTINCT user_id) 参与玩家数 FROM events WHERE event_type='cat_adopted' AND actor_is_player=1 AND 1 GROUP BY COALESCE(json_extract(payload_json,'$.template_id'),COALESCE(json_extract(payload_json,'$.cat.breed'),json_extract(payload_json,'$.cat.skin'))) ORDER BY 次数 DESC,名称 LIMIT 5",
          "rawQueryText": "WITH e0 AS NOT MATERIALIZED (SELECT * FROM analytics_events WHERE ('__all__' IN (${client_platform:sqlstring}) OR client_platform IN (${client_platform:sqlstring})) AND ('__all__' IN (${release_version:sqlstring}) OR COALESCE(NULLIF(release_version,''),'__empty__') IN (${release_version:sqlstring})) AND ('${test_data}'='include' OR ('${test_data}'='auto' AND (client_platform='editor' OR (is_development_build=0 AND is_developer=0))) OR ('${test_data}'='exclude' AND is_development_build=0 AND is_developer=0 AND client_platform!='editor') OR ('${test_data}'='only' AND (is_development_build=1 OR is_developer=1 OR client_platform='editor')))),\n    s AS MATERIALIZED (SELECT * FROM analytics_sessions WHERE ('__all__' IN (${client_platform:sqlstring}) OR client_platform IN (${client_platform:sqlstring})) AND ('__all__' IN (${release_version:sqlstring}) OR COALESCE(NULLIF(release_version,''),'__empty__') IN (${release_version:sqlstring})) AND ('${test_data}'='include' OR ('${test_data}'='auto' AND (client_platform='editor' OR (is_development_build=0 AND is_developer=0))) OR ('${test_data}'='exclude' AND is_development_build=0 AND is_developer=0 AND client_platform!='editor') OR ('${test_data}'='only' AND (is_development_build=1 OR is_developer=1 OR client_platform='editor')))),\n    ids AS (SELECT user_id FROM s UNION SELECT user_id FROM e0),\n    states AS (SELECT *,ROW_NUMBER() OVER(PARTITION BY user_id ORDER BY julianday(occurred_at) DESC,sequence DESC,event_id DESC) rn\n        FROM e0 WHERE event_type='player_state_snapshot'),\n    legacy AS (SELECT g.*,ROW_NUMBER() OVER(PARTITION BY user_id ORDER BY julianday(real_time_started_iso) DESC,imported_at DESC) rn\n        FROM gameplay_sessions g WHERE EXISTS(SELECT 1 FROM s WHERE s.user_id=g.user_id AND s.session_id=g.session_id)),\n    days AS (SELECT user_id,COUNT(*) play_days FROM\n        (SELECT DISTINCT user_id,archive_id,game_day FROM e0 WHERE NULLIF(archive_id,'') IS NOT NULL AND game_day IS NOT NULL) GROUP BY user_id),\n    p AS (SELECT ids.user_id,COALESCE(NULLIF(json_extract(st.payload_json,'$.nickname'),''),NULLIF(u.nickname,''),NULLIF(u.player_name,''),ids.user_id) nickname,\n        (SELECT SUM(duration_sec) FROM s WHERE s.user_id=ids.user_id) play_seconds,\n        (SELECT first_entry_at FROM analytics_first_entry f WHERE f.user_id=ids.user_id AND f.cohort_scope=CASE WHEN '${test_data}' IN ('include','only') OR ('${test_data}'='auto' AND ('editor' IN (${client_platform:sqlstring}) OR '__all__' IN (${client_platform:sqlstring}))) THEN 'all' ELSE 'production' END) first_login,\n        (SELECT MAX(started_at) FROM s WHERE s.user_id=ids.user_id) latest_login,\n        d.play_days,COALESCE(json_extract(st.payload_json,'$.money'),l.money_end,l.money_start) money,\n        COALESCE(json_extract(st.payload_json,'$.island_level'),l.island_level_max) island_level,\n        json_array_length(json_extract(st.payload_json,'$.cats')) cat_count,\n        st.payload_json state,st.occurred_at state_at,\n        json_extract(st.payload_json,'$.cat_house_count') house_count,\n        json_extract(st.payload_json,'$.cat_house_capacity') house_capacity,\n        json_extract(st.payload_json,'$.cat_house_occupied') house_occupied\n        FROM ids LEFT JOIN user_sessions u USING(user_id) LEFT JOIN states st ON st.user_id=ids.user_id AND st.rn=1\n        LEFT JOIN legacy l ON l.user_id=ids.user_id AND l.rn=1 LEFT JOIN days d USING(user_id)),\n    e AS (SELECT * FROM e0 WHERE ('${behavior_period}'='all' OR julianday(occurred_at) >= julianday(${__from}/1000,'unixepoch') AND julianday(occurred_at) < julianday(${__to}/1000,'unixepoch'))\n        OR (occurred_at IS NULL AND '${behavior_period}'='all')),\n    players AS (SELECT * FROM p WHERE 1),\n    events AS (SELECT * FROM e WHERE 1) SELECT COALESCE(NULLIF(COALESCE(json_extract(payload_json,'$.cat.breed'),json_extract(payload_json,'$.cat.skin')),''),'未采集名称') 名称,COUNT(*) 次数,COUNT(DISTINCT user_id) 参与玩家数 FROM events WHERE event_type='cat_adopted' AND actor_is_player=1 AND 1 GROUP BY COALESCE(json_extract(payload_json,'$.template_id'),COALESCE(json_extract(payload_json,'$.cat.breed'),json_extract(payload_json,'$.cat.skin'))) ORDER BY 次数 DESC,名称 LIMIT 5"
        }
      ],
      "fieldConfig": {
        "defaults": {
          "unit": "short",
          "custom": {
            "filterable": true
          },
          "noValue": "未采集"
        },
        "overrides": [
          {
            "matcher": {
              "id": "byName",
              "options": "user_id"
            },
            "properties": [
              {
                "id": "links",
                "value": [
                  {
                    "title": "查看玩家旅程",
                    "url": "/d/gameplay-player-detail?${__url_time_range}&${client_platform:queryparam}&${release_version:queryparam}&${test_data:queryparam}&var-user_id=${__data.fields.user_id}&var-user_id=${__value.raw}"
                  }
                ]
              }
            ]
          },
          {
            "matcher": {
              "id": "byName",
              "options": "session_id"
            },
            "properties": [
              {
                "id": "links",
                "value": [
                  {
                    "title": "查看玩家旅程",
                    "url": "/d/gameplay-player-detail?${__url_time_range}&${client_platform:queryparam}&${release_version:queryparam}&${test_data:queryparam}&var-user_id=${__data.fields.user_id}&var-session_id=${__value.raw}"
                  }
                ]
              }
            ]
          },
          {
            "matcher": {
              "id": "byName",
              "options": "llm_request_id"
            },
            "properties": [
              {
                "id": "links",
                "value": [
                  {
                    "title": "查看玩家旅程",
                    "url": "/d/gameplay-player-detail?${__url_time_range}&${client_platform:queryparam}&${release_version:queryparam}&${test_data:queryparam}&var-user_id=${__data.fields.user_id}&var-llm_request_id=${__value.raw}"
                  }
                ]
              }
            ]
          }
        ]
      },
      "options": {
        "showHeader": true,
        "cellHeight": "sm"
      },
      "gridPos": {
        "x": 12,
        "y": 28,
        "w": 12,
        "h": 9
      }
    },
    {
      "id": 122,
      "title": "服装店购买 Top 5",
      "type": "table",
      "datasource": {
        "type": "frser-sqlite-datasource",
        "uid": "${DS_SQLITE}"
      },
      "description": "累计按玩家去重；当前状态取最新快照。缺失显示未采集，数值零保留。行为可切换期间，日期为北京时间。",
      "targets": [
        {
          "refId": "A",
          "queryType": "table",
          "queryText": "WITH e0 AS NOT MATERIALIZED (SELECT * FROM analytics_events WHERE ('__all__' IN (${client_platform:sqlstring}) OR client_platform IN (${client_platform:sqlstring})) AND ('__all__' IN (${release_version:sqlstring}) OR COALESCE(NULLIF(release_version,''),'__empty__') IN (${release_version:sqlstring})) AND ('${test_data}'='include' OR ('${test_data}'='auto' AND (client_platform='editor' OR (is_development_build=0 AND is_developer=0))) OR ('${test_data}'='exclude' AND is_development_build=0 AND is_developer=0 AND client_platform!='editor') OR ('${test_data}'='only' AND (is_development_build=1 OR is_developer=1 OR client_platform='editor')))),\n    s AS MATERIALIZED (SELECT * FROM analytics_sessions WHERE ('__all__' IN (${client_platform:sqlstring}) OR client_platform IN (${client_platform:sqlstring})) AND ('__all__' IN (${release_version:sqlstring}) OR COALESCE(NULLIF(release_version,''),'__empty__') IN (${release_version:sqlstring})) AND ('${test_data}'='include' OR ('${test_data}'='auto' AND (client_platform='editor' OR (is_development_build=0 AND is_developer=0))) OR ('${test_data}'='exclude' AND is_development_build=0 AND is_developer=0 AND client_platform!='editor') OR ('${test_data}'='only' AND (is_development_build=1 OR is_developer=1 OR client_platform='editor')))),\n    ids AS (SELECT user_id FROM s UNION SELECT user_id FROM e0),\n    states AS (SELECT *,ROW_NUMBER() OVER(PARTITION BY user_id ORDER BY julianday(occurred_at) DESC,sequence DESC,event_id DESC) rn\n        FROM e0 WHERE event_type='player_state_snapshot'),\n    legacy AS (SELECT g.*,ROW_NUMBER() OVER(PARTITION BY user_id ORDER BY julianday(real_time_started_iso) DESC,imported_at DESC) rn\n        FROM gameplay_sessions g WHERE EXISTS(SELECT 1 FROM s WHERE s.user_id=g.user_id AND s.session_id=g.session_id)),\n    days AS (SELECT user_id,COUNT(*) play_days FROM\n        (SELECT DISTINCT user_id,archive_id,game_day FROM e0 WHERE NULLIF(archive_id,'') IS NOT NULL AND game_day IS NOT NULL) GROUP BY user_id),\n    p AS (SELECT ids.user_id,COALESCE(NULLIF(json_extract(st.payload_json,'$.nickname'),''),NULLIF(u.nickname,''),NULLIF(u.player_name,''),ids.user_id) nickname,\n        (SELECT SUM(duration_sec) FROM s WHERE s.user_id=ids.user_id) play_seconds,\n        (SELECT first_entry_at FROM analytics_first_entry f WHERE f.user_id=ids.user_id AND f.cohort_scope=CASE WHEN '${test_data}' IN ('include','only') OR ('${test_data}'='auto' AND ('editor' IN (${client_platform:sqlstring}) OR '__all__' IN (${client_platform:sqlstring}))) THEN 'all' ELSE 'production' END) first_login,\n        (SELECT MAX(started_at) FROM s WHERE s.user_id=ids.user_id) latest_login,\n        d.play_days,COALESCE(json_extract(st.payload_json,'$.money'),l.money_end,l.money_start) money,\n        COALESCE(json_extract(st.payload_json,'$.island_level'),l.island_level_max) island_level,\n        json_array_length(json_extract(st.payload_json,'$.cats')) cat_count,\n        st.payload_json state,st.occurred_at state_at,\n        json_extract(st.payload_json,'$.cat_house_count') house_count,\n        json_extract(st.payload_json,'$.cat_house_capacity') house_capacity,\n        json_extract(st.payload_json,'$.cat_house_occupied') house_occupied\n        FROM ids LEFT JOIN user_sessions u USING(user_id) LEFT JOIN states st ON st.user_id=ids.user_id AND st.rn=1\n        LEFT JOIN legacy l ON l.user_id=ids.user_id AND l.rn=1 LEFT JOIN days d USING(user_id)),\n    e AS (SELECT * FROM e0 WHERE ('${behavior_period}'='all' OR julianday(occurred_at) >= julianday(${__from}/1000,'unixepoch') AND julianday(occurred_at) < julianday(${__to}/1000,'unixepoch'))\n        OR (occurred_at IS NULL AND '${behavior_period}'='all')),\n    players AS (SELECT * FROM p WHERE 1),\n    events AS (SELECT * FROM e WHERE 1) SELECT COALESCE(NULLIF(json_extract(payload_json,'$.item_name'),''),'未采集名称') 名称,COUNT(*) 次数,COUNT(DISTINCT user_id) 参与玩家数,SUM(COALESCE(json_extract(payload_json,'$.quantity'),1)) 件数 FROM events WHERE event_type='shop_purchase' AND actor_is_player=1 AND json_extract(payload_json,'$.shop_type')='clothing' GROUP BY COALESCE(json_extract(payload_json,'$.item_id'),json_extract(payload_json,'$.item_name')) ORDER BY 次数 DESC,名称 LIMIT 5",
          "rawQueryText": "WITH e0 AS NOT MATERIALIZED (SELECT * FROM analytics_events WHERE ('__all__' IN (${client_platform:sqlstring}) OR client_platform IN (${client_platform:sqlstring})) AND ('__all__' IN (${release_version:sqlstring}) OR COALESCE(NULLIF(release_version,''),'__empty__') IN (${release_version:sqlstring})) AND ('${test_data}'='include' OR ('${test_data}'='auto' AND (client_platform='editor' OR (is_development_build=0 AND is_developer=0))) OR ('${test_data}'='exclude' AND is_development_build=0 AND is_developer=0 AND client_platform!='editor') OR ('${test_data}'='only' AND (is_development_build=1 OR is_developer=1 OR client_platform='editor')))),\n    s AS MATERIALIZED (SELECT * FROM analytics_sessions WHERE ('__all__' IN (${client_platform:sqlstring}) OR client_platform IN (${client_platform:sqlstring})) AND ('__all__' IN (${release_version:sqlstring}) OR COALESCE(NULLIF(release_version,''),'__empty__') IN (${release_version:sqlstring})) AND ('${test_data}'='include' OR ('${test_data}'='auto' AND (client_platform='editor' OR (is_development_build=0 AND is_developer=0))) OR ('${test_data}'='exclude' AND is_development_build=0 AND is_developer=0 AND client_platform!='editor') OR ('${test_data}'='only' AND (is_development_build=1 OR is_developer=1 OR client_platform='editor')))),\n    ids AS (SELECT user_id FROM s UNION SELECT user_id FROM e0),\n    states AS (SELECT *,ROW_NUMBER() OVER(PARTITION BY user_id ORDER BY julianday(occurred_at) DESC,sequence DESC,event_id DESC) rn\n        FROM e0 WHERE event_type='player_state_snapshot'),\n    legacy AS (SELECT g.*,ROW_NUMBER() OVER(PARTITION BY user_id ORDER BY julianday(real_time_started_iso) DESC,imported_at DESC) rn\n        FROM gameplay_sessions g WHERE EXISTS(SELECT 1 FROM s WHERE s.user_id=g.user_id AND s.session_id=g.session_id)),\n    days AS (SELECT user_id,COUNT(*) play_days FROM\n        (SELECT DISTINCT user_id,archive_id,game_day FROM e0 WHERE NULLIF(archive_id,'') IS NOT NULL AND game_day IS NOT NULL) GROUP BY user_id),\n    p AS (SELECT ids.user_id,COALESCE(NULLIF(json_extract(st.payload_json,'$.nickname'),''),NULLIF(u.nickname,''),NULLIF(u.player_name,''),ids.user_id) nickname,\n        (SELECT SUM(duration_sec) FROM s WHERE s.user_id=ids.user_id) play_seconds,\n        (SELECT first_entry_at FROM analytics_first_entry f WHERE f.user_id=ids.user_id AND f.cohort_scope=CASE WHEN '${test_data}' IN ('include','only') OR ('${test_data}'='auto' AND ('editor' IN (${client_platform:sqlstring}) OR '__all__' IN (${client_platform:sqlstring}))) THEN 'all' ELSE 'production' END) first_login,\n        (SELECT MAX(started_at) FROM s WHERE s.user_id=ids.user_id) latest_login,\n        d.play_days,COALESCE(json_extract(st.payload_json,'$.money'),l.money_end,l.money_start) money,\n        COALESCE(json_extract(st.payload_json,'$.island_level'),l.island_level_max) island_level,\n        json_array_length(json_extract(st.payload_json,'$.cats')) cat_count,\n        st.payload_json state,st.occurred_at state_at,\n        json_extract(st.payload_json,'$.cat_house_count') house_count,\n        json_extract(st.payload_json,'$.cat_house_capacity') house_capacity,\n        json_extract(st.payload_json,'$.cat_house_occupied') house_occupied\n        FROM ids LEFT JOIN user_sessions u USING(user_id) LEFT JOIN states st ON st.user_id=ids.user_id AND st.rn=1\n        LEFT JOIN legacy l ON l.user_id=ids.user_id AND l.rn=1 LEFT JOIN days d USING(user_id)),\n    e AS (SELECT * FROM e0 WHERE ('${behavior_period}'='all' OR julianday(occurred_at) >= julianday(${__from}/1000,'unixepoch') AND julianday(occurred_at) < julianday(${__to}/1000,'unixepoch'))\n        OR (occurred_at IS NULL AND '${behavior_period}'='all')),\n    players AS (SELECT * FROM p WHERE 1),\n    events AS (SELECT * FROM e WHERE 1) SELECT COALESCE(NULLIF(json_extract(payload_json,'$.item_name'),''),'未采集名称') 名称,COUNT(*) 次数,COUNT(DISTINCT user_id) 参与玩家数,SUM(COALESCE(json_extract(payload_json,'$.quantity'),1)) 件数 FROM events WHERE event_type='shop_purchase' AND actor_is_player=1 AND json_extract(payload_json,'$.shop_type')='clothing' GROUP BY COALESCE(json_extract(payload_json,'$.item_id'),json_extract(payload_json,'$.item_name')) ORDER BY 次数 DESC,名称 LIMIT 5"
        }
      ],
      "fieldConfig": {
        "defaults": {
          "unit": "short",
          "custom": {
            "filterable": true
          },
          "noValue": "未采集"
        },
        "overrides": [
          {
            "matcher": {
              "id": "byName",
              "options": "user_id"
            },
            "properties": [
              {
                "id": "links",
                "value": [
                  {
                    "title": "查看玩家旅程",
                    "url": "/d/gameplay-player-detail?${__url_time_range}&${client_platform:queryparam}&${release_version:queryparam}&${test_data:queryparam}&var-user_id=${__data.fields.user_id}&var-user_id=${__value.raw}"
                  }
                ]
              }
            ]
          },
          {
            "matcher": {
              "id": "byName",
              "options": "session_id"
            },
            "properties": [
              {
                "id": "links",
                "value": [
                  {
                    "title": "查看玩家旅程",
                    "url": "/d/gameplay-player-detail?${__url_time_range}&${client_platform:queryparam}&${release_version:queryparam}&${test_data:queryparam}&var-user_id=${__data.fields.user_id}&var-session_id=${__value.raw}"
                  }
                ]
              }
            ]
          },
          {
            "matcher": {
              "id": "byName",
              "options": "llm_request_id"
            },
            "properties": [
              {
                "id": "links",
                "value": [
                  {
                    "title": "查看玩家旅程",
                    "url": "/d/gameplay-player-detail?${__url_time_range}&${client_platform:queryparam}&${release_version:queryparam}&${test_data:queryparam}&var-user_id=${__data.fields.user_id}&var-llm_request_id=${__value.raw}"
                  }
                ]
              }
            ]
          }
        ]
      },
      "options": {
        "showHeader": true,
        "cellHeight": "sm"
      },
      "gridPos": {
        "x": 0,
        "y": 37,
        "w": 12,
        "h": 9
      }
    },
    {
      "id": 123,
      "title": "杂货店购买 Top 5",
      "type": "table",
      "datasource": {
        "type": "frser-sqlite-datasource",
        "uid": "${DS_SQLITE}"
      },
      "description": "累计按玩家去重；当前状态取最新快照。缺失显示未采集，数值零保留。行为可切换期间，日期为北京时间。",
      "targets": [
        {
          "refId": "A",
          "queryType": "table",
          "queryText": "WITH e0 AS NOT MATERIALIZED (SELECT * FROM analytics_events WHERE ('__all__' IN (${client_platform:sqlstring}) OR client_platform IN (${client_platform:sqlstring})) AND ('__all__' IN (${release_version:sqlstring}) OR COALESCE(NULLIF(release_version,''),'__empty__') IN (${release_version:sqlstring})) AND ('${test_data}'='include' OR ('${test_data}'='auto' AND (client_platform='editor' OR (is_development_build=0 AND is_developer=0))) OR ('${test_data}'='exclude' AND is_development_build=0 AND is_developer=0 AND client_platform!='editor') OR ('${test_data}'='only' AND (is_development_build=1 OR is_developer=1 OR client_platform='editor')))),\n    s AS MATERIALIZED (SELECT * FROM analytics_sessions WHERE ('__all__' IN (${client_platform:sqlstring}) OR client_platform IN (${client_platform:sqlstring})) AND ('__all__' IN (${release_version:sqlstring}) OR COALESCE(NULLIF(release_version,''),'__empty__') IN (${release_version:sqlstring})) AND ('${test_data}'='include' OR ('${test_data}'='auto' AND (client_platform='editor' OR (is_development_build=0 AND is_developer=0))) OR ('${test_data}'='exclude' AND is_development_build=0 AND is_developer=0 AND client_platform!='editor') OR ('${test_data}'='only' AND (is_development_build=1 OR is_developer=1 OR client_platform='editor')))),\n    ids AS (SELECT user_id FROM s UNION SELECT user_id FROM e0),\n    states AS (SELECT *,ROW_NUMBER() OVER(PARTITION BY user_id ORDER BY julianday(occurred_at) DESC,sequence DESC,event_id DESC) rn\n        FROM e0 WHERE event_type='player_state_snapshot'),\n    legacy AS (SELECT g.*,ROW_NUMBER() OVER(PARTITION BY user_id ORDER BY julianday(real_time_started_iso) DESC,imported_at DESC) rn\n        FROM gameplay_sessions g WHERE EXISTS(SELECT 1 FROM s WHERE s.user_id=g.user_id AND s.session_id=g.session_id)),\n    days AS (SELECT user_id,COUNT(*) play_days FROM\n        (SELECT DISTINCT user_id,archive_id,game_day FROM e0 WHERE NULLIF(archive_id,'') IS NOT NULL AND game_day IS NOT NULL) GROUP BY user_id),\n    p AS (SELECT ids.user_id,COALESCE(NULLIF(json_extract(st.payload_json,'$.nickname'),''),NULLIF(u.nickname,''),NULLIF(u.player_name,''),ids.user_id) nickname,\n        (SELECT SUM(duration_sec) FROM s WHERE s.user_id=ids.user_id) play_seconds,\n        (SELECT first_entry_at FROM analytics_first_entry f WHERE f.user_id=ids.user_id AND f.cohort_scope=CASE WHEN '${test_data}' IN ('include','only') OR ('${test_data}'='auto' AND ('editor' IN (${client_platform:sqlstring}) OR '__all__' IN (${client_platform:sqlstring}))) THEN 'all' ELSE 'production' END) first_login,\n        (SELECT MAX(started_at) FROM s WHERE s.user_id=ids.user_id) latest_login,\n        d.play_days,COALESCE(json_extract(st.payload_json,'$.money'),l.money_end,l.money_start) money,\n        COALESCE(json_extract(st.payload_json,'$.island_level'),l.island_level_max) island_level,\n        json_array_length(json_extract(st.payload_json,'$.cats')) cat_count,\n        st.payload_json state,st.occurred_at state_at,\n        json_extract(st.payload_json,'$.cat_house_count') house_count,\n        json_extract(st.payload_json,'$.cat_house_capacity') house_capacity,\n        json_extract(st.payload_json,'$.cat_house_occupied') house_occupied\n        FROM ids LEFT JOIN user_sessions u USING(user_id) LEFT JOIN states st ON st.user_id=ids.user_id AND st.rn=1\n        LEFT JOIN legacy l ON l.user_id=ids.user_id AND l.rn=1 LEFT JOIN days d USING(user_id)),\n    e AS (SELECT * FROM e0 WHERE ('${behavior_period}'='all' OR julianday(occurred_at) >= julianday(${__from}/1000,'unixepoch') AND julianday(occurred_at) < julianday(${__to}/1000,'unixepoch'))\n        OR (occurred_at IS NULL AND '${behavior_period}'='all')),\n    players AS (SELECT * FROM p WHERE 1),\n    events AS (SELECT * FROM e WHERE 1) SELECT COALESCE(NULLIF(json_extract(payload_json,'$.item_name'),''),'未采集名称') 名称,COUNT(*) 次数,COUNT(DISTINCT user_id) 参与玩家数,SUM(COALESCE(json_extract(payload_json,'$.quantity'),1)) 件数 FROM events WHERE event_type='shop_purchase' AND actor_is_player=1 AND json_extract(payload_json,'$.shop_type')='variety' GROUP BY COALESCE(json_extract(payload_json,'$.item_id'),json_extract(payload_json,'$.item_name')) ORDER BY 次数 DESC,名称 LIMIT 5",
          "rawQueryText": "WITH e0 AS NOT MATERIALIZED (SELECT * FROM analytics_events WHERE ('__all__' IN (${client_platform:sqlstring}) OR client_platform IN (${client_platform:sqlstring})) AND ('__all__' IN (${release_version:sqlstring}) OR COALESCE(NULLIF(release_version,''),'__empty__') IN (${release_version:sqlstring})) AND ('${test_data}'='include' OR ('${test_data}'='auto' AND (client_platform='editor' OR (is_development_build=0 AND is_developer=0))) OR ('${test_data}'='exclude' AND is_development_build=0 AND is_developer=0 AND client_platform!='editor') OR ('${test_data}'='only' AND (is_development_build=1 OR is_developer=1 OR client_platform='editor')))),\n    s AS MATERIALIZED (SELECT * FROM analytics_sessions WHERE ('__all__' IN (${client_platform:sqlstring}) OR client_platform IN (${client_platform:sqlstring})) AND ('__all__' IN (${release_version:sqlstring}) OR COALESCE(NULLIF(release_version,''),'__empty__') IN (${release_version:sqlstring})) AND ('${test_data}'='include' OR ('${test_data}'='auto' AND (client_platform='editor' OR (is_development_build=0 AND is_developer=0))) OR ('${test_data}'='exclude' AND is_development_build=0 AND is_developer=0 AND client_platform!='editor') OR ('${test_data}'='only' AND (is_development_build=1 OR is_developer=1 OR client_platform='editor')))),\n    ids AS (SELECT user_id FROM s UNION SELECT user_id FROM e0),\n    states AS (SELECT *,ROW_NUMBER() OVER(PARTITION BY user_id ORDER BY julianday(occurred_at) DESC,sequence DESC,event_id DESC) rn\n        FROM e0 WHERE event_type='player_state_snapshot'),\n    legacy AS (SELECT g.*,ROW_NUMBER() OVER(PARTITION BY user_id ORDER BY julianday(real_time_started_iso) DESC,imported_at DESC) rn\n        FROM gameplay_sessions g WHERE EXISTS(SELECT 1 FROM s WHERE s.user_id=g.user_id AND s.session_id=g.session_id)),\n    days AS (SELECT user_id,COUNT(*) play_days FROM\n        (SELECT DISTINCT user_id,archive_id,game_day FROM e0 WHERE NULLIF(archive_id,'') IS NOT NULL AND game_day IS NOT NULL) GROUP BY user_id),\n    p AS (SELECT ids.user_id,COALESCE(NULLIF(json_extract(st.payload_json,'$.nickname'),''),NULLIF(u.nickname,''),NULLIF(u.player_name,''),ids.user_id) nickname,\n        (SELECT SUM(duration_sec) FROM s WHERE s.user_id=ids.user_id) play_seconds,\n        (SELECT first_entry_at FROM analytics_first_entry f WHERE f.user_id=ids.user_id AND f.cohort_scope=CASE WHEN '${test_data}' IN ('include','only') OR ('${test_data}'='auto' AND ('editor' IN (${client_platform:sqlstring}) OR '__all__' IN (${client_platform:sqlstring}))) THEN 'all' ELSE 'production' END) first_login,\n        (SELECT MAX(started_at) FROM s WHERE s.user_id=ids.user_id) latest_login,\n        d.play_days,COALESCE(json_extract(st.payload_json,'$.money'),l.money_end,l.money_start) money,\n        COALESCE(json_extract(st.payload_json,'$.island_level'),l.island_level_max) island_level,\n        json_array_length(json_extract(st.payload_json,'$.cats')) cat_count,\n        st.payload_json state,st.occurred_at state_at,\n        json_extract(st.payload_json,'$.cat_house_count') house_count,\n        json_extract(st.payload_json,'$.cat_house_capacity') house_capacity,\n        json_extract(st.payload_json,'$.cat_house_occupied') house_occupied\n        FROM ids LEFT JOIN user_sessions u USING(user_id) LEFT JOIN states st ON st.user_id=ids.user_id AND st.rn=1\n        LEFT JOIN legacy l ON l.user_id=ids.user_id AND l.rn=1 LEFT JOIN days d USING(user_id)),\n    e AS (SELECT * FROM e0 WHERE ('${behavior_period}'='all' OR julianday(occurred_at) >= julianday(${__from}/1000,'unixepoch') AND julianday(occurred_at) < julianday(${__to}/1000,'unixepoch'))\n        OR (occurred_at IS NULL AND '${behavior_period}'='all')),\n    players AS (SELECT * FROM p WHERE 1),\n    events AS (SELECT * FROM e WHERE 1) SELECT COALESCE(NULLIF(json_extract(payload_json,'$.item_name'),''),'未采集名称') 名称,COUNT(*) 次数,COUNT(DISTINCT user_id) 参与玩家数,SUM(COALESCE(json_extract(payload_json,'$.quantity'),1)) 件数 FROM events WHERE event_type='shop_purchase' AND actor_is_player=1 AND json_extract(payload_json,'$.shop_type')='variety' GROUP BY COALESCE(json_extract(payload_json,'$.item_id'),json_extract(payload_json,'$.item_name')) ORDER BY 次数 DESC,名称 LIMIT 5"
        }
      ],
      "fieldConfig": {
        "defaults": {
          "unit": "short",
          "custom": {
            "filterable": true
          },
          "noValue": "未采集"
        },
        "overrides": [
          {
            "matcher": {
              "id": "byName",
              "options": "user_id"
            },
            "properties": [
              {
                "id": "links",
                "value": [
                  {
                    "title": "查看玩家旅程",
                    "url": "/d/gameplay-player-detail?${__url_time_range}&${client_platform:queryparam}&${release_version:queryparam}&${test_data:queryparam}&var-user_id=${__data.fields.user_id}&var-user_id=${__value.raw}"
                  }
                ]
              }
            ]
          },
          {
            "matcher": {
              "id": "byName",
              "options": "session_id"
            },
            "properties": [
              {
                "id": "links",
                "value": [
                  {
                    "title": "查看玩家旅程",
                    "url": "/d/gameplay-player-detail?${__url_time_range}&${client_platform:queryparam}&${release_version:queryparam}&${test_data:queryparam}&var-user_id=${__data.fields.user_id}&var-session_id=${__value.raw}"
                  }
                ]
              }
            ]
          },
          {
            "matcher": {
              "id": "byName",
              "options": "llm_request_id"
            },
            "properties": [
              {
                "id": "links",
                "value": [
                  {
                    "title": "查看玩家旅程",
                    "url": "/d/gameplay-player-detail?${__url_time_range}&${client_platform:queryparam}&${release_version:queryparam}&${test_data:queryparam}&var-user_id=${__data.fields.user_id}&var-llm_request_id=${__value.raw}"
                  }
                ]
              }
            ]
          }
        ]
      },
      "options": {
        "showHeader": true,
        "cellHeight": "sm"
      },
      "gridPos": {
        "x": 12,
        "y": 37,
        "w": 12,
        "h": 9
      }
    },
    {
      "id": 124,
      "title": "服装店冻结 Top 5",
      "type": "table",
      "datasource": {
        "type": "frser-sqlite-datasource",
        "uid": "${DS_SQLITE}"
      },
      "description": "累计按玩家去重；当前状态取最新快照。缺失显示未采集，数值零保留。行为可切换期间，日期为北京时间。",
      "targets": [
        {
          "refId": "A",
          "queryType": "table",
          "queryText": "WITH e0 AS NOT MATERIALIZED (SELECT * FROM analytics_events WHERE ('__all__' IN (${client_platform:sqlstring}) OR client_platform IN (${client_platform:sqlstring})) AND ('__all__' IN (${release_version:sqlstring}) OR COALESCE(NULLIF(release_version,''),'__empty__') IN (${release_version:sqlstring})) AND ('${test_data}'='include' OR ('${test_data}'='auto' AND (client_platform='editor' OR (is_development_build=0 AND is_developer=0))) OR ('${test_data}'='exclude' AND is_development_build=0 AND is_developer=0 AND client_platform!='editor') OR ('${test_data}'='only' AND (is_development_build=1 OR is_developer=1 OR client_platform='editor')))),\n    s AS MATERIALIZED (SELECT * FROM analytics_sessions WHERE ('__all__' IN (${client_platform:sqlstring}) OR client_platform IN (${client_platform:sqlstring})) AND ('__all__' IN (${release_version:sqlstring}) OR COALESCE(NULLIF(release_version,''),'__empty__') IN (${release_version:sqlstring})) AND ('${test_data}'='include' OR ('${test_data}'='auto' AND (client_platform='editor' OR (is_development_build=0 AND is_developer=0))) OR ('${test_data}'='exclude' AND is_development_build=0 AND is_developer=0 AND client_platform!='editor') OR ('${test_data}'='only' AND (is_development_build=1 OR is_developer=1 OR client_platform='editor')))),\n    ids AS (SELECT user_id FROM s UNION SELECT user_id FROM e0),\n    states AS (SELECT *,ROW_NUMBER() OVER(PARTITION BY user_id ORDER BY julianday(occurred_at) DESC,sequence DESC,event_id DESC) rn\n        FROM e0 WHERE event_type='player_state_snapshot'),\n    legacy AS (SELECT g.*,ROW_NUMBER() OVER(PARTITION BY user_id ORDER BY julianday(real_time_started_iso) DESC,imported_at DESC) rn\n        FROM gameplay_sessions g WHERE EXISTS(SELECT 1 FROM s WHERE s.user_id=g.user_id AND s.session_id=g.session_id)),\n    days AS (SELECT user_id,COUNT(*) play_days FROM\n        (SELECT DISTINCT user_id,archive_id,game_day FROM e0 WHERE NULLIF(archive_id,'') IS NOT NULL AND game_day IS NOT NULL) GROUP BY user_id),\n    p AS (SELECT ids.user_id,COALESCE(NULLIF(json_extract(st.payload_json,'$.nickname'),''),NULLIF(u.nickname,''),NULLIF(u.player_name,''),ids.user_id) nickname,\n        (SELECT SUM(duration_sec) FROM s WHERE s.user_id=ids.user_id) play_seconds,\n        (SELECT first_entry_at FROM analytics_first_entry f WHERE f.user_id=ids.user_id AND f.cohort_scope=CASE WHEN '${test_data}' IN ('include','only') OR ('${test_data}'='auto' AND ('editor' IN (${client_platform:sqlstring}) OR '__all__' IN (${client_platform:sqlstring}))) THEN 'all' ELSE 'production' END) first_login,\n        (SELECT MAX(started_at) FROM s WHERE s.user_id=ids.user_id) latest_login,\n        d.play_days,COALESCE(json_extract(st.payload_json,'$.money'),l.money_end,l.money_start) money,\n        COALESCE(json_extract(st.payload_json,'$.island_level'),l.island_level_max) island_level,\n        json_array_length(json_extract(st.payload_json,'$.cats')) cat_count,\n        st.payload_json state,st.occurred_at state_at,\n        json_extract(st.payload_json,'$.cat_house_count') house_count,\n        json_extract(st.payload_json,'$.cat_house_capacity') house_capacity,\n        json_extract(st.payload_json,'$.cat_house_occupied') house_occupied\n        FROM ids LEFT JOIN user_sessions u USING(user_id) LEFT JOIN states st ON st.user_id=ids.user_id AND st.rn=1\n        LEFT JOIN legacy l ON l.user_id=ids.user_id AND l.rn=1 LEFT JOIN days d USING(user_id)),\n    e AS (SELECT * FROM e0 WHERE ('${behavior_period}'='all' OR julianday(occurred_at) >= julianday(${__from}/1000,'unixepoch') AND julianday(occurred_at) < julianday(${__to}/1000,'unixepoch'))\n        OR (occurred_at IS NULL AND '${behavior_period}'='all')),\n    players AS (SELECT * FROM p WHERE 1),\n    events AS (SELECT * FROM e WHERE 1) SELECT COALESCE(NULLIF(json_extract(payload_json,'$.item_name'),''),'未采集名称') 名称,COUNT(*) 次数,COUNT(DISTINCT user_id) 参与玩家数 FROM events WHERE event_type='shop_freeze_changed' AND actor_is_player=1 AND json_extract(payload_json,'$.shop_type')='clothing' AND json_extract(payload_json,'$.frozen')=1 GROUP BY COALESCE(json_extract(payload_json,'$.item_id'),json_extract(payload_json,'$.item_name')) ORDER BY 次数 DESC,名称 LIMIT 5",
          "rawQueryText": "WITH e0 AS NOT MATERIALIZED (SELECT * FROM analytics_events WHERE ('__all__' IN (${client_platform:sqlstring}) OR client_platform IN (${client_platform:sqlstring})) AND ('__all__' IN (${release_version:sqlstring}) OR COALESCE(NULLIF(release_version,''),'__empty__') IN (${release_version:sqlstring})) AND ('${test_data}'='include' OR ('${test_data}'='auto' AND (client_platform='editor' OR (is_development_build=0 AND is_developer=0))) OR ('${test_data}'='exclude' AND is_development_build=0 AND is_developer=0 AND client_platform!='editor') OR ('${test_data}'='only' AND (is_development_build=1 OR is_developer=1 OR client_platform='editor')))),\n    s AS MATERIALIZED (SELECT * FROM analytics_sessions WHERE ('__all__' IN (${client_platform:sqlstring}) OR client_platform IN (${client_platform:sqlstring})) AND ('__all__' IN (${release_version:sqlstring}) OR COALESCE(NULLIF(release_version,''),'__empty__') IN (${release_version:sqlstring})) AND ('${test_data}'='include' OR ('${test_data}'='auto' AND (client_platform='editor' OR (is_development_build=0 AND is_developer=0))) OR ('${test_data}'='exclude' AND is_development_build=0 AND is_developer=0 AND client_platform!='editor') OR ('${test_data}'='only' AND (is_development_build=1 OR is_developer=1 OR client_platform='editor')))),\n    ids AS (SELECT user_id FROM s UNION SELECT user_id FROM e0),\n    states AS (SELECT *,ROW_NUMBER() OVER(PARTITION BY user_id ORDER BY julianday(occurred_at) DESC,sequence DESC,event_id DESC) rn\n        FROM e0 WHERE event_type='player_state_snapshot'),\n    legacy AS (SELECT g.*,ROW_NUMBER() OVER(PARTITION BY user_id ORDER BY julianday(real_time_started_iso) DESC,imported_at DESC) rn\n        FROM gameplay_sessions g WHERE EXISTS(SELECT 1 FROM s WHERE s.user_id=g.user_id AND s.session_id=g.session_id)),\n    days AS (SELECT user_id,COUNT(*) play_days FROM\n        (SELECT DISTINCT user_id,archive_id,game_day FROM e0 WHERE NULLIF(archive_id,'') IS NOT NULL AND game_day IS NOT NULL) GROUP BY user_id),\n    p AS (SELECT ids.user_id,COALESCE(NULLIF(json_extract(st.payload_json,'$.nickname'),''),NULLIF(u.nickname,''),NULLIF(u.player_name,''),ids.user_id) nickname,\n        (SELECT SUM(duration_sec) FROM s WHERE s.user_id=ids.user_id) play_seconds,\n        (SELECT first_entry_at FROM analytics_first_entry f WHERE f.user_id=ids.user_id AND f.cohort_scope=CASE WHEN '${test_data}' IN ('include','only') OR ('${test_data}'='auto' AND ('editor' IN (${client_platform:sqlstring}) OR '__all__' IN (${client_platform:sqlstring}))) THEN 'all' ELSE 'production' END) first_login,\n        (SELECT MAX(started_at) FROM s WHERE s.user_id=ids.user_id) latest_login,\n        d.play_days,COALESCE(json_extract(st.payload_json,'$.money'),l.money_end,l.money_start) money,\n        COALESCE(json_extract(st.payload_json,'$.island_level'),l.island_level_max) island_level,\n        json_array_length(json_extract(st.payload_json,'$.cats')) cat_count,\n        st.payload_json state,st.occurred_at state_at,\n        json_extract(st.payload_json,'$.cat_house_count') house_count,\n        json_extract(st.payload_json,'$.cat_house_capacity') house_capacity,\n        json_extract(st.payload_json,'$.cat_house_occupied') house_occupied\n        FROM ids LEFT JOIN user_sessions u USING(user_id) LEFT JOIN states st ON st.user_id=ids.user_id AND st.rn=1\n        LEFT JOIN legacy l ON l.user_id=ids.user_id AND l.rn=1 LEFT JOIN days d USING(user_id)),\n    e AS (SELECT * FROM e0 WHERE ('${behavior_period}'='all' OR julianday(occurred_at) >= julianday(${__from}/1000,'unixepoch') AND julianday(occurred_at) < julianday(${__to}/1000,'unixepoch'))\n        OR (occurred_at IS NULL AND '${behavior_period}'='all')),\n    players AS (SELECT * FROM p WHERE 1),\n    events AS (SELECT * FROM e WHERE 1) SELECT COALESCE(NULLIF(json_extract(payload_json,'$.item_name'),''),'未采集名称') 名称,COUNT(*) 次数,COUNT(DISTINCT user_id) 参与玩家数 FROM events WHERE event_type='shop_freeze_changed' AND actor_is_player=1 AND json_extract(payload_json,'$.shop_type')='clothing' AND json_extract(payload_json,'$.frozen')=1 GROUP BY COALESCE(json_extract(payload_json,'$.item_id'),json_extract(payload_json,'$.item_name')) ORDER BY 次数 DESC,名称 LIMIT 5"
        }
      ],
      "fieldConfig": {
        "defaults": {
          "unit": "short",
          "custom": {
            "filterable": true
          },
          "noValue": "未采集"
        },
        "overrides": [
          {
            "matcher": {
              "id": "byName",
              "options": "user_id"
            },
            "properties": [
              {
                "id": "links",
                "value": [
                  {
                    "title": "查看玩家旅程",
                    "url": "/d/gameplay-player-detail?${__url_time_range}&${client_platform:queryparam}&${release_version:queryparam}&${test_data:queryparam}&var-user_id=${__data.fields.user_id}&var-user_id=${__value.raw}"
                  }
                ]
              }
            ]
          },
          {
            "matcher": {
              "id": "byName",
              "options": "session_id"
            },
            "properties": [
              {
                "id": "links",
                "value": [
                  {
                    "title": "查看玩家旅程",
                    "url": "/d/gameplay-player-detail?${__url_time_range}&${client_platform:queryparam}&${release_version:queryparam}&${test_data:queryparam}&var-user_id=${__data.fields.user_id}&var-session_id=${__value.raw}"
                  }
                ]
              }
            ]
          },
          {
            "matcher": {
              "id": "byName",
              "options": "llm_request_id"
            },
            "properties": [
              {
                "id": "links",
                "value": [
                  {
                    "title": "查看玩家旅程",
                    "url": "/d/gameplay-player-detail?${__url_time_range}&${client_platform:queryparam}&${release_version:queryparam}&${test_data:queryparam}&var-user_id=${__data.fields.user_id}&var-llm_request_id=${__value.raw}"
                  }
                ]
              }
            ]
          }
        ]
      },
      "options": {
        "showHeader": true,
        "cellHeight": "sm"
      },
      "gridPos": {
        "x": 0,
        "y": 46,
        "w": 12,
        "h": 9
      }
    },
    {
      "id": 125,
      "title": "杂货店冻结 Top 5",
      "type": "table",
      "datasource": {
        "type": "frser-sqlite-datasource",
        "uid": "${DS_SQLITE}"
      },
      "description": "累计按玩家去重；当前状态取最新快照。缺失显示未采集，数值零保留。行为可切换期间，日期为北京时间。",
      "targets": [
        {
          "refId": "A",
          "queryType": "table",
          "queryText": "WITH e0 AS NOT MATERIALIZED (SELECT * FROM analytics_events WHERE ('__all__' IN (${client_platform:sqlstring}) OR client_platform IN (${client_platform:sqlstring})) AND ('__all__' IN (${release_version:sqlstring}) OR COALESCE(NULLIF(release_version,''),'__empty__') IN (${release_version:sqlstring})) AND ('${test_data}'='include' OR ('${test_data}'='auto' AND (client_platform='editor' OR (is_development_build=0 AND is_developer=0))) OR ('${test_data}'='exclude' AND is_development_build=0 AND is_developer=0 AND client_platform!='editor') OR ('${test_data}'='only' AND (is_development_build=1 OR is_developer=1 OR client_platform='editor')))),\n    s AS MATERIALIZED (SELECT * FROM analytics_sessions WHERE ('__all__' IN (${client_platform:sqlstring}) OR client_platform IN (${client_platform:sqlstring})) AND ('__all__' IN (${release_version:sqlstring}) OR COALESCE(NULLIF(release_version,''),'__empty__') IN (${release_version:sqlstring})) AND ('${test_data}'='include' OR ('${test_data}'='auto' AND (client_platform='editor' OR (is_development_build=0 AND is_developer=0))) OR ('${test_data}'='exclude' AND is_development_build=0 AND is_developer=0 AND client_platform!='editor') OR ('${test_data}'='only' AND (is_development_build=1 OR is_developer=1 OR client_platform='editor')))),\n    ids AS (SELECT user_id FROM s UNION SELECT user_id FROM e0),\n    states AS (SELECT *,ROW_NUMBER() OVER(PARTITION BY user_id ORDER BY julianday(occurred_at) DESC,sequence DESC,event_id DESC) rn\n        FROM e0 WHERE event_type='player_state_snapshot'),\n    legacy AS (SELECT g.*,ROW_NUMBER() OVER(PARTITION BY user_id ORDER BY julianday(real_time_started_iso) DESC,imported_at DESC) rn\n        FROM gameplay_sessions g WHERE EXISTS(SELECT 1 FROM s WHERE s.user_id=g.user_id AND s.session_id=g.session_id)),\n    days AS (SELECT user_id,COUNT(*) play_days FROM\n        (SELECT DISTINCT user_id,archive_id,game_day FROM e0 WHERE NULLIF(archive_id,'') IS NOT NULL AND game_day IS NOT NULL) GROUP BY user_id),\n    p AS (SELECT ids.user_id,COALESCE(NULLIF(json_extract(st.payload_json,'$.nickname'),''),NULLIF(u.nickname,''),NULLIF(u.player_name,''),ids.user_id) nickname,\n        (SELECT SUM(duration_sec) FROM s WHERE s.user_id=ids.user_id) play_seconds,\n        (SELECT first_entry_at FROM analytics_first_entry f WHERE f.user_id=ids.user_id AND f.cohort_scope=CASE WHEN '${test_data}' IN ('include','only') OR ('${test_data}'='auto' AND ('editor' IN (${client_platform:sqlstring}) OR '__all__' IN (${client_platform:sqlstring}))) THEN 'all' ELSE 'production' END) first_login,\n        (SELECT MAX(started_at) FROM s WHERE s.user_id=ids.user_id) latest_login,\n        d.play_days,COALESCE(json_extract(st.payload_json,'$.money'),l.money_end,l.money_start) money,\n        COALESCE(json_extract(st.payload_json,'$.island_level'),l.island_level_max) island_level,\n        json_array_length(json_extract(st.payload_json,'$.cats')) cat_count,\n        st.payload_json state,st.occurred_at state_at,\n        json_extract(st.payload_json,'$.cat_house_count') house_count,\n        json_extract(st.payload_json,'$.cat_house_capacity') house_capacity,\n        json_extract(st.payload_json,'$.cat_house_occupied') house_occupied\n        FROM ids LEFT JOIN user_sessions u USING(user_id) LEFT JOIN states st ON st.user_id=ids.user_id AND st.rn=1\n        LEFT JOIN legacy l ON l.user_id=ids.user_id AND l.rn=1 LEFT JOIN days d USING(user_id)),\n    e AS (SELECT * FROM e0 WHERE ('${behavior_period}'='all' OR julianday(occurred_at) >= julianday(${__from}/1000,'unixepoch') AND julianday(occurred_at) < julianday(${__to}/1000,'unixepoch'))\n        OR (occurred_at IS NULL AND '${behavior_period}'='all')),\n    players AS (SELECT * FROM p WHERE 1),\n    events AS (SELECT * FROM e WHERE 1) SELECT COALESCE(NULLIF(json_extract(payload_json,'$.item_name'),''),'未采集名称') 名称,COUNT(*) 次数,COUNT(DISTINCT user_id) 参与玩家数 FROM events WHERE event_type='shop_freeze_changed' AND actor_is_player=1 AND json_extract(payload_json,'$.shop_type')='variety' AND json_extract(payload_json,'$.frozen')=1 GROUP BY COALESCE(json_extract(payload_json,'$.item_id'),json_extract(payload_json,'$.item_name')) ORDER BY 次数 DESC,名称 LIMIT 5",
          "rawQueryText": "WITH e0 AS NOT MATERIALIZED (SELECT * FROM analytics_events WHERE ('__all__' IN (${client_platform:sqlstring}) OR client_platform IN (${client_platform:sqlstring})) AND ('__all__' IN (${release_version:sqlstring}) OR COALESCE(NULLIF(release_version,''),'__empty__') IN (${release_version:sqlstring})) AND ('${test_data}'='include' OR ('${test_data}'='auto' AND (client_platform='editor' OR (is_development_build=0 AND is_developer=0))) OR ('${test_data}'='exclude' AND is_development_build=0 AND is_developer=0 AND client_platform!='editor') OR ('${test_data}'='only' AND (is_development_build=1 OR is_developer=1 OR client_platform='editor')))),\n    s AS MATERIALIZED (SELECT * FROM analytics_sessions WHERE ('__all__' IN (${client_platform:sqlstring}) OR client_platform IN (${client_platform:sqlstring})) AND ('__all__' IN (${release_version:sqlstring}) OR COALESCE(NULLIF(release_version,''),'__empty__') IN (${release_version:sqlstring})) AND ('${test_data}'='include' OR ('${test_data}'='auto' AND (client_platform='editor' OR (is_development_build=0 AND is_developer=0))) OR ('${test_data}'='exclude' AND is_development_build=0 AND is_developer=0 AND client_platform!='editor') OR ('${test_data}'='only' AND (is_development_build=1 OR is_developer=1 OR client_platform='editor')))),\n    ids AS (SELECT user_id FROM s UNION SELECT user_id FROM e0),\n    states AS (SELECT *,ROW_NUMBER() OVER(PARTITION BY user_id ORDER BY julianday(occurred_at) DESC,sequence DESC,event_id DESC) rn\n        FROM e0 WHERE event_type='player_state_snapshot'),\n    legacy AS (SELECT g.*,ROW_NUMBER() OVER(PARTITION BY user_id ORDER BY julianday(real_time_started_iso) DESC,imported_at DESC) rn\n        FROM gameplay_sessions g WHERE EXISTS(SELECT 1 FROM s WHERE s.user_id=g.user_id AND s.session_id=g.session_id)),\n    days AS (SELECT user_id,COUNT(*) play_days FROM\n        (SELECT DISTINCT user_id,archive_id,game_day FROM e0 WHERE NULLIF(archive_id,'') IS NOT NULL AND game_day IS NOT NULL) GROUP BY user_id),\n    p AS (SELECT ids.user_id,COALESCE(NULLIF(json_extract(st.payload_json,'$.nickname'),''),NULLIF(u.nickname,''),NULLIF(u.player_name,''),ids.user_id) nickname,\n        (SELECT SUM(duration_sec) FROM s WHERE s.user_id=ids.user_id) play_seconds,\n        (SELECT first_entry_at FROM analytics_first_entry f WHERE f.user_id=ids.user_id AND f.cohort_scope=CASE WHEN '${test_data}' IN ('include','only') OR ('${test_data}'='auto' AND ('editor' IN (${client_platform:sqlstring}) OR '__all__' IN (${client_platform:sqlstring}))) THEN 'all' ELSE 'production' END) first_login,\n        (SELECT MAX(started_at) FROM s WHERE s.user_id=ids.user_id) latest_login,\n        d.play_days,COALESCE(json_extract(st.payload_json,'$.money'),l.money_end,l.money_start) money,\n        COALESCE(json_extract(st.payload_json,'$.island_level'),l.island_level_max) island_level,\n        json_array_length(json_extract(st.payload_json,'$.cats')) cat_count,\n        st.payload_json state,st.occurred_at state_at,\n        json_extract(st.payload_json,'$.cat_house_count') house_count,\n        json_extract(st.payload_json,'$.cat_house_capacity') house_capacity,\n        json_extract(st.payload_json,'$.cat_house_occupied') house_occupied\n        FROM ids LEFT JOIN user_sessions u USING(user_id) LEFT JOIN states st ON st.user_id=ids.user_id AND st.rn=1\n        LEFT JOIN legacy l ON l.user_id=ids.user_id AND l.rn=1 LEFT JOIN days d USING(user_id)),\n    e AS (SELECT * FROM e0 WHERE ('${behavior_period}'='all' OR julianday(occurred_at) >= julianday(${__from}/1000,'unixepoch') AND julianday(occurred_at) < julianday(${__to}/1000,'unixepoch'))\n        OR (occurred_at IS NULL AND '${behavior_period}'='all')),\n    players AS (SELECT * FROM p WHERE 1),\n    events AS (SELECT * FROM e WHERE 1) SELECT COALESCE(NULLIF(json_extract(payload_json,'$.item_name'),''),'未采集名称') 名称,COUNT(*) 次数,COUNT(DISTINCT user_id) 参与玩家数 FROM events WHERE event_type='shop_freeze_changed' AND actor_is_player=1 AND json_extract(payload_json,'$.shop_type')='variety' AND json_extract(payload_json,'$.frozen')=1 GROUP BY COALESCE(json_extract(payload_json,'$.item_id'),json_extract(payload_json,'$.item_name')) ORDER BY 次数 DESC,名称 LIMIT 5"
        }
      ],
      "fieldConfig": {
        "defaults": {
          "unit": "short",
          "custom": {
            "filterable": true
          },
          "noValue": "未采集"
        },
        "overrides": [
          {
            "matcher": {
              "id": "byName",
              "options": "user_id"
            },
            "properties": [
              {
                "id": "links",
                "value": [
                  {
                    "title": "查看玩家旅程",
                    "url": "/d/gameplay-player-detail?${__url_time_range}&${client_platform:queryparam}&${release_version:queryparam}&${test_data:queryparam}&var-user_id=${__data.fields.user_id}&var-user_id=${__value.raw}"
                  }
                ]
              }
            ]
          },
          {
            "matcher": {
              "id": "byName",
              "options": "session_id"
            },
            "properties": [
              {
                "id": "links",
                "value": [
                  {
                    "title": "查看玩家旅程",
                    "url": "/d/gameplay-player-detail?${__url_time_range}&${client_platform:queryparam}&${release_version:queryparam}&${test_data:queryparam}&var-user_id=${__data.fields.user_id}&var-session_id=${__value.raw}"
                  }
                ]
              }
            ]
          },
          {
            "matcher": {
              "id": "byName",
              "options": "llm_request_id"
            },
            "properties": [
              {
                "id": "links",
                "value": [
                  {
                    "title": "查看玩家旅程",
                    "url": "/d/gameplay-player-detail?${__url_time_range}&${client_platform:queryparam}&${release_version:queryparam}&${test_data:queryparam}&var-user_id=${__data.fields.user_id}&var-llm_request_id=${__value.raw}"
                  }
                ]
              }
            ]
          }
        ]
      },
      "options": {
        "showHeader": true,
        "cellHeight": "sm"
      },
      "gridPos": {
        "x": 12,
        "y": 46,
        "w": 12,
        "h": 9
      }
    },
    {
      "id": 126,
      "title": "种植种子 Top 5",
      "type": "table",
      "datasource": {
        "type": "frser-sqlite-datasource",
        "uid": "${DS_SQLITE}"
      },
      "description": "累计按玩家去重；当前状态取最新快照。缺失显示未采集，数值零保留。行为可切换期间，日期为北京时间。",
      "targets": [
        {
          "refId": "A",
          "queryType": "table",
          "queryText": "WITH e0 AS NOT MATERIALIZED (SELECT * FROM analytics_events WHERE ('__all__' IN (${client_platform:sqlstring}) OR client_platform IN (${client_platform:sqlstring})) AND ('__all__' IN (${release_version:sqlstring}) OR COALESCE(NULLIF(release_version,''),'__empty__') IN (${release_version:sqlstring})) AND ('${test_data}'='include' OR ('${test_data}'='auto' AND (client_platform='editor' OR (is_development_build=0 AND is_developer=0))) OR ('${test_data}'='exclude' AND is_development_build=0 AND is_developer=0 AND client_platform!='editor') OR ('${test_data}'='only' AND (is_development_build=1 OR is_developer=1 OR client_platform='editor')))),\n    s AS MATERIALIZED (SELECT * FROM analytics_sessions WHERE ('__all__' IN (${client_platform:sqlstring}) OR client_platform IN (${client_platform:sqlstring})) AND ('__all__' IN (${release_version:sqlstring}) OR COALESCE(NULLIF(release_version,''),'__empty__') IN (${release_version:sqlstring})) AND ('${test_data}'='include' OR ('${test_data}'='auto' AND (client_platform='editor' OR (is_development_build=0 AND is_developer=0))) OR ('${test_data}'='exclude' AND is_development_build=0 AND is_developer=0 AND client_platform!='editor') OR ('${test_data}'='only' AND (is_development_build=1 OR is_developer=1 OR client_platform='editor')))),\n    ids AS (SELECT user_id FROM s UNION SELECT user_id FROM e0),\n    states AS (SELECT *,ROW_NUMBER() OVER(PARTITION BY user_id ORDER BY julianday(occurred_at) DESC,sequence DESC,event_id DESC) rn\n        FROM e0 WHERE event_type='player_state_snapshot'),\n    legacy AS (SELECT g.*,ROW_NUMBER() OVER(PARTITION BY user_id ORDER BY julianday(real_time_started_iso) DESC,imported_at DESC) rn\n        FROM gameplay_sessions g WHERE EXISTS(SELECT 1 FROM s WHERE s.user_id=g.user_id AND s.session_id=g.session_id)),\n    days AS (SELECT user_id,COUNT(*) play_days FROM\n        (SELECT DISTINCT user_id,archive_id,game_day FROM e0 WHERE NULLIF(archive_id,'') IS NOT NULL AND game_day IS NOT NULL) GROUP BY user_id),\n    p AS (SELECT ids.user_id,COALESCE(NULLIF(json_extract(st.payload_json,'$.nickname'),''),NULLIF(u.nickname,''),NULLIF(u.player_name,''),ids.user_id) nickname,\n        (SELECT SUM(duration_sec) FROM s WHERE s.user_id=ids.user_id) play_seconds,\n        (SELECT first_entry_at FROM analytics_first_entry f WHERE f.user_id=ids.user_id AND f.cohort_scope=CASE WHEN '${test_data}' IN ('include','only') OR ('${test_data}'='auto' AND ('editor' IN (${client_platform:sqlstring}) OR '__all__' IN (${client_platform:sqlstring}))) THEN 'all' ELSE 'production' END) first_login,\n        (SELECT MAX(started_at) FROM s WHERE s.user_id=ids.user_id) latest_login,\n        d.play_days,COALESCE(json_extract(st.payload_json,'$.money'),l.money_end,l.money_start) money,\n        COALESCE(json_extract(st.payload_json,'$.island_level'),l.island_level_max) island_level,\n        json_array_length(json_extract(st.payload_json,'$.cats')) cat_count,\n        st.payload_json state,st.occurred_at state_at,\n        json_extract(st.payload_json,'$.cat_house_count') house_count,\n        json_extract(st.payload_json,'$.cat_house_capacity') house_capacity,\n        json_extract(st.payload_json,'$.cat_house_occupied') house_occupied\n        FROM ids LEFT JOIN user_sessions u USING(user_id) LEFT JOIN states st ON st.user_id=ids.user_id AND st.rn=1\n        LEFT JOIN legacy l ON l.user_id=ids.user_id AND l.rn=1 LEFT JOIN days d USING(user_id)),\n    e AS (SELECT * FROM e0 WHERE ('${behavior_period}'='all' OR julianday(occurred_at) >= julianday(${__from}/1000,'unixepoch') AND julianday(occurred_at) < julianday(${__to}/1000,'unixepoch'))\n        OR (occurred_at IS NULL AND '${behavior_period}'='all')),\n    players AS (SELECT * FROM p WHERE 1),\n    events AS (SELECT * FROM e WHERE 1) SELECT COALESCE(NULLIF(json_extract(payload_json,'$.seed_name'),''),'未采集名称') 名称,COUNT(*) 次数,COUNT(DISTINCT user_id) 参与玩家数 FROM events WHERE event_type='farming_plant' AND actor_is_player=1 AND 1 GROUP BY COALESCE(json_extract(payload_json,'$.seed_id'),json_extract(payload_json,'$.seed_name')) ORDER BY 次数 DESC,名称 LIMIT 5",
          "rawQueryText": "WITH e0 AS NOT MATERIALIZED (SELECT * FROM analytics_events WHERE ('__all__' IN (${client_platform:sqlstring}) OR client_platform IN (${client_platform:sqlstring})) AND ('__all__' IN (${release_version:sqlstring}) OR COALESCE(NULLIF(release_version,''),'__empty__') IN (${release_version:sqlstring})) AND ('${test_data}'='include' OR ('${test_data}'='auto' AND (client_platform='editor' OR (is_development_build=0 AND is_developer=0))) OR ('${test_data}'='exclude' AND is_development_build=0 AND is_developer=0 AND client_platform!='editor') OR ('${test_data}'='only' AND (is_development_build=1 OR is_developer=1 OR client_platform='editor')))),\n    s AS MATERIALIZED (SELECT * FROM analytics_sessions WHERE ('__all__' IN (${client_platform:sqlstring}) OR client_platform IN (${client_platform:sqlstring})) AND ('__all__' IN (${release_version:sqlstring}) OR COALESCE(NULLIF(release_version,''),'__empty__') IN (${release_version:sqlstring})) AND ('${test_data}'='include' OR ('${test_data}'='auto' AND (client_platform='editor' OR (is_development_build=0 AND is_developer=0))) OR ('${test_data}'='exclude' AND is_development_build=0 AND is_developer=0 AND client_platform!='editor') OR ('${test_data}'='only' AND (is_development_build=1 OR is_developer=1 OR client_platform='editor')))),\n    ids AS (SELECT user_id FROM s UNION SELECT user_id FROM e0),\n    states AS (SELECT *,ROW_NUMBER() OVER(PARTITION BY user_id ORDER BY julianday(occurred_at) DESC,sequence DESC,event_id DESC) rn\n        FROM e0 WHERE event_type='player_state_snapshot'),\n    legacy AS (SELECT g.*,ROW_NUMBER() OVER(PARTITION BY user_id ORDER BY julianday(real_time_started_iso) DESC,imported_at DESC) rn\n        FROM gameplay_sessions g WHERE EXISTS(SELECT 1 FROM s WHERE s.user_id=g.user_id AND s.session_id=g.session_id)),\n    days AS (SELECT user_id,COUNT(*) play_days FROM\n        (SELECT DISTINCT user_id,archive_id,game_day FROM e0 WHERE NULLIF(archive_id,'') IS NOT NULL AND game_day IS NOT NULL) GROUP BY user_id),\n    p AS (SELECT ids.user_id,COALESCE(NULLIF(json_extract(st.payload_json,'$.nickname'),''),NULLIF(u.nickname,''),NULLIF(u.player_name,''),ids.user_id) nickname,\n        (SELECT SUM(duration_sec) FROM s WHERE s.user_id=ids.user_id) play_seconds,\n        (SELECT first_entry_at FROM analytics_first_entry f WHERE f.user_id=ids.user_id AND f.cohort_scope=CASE WHEN '${test_data}' IN ('include','only') OR ('${test_data}'='auto' AND ('editor' IN (${client_platform:sqlstring}) OR '__all__' IN (${client_platform:sqlstring}))) THEN 'all' ELSE 'production' END) first_login,\n        (SELECT MAX(started_at) FROM s WHERE s.user_id=ids.user_id) latest_login,\n        d.play_days,COALESCE(json_extract(st.payload_json,'$.money'),l.money_end,l.money_start) money,\n        COALESCE(json_extract(st.payload_json,'$.island_level'),l.island_level_max) island_level,\n        json_array_length(json_extract(st.payload_json,'$.cats')) cat_count,\n        st.payload_json state,st.occurred_at state_at,\n        json_extract(st.payload_json,'$.cat_house_count') house_count,\n        json_extract(st.payload_json,'$.cat_house_capacity') house_capacity,\n        json_extract(st.payload_json,'$.cat_house_occupied') house_occupied\n        FROM ids LEFT JOIN user_sessions u USING(user_id) LEFT JOIN states st ON st.user_id=ids.user_id AND st.rn=1\n        LEFT JOIN legacy l ON l.user_id=ids.user_id AND l.rn=1 LEFT JOIN days d USING(user_id)),\n    e AS (SELECT * FROM e0 WHERE ('${behavior_period}'='all' OR julianday(occurred_at) >= julianday(${__from}/1000,'unixepoch') AND julianday(occurred_at) < julianday(${__to}/1000,'unixepoch'))\n        OR (occurred_at IS NULL AND '${behavior_period}'='all')),\n    players AS (SELECT * FROM p WHERE 1),\n    events AS (SELECT * FROM e WHERE 1) SELECT COALESCE(NULLIF(json_extract(payload_json,'$.seed_name'),''),'未采集名称') 名称,COUNT(*) 次数,COUNT(DISTINCT user_id) 参与玩家数 FROM events WHERE event_type='farming_plant' AND actor_is_player=1 AND 1 GROUP BY COALESCE(json_extract(payload_json,'$.seed_id'),json_extract(payload_json,'$.seed_name')) ORDER BY 次数 DESC,名称 LIMIT 5"
        }
      ],
      "fieldConfig": {
        "defaults": {
          "unit": "short",
          "custom": {
            "filterable": true
          },
          "noValue": "未采集"
        },
        "overrides": [
          {
            "matcher": {
              "id": "byName",
              "options": "user_id"
            },
            "properties": [
              {
                "id": "links",
                "value": [
                  {
                    "title": "查看玩家旅程",
                    "url": "/d/gameplay-player-detail?${__url_time_range}&${client_platform:queryparam}&${release_version:queryparam}&${test_data:queryparam}&var-user_id=${__data.fields.user_id}&var-user_id=${__value.raw}"
                  }
                ]
              }
            ]
          },
          {
            "matcher": {
              "id": "byName",
              "options": "session_id"
            },
            "properties": [
              {
                "id": "links",
                "value": [
                  {
                    "title": "查看玩家旅程",
                    "url": "/d/gameplay-player-detail?${__url_time_range}&${client_platform:queryparam}&${release_version:queryparam}&${test_data:queryparam}&var-user_id=${__data.fields.user_id}&var-session_id=${__value.raw}"
                  }
                ]
              }
            ]
          },
          {
            "matcher": {
              "id": "byName",
              "options": "llm_request_id"
            },
            "properties": [
              {
                "id": "links",
                "value": [
                  {
                    "title": "查看玩家旅程",
                    "url": "/d/gameplay-player-detail?${__url_time_range}&${client_platform:queryparam}&${release_version:queryparam}&${test_data:queryparam}&var-user_id=${__data.fields.user_id}&var-llm_request_id=${__value.raw}"
                  }
                ]
              }
            ]
          }
        ]
      },
      "options": {
        "showHeader": true,
        "cellHeight": "sm"
      },
      "gridPos": {
        "x": 0,
        "y": 55,
        "w": 12,
        "h": 9
      }
    },
    {
      "id": 127,
      "title": "做菜 Top 5",
      "type": "table",
      "datasource": {
        "type": "frser-sqlite-datasource",
        "uid": "${DS_SQLITE}"
      },
      "description": "累计按玩家去重；当前状态取最新快照。缺失显示未采集，数值零保留。行为可切换期间，日期为北京时间。",
      "targets": [
        {
          "refId": "A",
          "queryType": "table",
          "queryText": "WITH e0 AS NOT MATERIALIZED (SELECT * FROM analytics_events WHERE ('__all__' IN (${client_platform:sqlstring}) OR client_platform IN (${client_platform:sqlstring})) AND ('__all__' IN (${release_version:sqlstring}) OR COALESCE(NULLIF(release_version,''),'__empty__') IN (${release_version:sqlstring})) AND ('${test_data}'='include' OR ('${test_data}'='auto' AND (client_platform='editor' OR (is_development_build=0 AND is_developer=0))) OR ('${test_data}'='exclude' AND is_development_build=0 AND is_developer=0 AND client_platform!='editor') OR ('${test_data}'='only' AND (is_development_build=1 OR is_developer=1 OR client_platform='editor')))),\n    s AS MATERIALIZED (SELECT * FROM analytics_sessions WHERE ('__all__' IN (${client_platform:sqlstring}) OR client_platform IN (${client_platform:sqlstring})) AND ('__all__' IN (${release_version:sqlstring}) OR COALESCE(NULLIF(release_version,''),'__empty__') IN (${release_version:sqlstring})) AND ('${test_data}'='include' OR ('${test_data}'='auto' AND (client_platform='editor' OR (is_development_build=0 AND is_developer=0))) OR ('${test_data}'='exclude' AND is_development_build=0 AND is_developer=0 AND client_platform!='editor') OR ('${test_data}'='only' AND (is_development_build=1 OR is_developer=1 OR client_platform='editor')))),\n    ids AS (SELECT user_id FROM s UNION SELECT user_id FROM e0),\n    states AS (SELECT *,ROW_NUMBER() OVER(PARTITION BY user_id ORDER BY julianday(occurred_at) DESC,sequence DESC,event_id DESC) rn\n        FROM e0 WHERE event_type='player_state_snapshot'),\n    legacy AS (SELECT g.*,ROW_NUMBER() OVER(PARTITION BY user_id ORDER BY julianday(real_time_started_iso) DESC,imported_at DESC) rn\n        FROM gameplay_sessions g WHERE EXISTS(SELECT 1 FROM s WHERE s.user_id=g.user_id AND s.session_id=g.session_id)),\n    days AS (SELECT user_id,COUNT(*) play_days FROM\n        (SELECT DISTINCT user_id,archive_id,game_day FROM e0 WHERE NULLIF(archive_id,'') IS NOT NULL AND game_day IS NOT NULL) GROUP BY user_id),\n    p AS (SELECT ids.user_id,COALESCE(NULLIF(json_extract(st.payload_json,'$.nickname'),''),NULLIF(u.nickname,''),NULLIF(u.player_name,''),ids.user_id) nickname,\n        (SELECT SUM(duration_sec) FROM s WHERE s.user_id=ids.user_id) play_seconds,\n        (SELECT first_entry_at FROM analytics_first_entry f WHERE f.user_id=ids.user_id AND f.cohort_scope=CASE WHEN '${test_data}' IN ('include','only') OR ('${test_data}'='auto' AND ('editor' IN (${client_platform:sqlstring}) OR '__all__' IN (${client_platform:sqlstring}))) THEN 'all' ELSE 'production' END) first_login,\n        (SELECT MAX(started_at) FROM s WHERE s.user_id=ids.user_id) latest_login,\n        d.play_days,COALESCE(json_extract(st.payload_json,'$.money'),l.money_end,l.money_start) money,\n        COALESCE(json_extract(st.payload_json,'$.island_level'),l.island_level_max) island_level,\n        json_array_length(json_extract(st.payload_json,'$.cats')) cat_count,\n        st.payload_json state,st.occurred_at state_at,\n        json_extract(st.payload_json,'$.cat_house_count') house_count,\n        json_extract(st.payload_json,'$.cat_house_capacity') house_capacity,\n        json_extract(st.payload_json,'$.cat_house_occupied') house_occupied\n        FROM ids LEFT JOIN user_sessions u USING(user_id) LEFT JOIN states st ON st.user_id=ids.user_id AND st.rn=1\n        LEFT JOIN legacy l ON l.user_id=ids.user_id AND l.rn=1 LEFT JOIN days d USING(user_id)),\n    e AS (SELECT * FROM e0 WHERE ('${behavior_period}'='all' OR julianday(occurred_at) >= julianday(${__from}/1000,'unixepoch') AND julianday(occurred_at) < julianday(${__to}/1000,'unixepoch'))\n        OR (occurred_at IS NULL AND '${behavior_period}'='all')),\n    players AS (SELECT * FROM p WHERE 1),\n    events AS (SELECT * FROM e WHERE 1) SELECT COALESCE(NULLIF(COALESCE(json_extract(payload_json,'$.dish_name'),json_extract(payload_json,'$.recipe_name')),''),'未采集名称') 名称,COUNT(*) 次数,COUNT(DISTINCT user_id) 参与玩家数 FROM events WHERE event_type IN ('cooking_complete','cooking_completed') AND actor_is_player=1 AND 1 GROUP BY COALESCE(json_extract(payload_json,'$.recipe_id'),COALESCE(json_extract(payload_json,'$.dish_name'),json_extract(payload_json,'$.recipe_name'))) ORDER BY 次数 DESC,名称 LIMIT 5",
          "rawQueryText": "WITH e0 AS NOT MATERIALIZED (SELECT * FROM analytics_events WHERE ('__all__' IN (${client_platform:sqlstring}) OR client_platform IN (${client_platform:sqlstring})) AND ('__all__' IN (${release_version:sqlstring}) OR COALESCE(NULLIF(release_version,''),'__empty__') IN (${release_version:sqlstring})) AND ('${test_data}'='include' OR ('${test_data}'='auto' AND (client_platform='editor' OR (is_development_build=0 AND is_developer=0))) OR ('${test_data}'='exclude' AND is_development_build=0 AND is_developer=0 AND client_platform!='editor') OR ('${test_data}'='only' AND (is_development_build=1 OR is_developer=1 OR client_platform='editor')))),\n    s AS MATERIALIZED (SELECT * FROM analytics_sessions WHERE ('__all__' IN (${client_platform:sqlstring}) OR client_platform IN (${client_platform:sqlstring})) AND ('__all__' IN (${release_version:sqlstring}) OR COALESCE(NULLIF(release_version,''),'__empty__') IN (${release_version:sqlstring})) AND ('${test_data}'='include' OR ('${test_data}'='auto' AND (client_platform='editor' OR (is_development_build=0 AND is_developer=0))) OR ('${test_data}'='exclude' AND is_development_build=0 AND is_developer=0 AND client_platform!='editor') OR ('${test_data}'='only' AND (is_development_build=1 OR is_developer=1 OR client_platform='editor')))),\n    ids AS (SELECT user_id FROM s UNION SELECT user_id FROM e0),\n    states AS (SELECT *,ROW_NUMBER() OVER(PARTITION BY user_id ORDER BY julianday(occurred_at) DESC,sequence DESC,event_id DESC) rn\n        FROM e0 WHERE event_type='player_state_snapshot'),\n    legacy AS (SELECT g.*,ROW_NUMBER() OVER(PARTITION BY user_id ORDER BY julianday(real_time_started_iso) DESC,imported_at DESC) rn\n        FROM gameplay_sessions g WHERE EXISTS(SELECT 1 FROM s WHERE s.user_id=g.user_id AND s.session_id=g.session_id)),\n    days AS (SELECT user_id,COUNT(*) play_days FROM\n        (SELECT DISTINCT user_id,archive_id,game_day FROM e0 WHERE NULLIF(archive_id,'') IS NOT NULL AND game_day IS NOT NULL) GROUP BY user_id),\n    p AS (SELECT ids.user_id,COALESCE(NULLIF(json_extract(st.payload_json,'$.nickname'),''),NULLIF(u.nickname,''),NULLIF(u.player_name,''),ids.user_id) nickname,\n        (SELECT SUM(duration_sec) FROM s WHERE s.user_id=ids.user_id) play_seconds,\n        (SELECT first_entry_at FROM analytics_first_entry f WHERE f.user_id=ids.user_id AND f.cohort_scope=CASE WHEN '${test_data}' IN ('include','only') OR ('${test_data}'='auto' AND ('editor' IN (${client_platform:sqlstring}) OR '__all__' IN (${client_platform:sqlstring}))) THEN 'all' ELSE 'production' END) first_login,\n        (SELECT MAX(started_at) FROM s WHERE s.user_id=ids.user_id) latest_login,\n        d.play_days,COALESCE(json_extract(st.payload_json,'$.money'),l.money_end,l.money_start) money,\n        COALESCE(json_extract(st.payload_json,'$.island_level'),l.island_level_max) island_level,\n        json_array_length(json_extract(st.payload_json,'$.cats')) cat_count,\n        st.payload_json state,st.occurred_at state_at,\n        json_extract(st.payload_json,'$.cat_house_count') house_count,\n        json_extract(st.payload_json,'$.cat_house_capacity') house_capacity,\n        json_extract(st.payload_json,'$.cat_house_occupied') house_occupied\n        FROM ids LEFT JOIN user_sessions u USING(user_id) LEFT JOIN states st ON st.user_id=ids.user_id AND st.rn=1\n        LEFT JOIN legacy l ON l.user_id=ids.user_id AND l.rn=1 LEFT JOIN days d USING(user_id)),\n    e AS (SELECT * FROM e0 WHERE ('${behavior_period}'='all' OR julianday(occurred_at) >= julianday(${__from}/1000,'unixepoch') AND julianday(occurred_at) < julianday(${__to}/1000,'unixepoch'))\n        OR (occurred_at IS NULL AND '${behavior_period}'='all')),\n    players AS (SELECT * FROM p WHERE 1),\n    events AS (SELECT * FROM e WHERE 1) SELECT COALESCE(NULLIF(COALESCE(json_extract(payload_json,'$.dish_name'),json_extract(payload_json,'$.recipe_name')),''),'未采集名称') 名称,COUNT(*) 次数,COUNT(DISTINCT user_id) 参与玩家数 FROM events WHERE event_type IN ('cooking_complete','cooking_completed') AND actor_is_player=1 AND 1 GROUP BY COALESCE(json_extract(payload_json,'$.recipe_id'),COALESCE(json_extract(payload_json,'$.dish_name'),json_extract(payload_json,'$.recipe_name'))) ORDER BY 次数 DESC,名称 LIMIT 5"
        }
      ],
      "fieldConfig": {
        "defaults": {
          "unit": "short",
          "custom": {
            "filterable": true
          },
          "noValue": "未采集"
        },
        "overrides": [
          {
            "matcher": {
              "id": "byName",
              "options": "user_id"
            },
            "properties": [
              {
                "id": "links",
                "value": [
                  {
                    "title": "查看玩家旅程",
                    "url": "/d/gameplay-player-detail?${__url_time_range}&${client_platform:queryparam}&${release_version:queryparam}&${test_data:queryparam}&var-user_id=${__data.fields.user_id}&var-user_id=${__value.raw}"
                  }
                ]
              }
            ]
          },
          {
            "matcher": {
              "id": "byName",
              "options": "session_id"
            },
            "properties": [
              {
                "id": "links",
                "value": [
                  {
                    "title": "查看玩家旅程",
                    "url": "/d/gameplay-player-detail?${__url_time_range}&${client_platform:queryparam}&${release_version:queryparam}&${test_data:queryparam}&var-user_id=${__data.fields.user_id}&var-session_id=${__value.raw}"
                  }
                ]
              }
            ]
          },
          {
            "matcher": {
              "id": "byName",
              "options": "llm_request_id"
            },
            "properties": [
              {
                "id": "links",
                "value": [
                  {
                    "title": "查看玩家旅程",
                    "url": "/d/gameplay-player-detail?${__url_time_range}&${client_platform:queryparam}&${release_version:queryparam}&${test_data:queryparam}&var-user_id=${__data.fields.user_id}&var-llm_request_id=${__value.raw}"
                  }
                ]
              }
            ]
          }
        ]
      },
      "options": {
        "showHeader": true,
        "cellHeight": "sm"
      },
      "gridPos": {
        "x": 12,
        "y": 55,
        "w": 12,
        "h": 9
      }
    },
    {
      "id": 128,
      "title": "建造建筑 Top 5",
      "type": "table",
      "datasource": {
        "type": "frser-sqlite-datasource",
        "uid": "${DS_SQLITE}"
      },
      "description": "累计按玩家去重；当前状态取最新快照。缺失显示未采集，数值零保留。行为可切换期间，日期为北京时间。",
      "targets": [
        {
          "refId": "A",
          "queryType": "table",
          "queryText": "WITH e0 AS NOT MATERIALIZED (SELECT * FROM analytics_events WHERE ('__all__' IN (${client_platform:sqlstring}) OR client_platform IN (${client_platform:sqlstring})) AND ('__all__' IN (${release_version:sqlstring}) OR COALESCE(NULLIF(release_version,''),'__empty__') IN (${release_version:sqlstring})) AND ('${test_data}'='include' OR ('${test_data}'='auto' AND (client_platform='editor' OR (is_development_build=0 AND is_developer=0))) OR ('${test_data}'='exclude' AND is_development_build=0 AND is_developer=0 AND client_platform!='editor') OR ('${test_data}'='only' AND (is_development_build=1 OR is_developer=1 OR client_platform='editor')))),\n    s AS MATERIALIZED (SELECT * FROM analytics_sessions WHERE ('__all__' IN (${client_platform:sqlstring}) OR client_platform IN (${client_platform:sqlstring})) AND ('__all__' IN (${release_version:sqlstring}) OR COALESCE(NULLIF(release_version,''),'__empty__') IN (${release_version:sqlstring})) AND ('${test_data}'='include' OR ('${test_data}'='auto' AND (client_platform='editor' OR (is_development_build=0 AND is_developer=0))) OR ('${test_data}'='exclude' AND is_development_build=0 AND is_developer=0 AND client_platform!='editor') OR ('${test_data}'='only' AND (is_development_build=1 OR is_developer=1 OR client_platform='editor')))),\n    ids AS (SELECT user_id FROM s UNION SELECT user_id FROM e0),\n    states AS (SELECT *,ROW_NUMBER() OVER(PARTITION BY user_id ORDER BY julianday(occurred_at) DESC,sequence DESC,event_id DESC) rn\n        FROM e0 WHERE event_type='player_state_snapshot'),\n    legacy AS (SELECT g.*,ROW_NUMBER() OVER(PARTITION BY user_id ORDER BY julianday(real_time_started_iso) DESC,imported_at DESC) rn\n        FROM gameplay_sessions g WHERE EXISTS(SELECT 1 FROM s WHERE s.user_id=g.user_id AND s.session_id=g.session_id)),\n    days AS (SELECT user_id,COUNT(*) play_days FROM\n        (SELECT DISTINCT user_id,archive_id,game_day FROM e0 WHERE NULLIF(archive_id,'') IS NOT NULL AND game_day IS NOT NULL) GROUP BY user_id),\n    p AS (SELECT ids.user_id,COALESCE(NULLIF(json_extract(st.payload_json,'$.nickname'),''),NULLIF(u.nickname,''),NULLIF(u.player_name,''),ids.user_id) nickname,\n        (SELECT SUM(duration_sec) FROM s WHERE s.user_id=ids.user_id) play_seconds,\n        (SELECT first_entry_at FROM analytics_first_entry f WHERE f.user_id=ids.user_id AND f.cohort_scope=CASE WHEN '${test_data}' IN ('include','only') OR ('${test_data}'='auto' AND ('editor' IN (${client_platform:sqlstring}) OR '__all__' IN (${client_platform:sqlstring}))) THEN 'all' ELSE 'production' END) first_login,\n        (SELECT MAX(started_at) FROM s WHERE s.user_id=ids.user_id) latest_login,\n        d.play_days,COALESCE(json_extract(st.payload_json,'$.money'),l.money_end,l.money_start) money,\n        COALESCE(json_extract(st.payload_json,'$.island_level'),l.island_level_max) island_level,\n        json_array_length(json_extract(st.payload_json,'$.cats')) cat_count,\n        st.payload_json state,st.occurred_at state_at,\n        json_extract(st.payload_json,'$.cat_house_count') house_count,\n        json_extract(st.payload_json,'$.cat_house_capacity') house_capacity,\n        json_extract(st.payload_json,'$.cat_house_occupied') house_occupied\n        FROM ids LEFT JOIN user_sessions u USING(user_id) LEFT JOIN states st ON st.user_id=ids.user_id AND st.rn=1\n        LEFT JOIN legacy l ON l.user_id=ids.user_id AND l.rn=1 LEFT JOIN days d USING(user_id)),\n    e AS (SELECT * FROM e0 WHERE ('${behavior_period}'='all' OR julianday(occurred_at) >= julianday(${__from}/1000,'unixepoch') AND julianday(occurred_at) < julianday(${__to}/1000,'unixepoch'))\n        OR (occurred_at IS NULL AND '${behavior_period}'='all')),\n    players AS (SELECT * FROM p WHERE 1),\n    events AS (SELECT * FROM e WHERE 1) SELECT COALESCE(NULLIF(json_extract(payload_json,'$.building_name'),''),'未采集名称') 名称,COUNT(*) 次数,COUNT(DISTINCT user_id) 参与玩家数 FROM events WHERE event_type='building_placed' AND actor_is_player=1 AND 1 GROUP BY COALESCE(json_extract(payload_json,'$.building_id'),json_extract(payload_json,'$.building_name')) ORDER BY 次数 DESC,名称 LIMIT 5",
          "rawQueryText": "WITH e0 AS NOT MATERIALIZED (SELECT * FROM analytics_events WHERE ('__all__' IN (${client_platform:sqlstring}) OR client_platform IN (${client_platform:sqlstring})) AND ('__all__' IN (${release_version:sqlstring}) OR COALESCE(NULLIF(release_version,''),'__empty__') IN (${release_version:sqlstring})) AND ('${test_data}'='include' OR ('${test_data}'='auto' AND (client_platform='editor' OR (is_development_build=0 AND is_developer=0))) OR ('${test_data}'='exclude' AND is_development_build=0 AND is_developer=0 AND client_platform!='editor') OR ('${test_data}'='only' AND (is_development_build=1 OR is_developer=1 OR client_platform='editor')))),\n    s AS MATERIALIZED (SELECT * FROM analytics_sessions WHERE ('__all__' IN (${client_platform:sqlstring}) OR client_platform IN (${client_platform:sqlstring})) AND ('__all__' IN (${release_version:sqlstring}) OR COALESCE(NULLIF(release_version,''),'__empty__') IN (${release_version:sqlstring})) AND ('${test_data}'='include' OR ('${test_data}'='auto' AND (client_platform='editor' OR (is_development_build=0 AND is_developer=0))) OR ('${test_data}'='exclude' AND is_development_build=0 AND is_developer=0 AND client_platform!='editor') OR ('${test_data}'='only' AND (is_development_build=1 OR is_developer=1 OR client_platform='editor')))),\n    ids AS (SELECT user_id FROM s UNION SELECT user_id FROM e0),\n    states AS (SELECT *,ROW_NUMBER() OVER(PARTITION BY user_id ORDER BY julianday(occurred_at) DESC,sequence DESC,event_id DESC) rn\n        FROM e0 WHERE event_type='player_state_snapshot'),\n    legacy AS (SELECT g.*,ROW_NUMBER() OVER(PARTITION BY user_id ORDER BY julianday(real_time_started_iso) DESC,imported_at DESC) rn\n        FROM gameplay_sessions g WHERE EXISTS(SELECT 1 FROM s WHERE s.user_id=g.user_id AND s.session_id=g.session_id)),\n    days AS (SELECT user_id,COUNT(*) play_days FROM\n        (SELECT DISTINCT user_id,archive_id,game_day FROM e0 WHERE NULLIF(archive_id,'') IS NOT NULL AND game_day IS NOT NULL) GROUP BY user_id),\n    p AS (SELECT ids.user_id,COALESCE(NULLIF(json_extract(st.payload_json,'$.nickname'),''),NULLIF(u.nickname,''),NULLIF(u.player_name,''),ids.user_id) nickname,\n        (SELECT SUM(duration_sec) FROM s WHERE s.user_id=ids.user_id) play_seconds,\n        (SELECT first_entry_at FROM analytics_first_entry f WHERE f.user_id=ids.user_id AND f.cohort_scope=CASE WHEN '${test_data}' IN ('include','only') OR ('${test_data}'='auto' AND ('editor' IN (${client_platform:sqlstring}) OR '__all__' IN (${client_platform:sqlstring}))) THEN 'all' ELSE 'production' END) first_login,\n        (SELECT MAX(started_at) FROM s WHERE s.user_id=ids.user_id) latest_login,\n        d.play_days,COALESCE(json_extract(st.payload_json,'$.money'),l.money_end,l.money_start) money,\n        COALESCE(json_extract(st.payload_json,'$.island_level'),l.island_level_max) island_level,\n        json_array_length(json_extract(st.payload_json,'$.cats')) cat_count,\n        st.payload_json state,st.occurred_at state_at,\n        json_extract(st.payload_json,'$.cat_house_count') house_count,\n        json_extract(st.payload_json,'$.cat_house_capacity') house_capacity,\n        json_extract(st.payload_json,'$.cat_house_occupied') house_occupied\n        FROM ids LEFT JOIN user_sessions u USING(user_id) LEFT JOIN states st ON st.user_id=ids.user_id AND st.rn=1\n        LEFT JOIN legacy l ON l.user_id=ids.user_id AND l.rn=1 LEFT JOIN days d USING(user_id)),\n    e AS (SELECT * FROM e0 WHERE ('${behavior_period}'='all' OR julianday(occurred_at) >= julianday(${__from}/1000,'unixepoch') AND julianday(occurred_at) < julianday(${__to}/1000,'unixepoch'))\n        OR (occurred_at IS NULL AND '${behavior_period}'='all')),\n    players AS (SELECT * FROM p WHERE 1),\n    events AS (SELECT * FROM e WHERE 1) SELECT COALESCE(NULLIF(json_extract(payload_json,'$.building_name'),''),'未采集名称') 名称,COUNT(*) 次数,COUNT(DISTINCT user_id) 参与玩家数 FROM events WHERE event_type='building_placed' AND actor_is_player=1 AND 1 GROUP BY COALESCE(json_extract(payload_json,'$.building_id'),json_extract(payload_json,'$.building_name')) ORDER BY 次数 DESC,名称 LIMIT 5"
        }
      ],
      "fieldConfig": {
        "defaults": {
          "unit": "short",
          "custom": {
            "filterable": true
          },
          "noValue": "未采集"
        },
        "overrides": [
          {
            "matcher": {
              "id": "byName",
              "options": "user_id"
            },
            "properties": [
              {
                "id": "links",
                "value": [
                  {
                    "title": "查看玩家旅程",
                    "url": "/d/gameplay-player-detail?${__url_time_range}&${client_platform:queryparam}&${release_version:queryparam}&${test_data:queryparam}&var-user_id=${__data.fields.user_id}&var-user_id=${__value.raw}"
                  }
                ]
              }
            ]
          },
          {
            "matcher": {
              "id": "byName",
              "options": "session_id"
            },
            "properties": [
              {
                "id": "links",
                "value": [
                  {
                    "title": "查看玩家旅程",
                    "url": "/d/gameplay-player-detail?${__url_time_range}&${client_platform:queryparam}&${release_version:queryparam}&${test_data:queryparam}&var-user_id=${__data.fields.user_id}&var-session_id=${__value.raw}"
                  }
                ]
              }
            ]
          },
          {
            "matcher": {
              "id": "byName",
              "options": "llm_request_id"
            },
            "properties": [
              {
                "id": "links",
                "value": [
                  {
                    "title": "查看玩家旅程",
                    "url": "/d/gameplay-player-detail?${__url_time_range}&${client_platform:queryparam}&${release_version:queryparam}&${test_data:queryparam}&var-user_id=${__data.fields.user_id}&var-llm_request_id=${__value.raw}"
                  }
                ]
              }
            ]
          }
        ]
      },
      "options": {
        "showHeader": true,
        "cellHeight": "sm"
      },
      "gridPos": {
        "x": 0,
        "y": 64,
        "w": 12,
        "h": 9
      }
    },
    {
      "id": 140,
      "title": "玩家行为 Top 5",
      "type": "table",
      "datasource": {
        "type": "frser-sqlite-datasource",
        "uid": "${DS_SQLITE}"
      },
      "description": "累计按玩家去重；当前状态取最新快照。缺失显示未采集，数值零保留。行为可切换期间，日期为北京时间。",
      "targets": [
        {
          "refId": "A",
          "queryType": "table",
          "queryText": "WITH e0 AS NOT MATERIALIZED (SELECT * FROM analytics_events WHERE ('__all__' IN (${client_platform:sqlstring}) OR client_platform IN (${client_platform:sqlstring})) AND ('__all__' IN (${release_version:sqlstring}) OR COALESCE(NULLIF(release_version,''),'__empty__') IN (${release_version:sqlstring})) AND ('${test_data}'='include' OR ('${test_data}'='auto' AND (client_platform='editor' OR (is_development_build=0 AND is_developer=0))) OR ('${test_data}'='exclude' AND is_development_build=0 AND is_developer=0 AND client_platform!='editor') OR ('${test_data}'='only' AND (is_development_build=1 OR is_developer=1 OR client_platform='editor')))),\n    s AS MATERIALIZED (SELECT * FROM analytics_sessions WHERE ('__all__' IN (${client_platform:sqlstring}) OR client_platform IN (${client_platform:sqlstring})) AND ('__all__' IN (${release_version:sqlstring}) OR COALESCE(NULLIF(release_version,''),'__empty__') IN (${release_version:sqlstring})) AND ('${test_data}'='include' OR ('${test_data}'='auto' AND (client_platform='editor' OR (is_development_build=0 AND is_developer=0))) OR ('${test_data}'='exclude' AND is_development_build=0 AND is_developer=0 AND client_platform!='editor') OR ('${test_data}'='only' AND (is_development_build=1 OR is_developer=1 OR client_platform='editor')))),\n    ids AS (SELECT user_id FROM s UNION SELECT user_id FROM e0),\n    states AS (SELECT *,ROW_NUMBER() OVER(PARTITION BY user_id ORDER BY julianday(occurred_at) DESC,sequence DESC,event_id DESC) rn\n        FROM e0 WHERE event_type='player_state_snapshot'),\n    legacy AS (SELECT g.*,ROW_NUMBER() OVER(PARTITION BY user_id ORDER BY julianday(real_time_started_iso) DESC,imported_at DESC) rn\n        FROM gameplay_sessions g WHERE EXISTS(SELECT 1 FROM s WHERE s.user_id=g.user_id AND s.session_id=g.session_id)),\n    days AS (SELECT user_id,COUNT(*) play_days FROM\n        (SELECT DISTINCT user_id,archive_id,game_day FROM e0 WHERE NULLIF(archive_id,'') IS NOT NULL AND game_day IS NOT NULL) GROUP BY user_id),\n    p AS (SELECT ids.user_id,COALESCE(NULLIF(json_extract(st.payload_json,'$.nickname'),''),NULLIF(u.nickname,''),NULLIF(u.player_name,''),ids.user_id) nickname,\n        (SELECT SUM(duration_sec) FROM s WHERE s.user_id=ids.user_id) play_seconds,\n        (SELECT first_entry_at FROM analytics_first_entry f WHERE f.user_id=ids.user_id AND f.cohort_scope=CASE WHEN '${test_data}' IN ('include','only') OR ('${test_data}'='auto' AND ('editor' IN (${client_platform:sqlstring}) OR '__all__' IN (${client_platform:sqlstring}))) THEN 'all' ELSE 'production' END) first_login,\n        (SELECT MAX(started_at) FROM s WHERE s.user_id=ids.user_id) latest_login,\n        d.play_days,COALESCE(json_extract(st.payload_json,'$.money'),l.money_end,l.money_start) money,\n        COALESCE(json_extract(st.payload_json,'$.island_level'),l.island_level_max) island_level,\n        json_array_length(json_extract(st.payload_json,'$.cats')) cat_count,\n        st.payload_json state,st.occurred_at state_at,\n        json_extract(st.payload_json,'$.cat_house_count') house_count,\n        json_extract(st.payload_json,'$.cat_house_capacity') house_capacity,\n        json_extract(st.payload_json,'$.cat_house_occupied') house_occupied\n        FROM ids LEFT JOIN user_sessions u USING(user_id) LEFT JOIN states st ON st.user_id=ids.user_id AND st.rn=1\n        LEFT JOIN legacy l ON l.user_id=ids.user_id AND l.rn=1 LEFT JOIN days d USING(user_id)),\n    e AS (SELECT * FROM e0 WHERE ('${behavior_period}'='all' OR julianday(occurred_at) >= julianday(${__from}/1000,'unixepoch') AND julianday(occurred_at) < julianday(${__to}/1000,'unixepoch'))\n        OR (occurred_at IS NULL AND '${behavior_period}'='all')),\n    players AS (SELECT * FROM p WHERE 1),\n    events AS (SELECT * FROM e WHERE 1) SELECT event_type 行为,COUNT(*) 次数 FROM events WHERE actor_is_player=1 AND (behavior_stat=1 OR (behavior_stat IS NULL AND event_type IN ('farming_plant','farming_water','farming_till','farming_harvest','fishing_catch','cooking_completed','building_placed','shop_purchase','cat_conversation','player_sleep'))) GROUP BY 1 ORDER BY 次数 DESC,行为 LIMIT 5",
          "rawQueryText": "WITH e0 AS NOT MATERIALIZED (SELECT * FROM analytics_events WHERE ('__all__' IN (${client_platform:sqlstring}) OR client_platform IN (${client_platform:sqlstring})) AND ('__all__' IN (${release_version:sqlstring}) OR COALESCE(NULLIF(release_version,''),'__empty__') IN (${release_version:sqlstring})) AND ('${test_data}'='include' OR ('${test_data}'='auto' AND (client_platform='editor' OR (is_development_build=0 AND is_developer=0))) OR ('${test_data}'='exclude' AND is_development_build=0 AND is_developer=0 AND client_platform!='editor') OR ('${test_data}'='only' AND (is_development_build=1 OR is_developer=1 OR client_platform='editor')))),\n    s AS MATERIALIZED (SELECT * FROM analytics_sessions WHERE ('__all__' IN (${client_platform:sqlstring}) OR client_platform IN (${client_platform:sqlstring})) AND ('__all__' IN (${release_version:sqlstring}) OR COALESCE(NULLIF(release_version,''),'__empty__') IN (${release_version:sqlstring})) AND ('${test_data}'='include' OR ('${test_data}'='auto' AND (client_platform='editor' OR (is_development_build=0 AND is_developer=0))) OR ('${test_data}'='exclude' AND is_development_build=0 AND is_developer=0 AND client_platform!='editor') OR ('${test_data}'='only' AND (is_development_build=1 OR is_developer=1 OR client_platform='editor')))),\n    ids AS (SELECT user_id FROM s UNION SELECT user_id FROM e0),\n    states AS (SELECT *,ROW_NUMBER() OVER(PARTITION BY user_id ORDER BY julianday(occurred_at) DESC,sequence DESC,event_id DESC) rn\n        FROM e0 WHERE event_type='player_state_snapshot'),\n    legacy AS (SELECT g.*,ROW_NUMBER() OVER(PARTITION BY user_id ORDER BY julianday(real_time_started_iso) DESC,imported_at DESC) rn\n        FROM gameplay_sessions g WHERE EXISTS(SELECT 1 FROM s WHERE s.user_id=g.user_id AND s.session_id=g.session_id)),\n    days AS (SELECT user_id,COUNT(*) play_days FROM\n        (SELECT DISTINCT user_id,archive_id,game_day FROM e0 WHERE NULLIF(archive_id,'') IS NOT NULL AND game_day IS NOT NULL) GROUP BY user_id),\n    p AS (SELECT ids.user_id,COALESCE(NULLIF(json_extract(st.payload_json,'$.nickname'),''),NULLIF(u.nickname,''),NULLIF(u.player_name,''),ids.user_id) nickname,\n        (SELECT SUM(duration_sec) FROM s WHERE s.user_id=ids.user_id) play_seconds,\n        (SELECT first_entry_at FROM analytics_first_entry f WHERE f.user_id=ids.user_id AND f.cohort_scope=CASE WHEN '${test_data}' IN ('include','only') OR ('${test_data}'='auto' AND ('editor' IN (${client_platform:sqlstring}) OR '__all__' IN (${client_platform:sqlstring}))) THEN 'all' ELSE 'production' END) first_login,\n        (SELECT MAX(started_at) FROM s WHERE s.user_id=ids.user_id) latest_login,\n        d.play_days,COALESCE(json_extract(st.payload_json,'$.money'),l.money_end,l.money_start) money,\n        COALESCE(json_extract(st.payload_json,'$.island_level'),l.island_level_max) island_level,\n        json_array_length(json_extract(st.payload_json,'$.cats')) cat_count,\n        st.payload_json state,st.occurred_at state_at,\n        json_extract(st.payload_json,'$.cat_house_count') house_count,\n        json_extract(st.payload_json,'$.cat_house_capacity') house_capacity,\n        json_extract(st.payload_json,'$.cat_house_occupied') house_occupied\n        FROM ids LEFT JOIN user_sessions u USING(user_id) LEFT JOIN states st ON st.user_id=ids.user_id AND st.rn=1\n        LEFT JOIN legacy l ON l.user_id=ids.user_id AND l.rn=1 LEFT JOIN days d USING(user_id)),\n    e AS (SELECT * FROM e0 WHERE ('${behavior_period}'='all' OR julianday(occurred_at) >= julianday(${__from}/1000,'unixepoch') AND julianday(occurred_at) < julianday(${__to}/1000,'unixepoch'))\n        OR (occurred_at IS NULL AND '${behavior_period}'='all')),\n    players AS (SELECT * FROM p WHERE 1),\n    events AS (SELECT * FROM e WHERE 1) SELECT event_type 行为,COUNT(*) 次数 FROM events WHERE actor_is_player=1 AND (behavior_stat=1 OR (behavior_stat IS NULL AND event_type IN ('farming_plant','farming_water','farming_till','farming_harvest','fishing_catch','cooking_completed','building_placed','shop_purchase','cat_conversation','player_sleep'))) GROUP BY 1 ORDER BY 次数 DESC,行为 LIMIT 5"
        }
      ],
      "fieldConfig": {
        "defaults": {
          "unit": "short",
          "custom": {
            "filterable": true
          },
          "noValue": "未采集"
        },
        "overrides": [
          {
            "matcher": {
              "id": "byName",
              "options": "user_id"
            },
            "properties": [
              {
                "id": "links",
                "value": [
                  {
                    "title": "查看玩家旅程",
                    "url": "/d/gameplay-player-detail?${__url_time_range}&${client_platform:queryparam}&${release_version:queryparam}&${test_data:queryparam}&var-user_id=${__data.fields.user_id}&var-user_id=${__value.raw}"
                  }
                ]
              }
            ]
          },
          {
            "matcher": {
              "id": "byName",
              "options": "session_id"
            },
            "properties": [
              {
                "id": "links",
                "value": [
                  {
                    "title": "查看玩家旅程",
                    "url": "/d/gameplay-player-detail?${__url_time_range}&${client_platform:queryparam}&${release_version:queryparam}&${test_data:queryparam}&var-user_id=${__data.fields.user_id}&var-session_id=${__value.raw}"
                  }
                ]
              }
            ]
          },
          {
            "matcher": {
              "id": "byName",
              "options": "llm_request_id"
            },
            "properties": [
              {
                "id": "links",
                "value": [
                  {
                    "title": "查看玩家旅程",
                    "url": "/d/gameplay-player-detail?${__url_time_range}&${client_platform:queryparam}&${release_version:queryparam}&${test_data:queryparam}&var-user_id=${__data.fields.user_id}&var-llm_request_id=${__value.raw}"
                  }
                ]
              }
            ]
          }
        ]
      },
      "options": {
        "showHeader": true,
        "cellHeight": "sm"
      },
      "gridPos": {
        "x": 12,
        "y": 64,
        "w": 12,
        "h": 9
      }
    },
    {
      "id": 141,
      "title": "体力消耗 Top 5",
      "type": "table",
      "datasource": {
        "type": "frser-sqlite-datasource",
        "uid": "${DS_SQLITE}"
      },
      "description": "累计按玩家去重；当前状态取最新快照。缺失显示未采集，数值零保留。行为可切换期间，日期为北京时间。",
      "targets": [
        {
          "refId": "A",
          "queryType": "table",
          "queryText": "WITH e0 AS NOT MATERIALIZED (SELECT * FROM analytics_events WHERE ('__all__' IN (${client_platform:sqlstring}) OR client_platform IN (${client_platform:sqlstring})) AND ('__all__' IN (${release_version:sqlstring}) OR COALESCE(NULLIF(release_version,''),'__empty__') IN (${release_version:sqlstring})) AND ('${test_data}'='include' OR ('${test_data}'='auto' AND (client_platform='editor' OR (is_development_build=0 AND is_developer=0))) OR ('${test_data}'='exclude' AND is_development_build=0 AND is_developer=0 AND client_platform!='editor') OR ('${test_data}'='only' AND (is_development_build=1 OR is_developer=1 OR client_platform='editor')))),\n    s AS MATERIALIZED (SELECT * FROM analytics_sessions WHERE ('__all__' IN (${client_platform:sqlstring}) OR client_platform IN (${client_platform:sqlstring})) AND ('__all__' IN (${release_version:sqlstring}) OR COALESCE(NULLIF(release_version,''),'__empty__') IN (${release_version:sqlstring})) AND ('${test_data}'='include' OR ('${test_data}'='auto' AND (client_platform='editor' OR (is_development_build=0 AND is_developer=0))) OR ('${test_data}'='exclude' AND is_development_build=0 AND is_developer=0 AND client_platform!='editor') OR ('${test_data}'='only' AND (is_development_build=1 OR is_developer=1 OR client_platform='editor')))),\n    ids AS (SELECT user_id FROM s UNION SELECT user_id FROM e0),\n    states AS (SELECT *,ROW_NUMBER() OVER(PARTITION BY user_id ORDER BY julianday(occurred_at) DESC,sequence DESC,event_id DESC) rn\n        FROM e0 WHERE event_type='player_state_snapshot'),\n    legacy AS (SELECT g.*,ROW_NUMBER() OVER(PARTITION BY user_id ORDER BY julianday(real_time_started_iso) DESC,imported_at DESC) rn\n        FROM gameplay_sessions g WHERE EXISTS(SELECT 1 FROM s WHERE s.user_id=g.user_id AND s.session_id=g.session_id)),\n    days AS (SELECT user_id,COUNT(*) play_days FROM\n        (SELECT DISTINCT user_id,archive_id,game_day FROM e0 WHERE NULLIF(archive_id,'') IS NOT NULL AND game_day IS NOT NULL) GROUP BY user_id),\n    p AS (SELECT ids.user_id,COALESCE(NULLIF(json_extract(st.payload_json,'$.nickname'),''),NULLIF(u.nickname,''),NULLIF(u.player_name,''),ids.user_id) nickname,\n        (SELECT SUM(duration_sec) FROM s WHERE s.user_id=ids.user_id) play_seconds,\n        (SELECT first_entry_at FROM analytics_first_entry f WHERE f.user_id=ids.user_id AND f.cohort_scope=CASE WHEN '${test_data}' IN ('include','only') OR ('${test_data}'='auto' AND ('editor' IN (${client_platform:sqlstring}) OR '__all__' IN (${client_platform:sqlstring}))) THEN 'all' ELSE 'production' END) first_login,\n        (SELECT MAX(started_at) FROM s WHERE s.user_id=ids.user_id) latest_login,\n        d.play_days,COALESCE(json_extract(st.payload_json,'$.money'),l.money_end,l.money_start) money,\n        COALESCE(json_extract(st.payload_json,'$.island_level'),l.island_level_max) island_level,\n        json_array_length(json_extract(st.payload_json,'$.cats')) cat_count,\n        st.payload_json state,st.occurred_at state_at,\n        json_extract(st.payload_json,'$.cat_house_count') house_count,\n        json_extract(st.payload_json,'$.cat_house_capacity') house_capacity,\n        json_extract(st.payload_json,'$.cat_house_occupied') house_occupied\n        FROM ids LEFT JOIN user_sessions u USING(user_id) LEFT JOIN states st ON st.user_id=ids.user_id AND st.rn=1\n        LEFT JOIN legacy l ON l.user_id=ids.user_id AND l.rn=1 LEFT JOIN days d USING(user_id)),\n    e AS (SELECT * FROM e0 WHERE ('${behavior_period}'='all' OR julianday(occurred_at) >= julianday(${__from}/1000,'unixepoch') AND julianday(occurred_at) < julianday(${__to}/1000,'unixepoch'))\n        OR (occurred_at IS NULL AND '${behavior_period}'='all')),\n    players AS (SELECT * FROM p WHERE 1),\n    events AS (SELECT * FROM e WHERE 1) SELECT action 行为,SUM(amount) 体力,COUNT(*) 扣除次数,SUM(legacy) 旧版样本数 FROM (\n            SELECT json_extract(payload_json,'$.action') action,json_extract(payload_json,'$.amount') amount,0 legacy FROM events WHERE event_type='stamina_spent'\n            UNION ALL SELECT CASE WHEN event_type='fishing_catch' THEN 'fishing' ELSE event_type END,energy_cost,1 FROM events old\n            WHERE COALESCE(schema_version,0)<3 AND actor_is_player=1 AND energy_cost>0\n            AND NOT(event_type='building_placed' AND json_extract(payload_json,'$.building_id')=6001\n                AND json_extract(payload_json,'$.position_x') IS NOT NULL\n                AND EXISTS(SELECT 1 FROM e0 till WHERE till.user_id=old.user_id AND till.session_id=old.session_id AND till.game_day=old.game_day\n                    AND till.event_type='farming_till' AND ABS(till.sequence-old.sequence)<=1 AND till.energy_cost=old.energy_cost\n                    AND json_extract(till.payload_json,'$.position_x')=json_extract(old.payload_json,'$.position_x')\n                    AND json_extract(till.payload_json,'$.position_y')=json_extract(old.payload_json,'$.position_y')))\n        ) GROUP BY action ORDER BY 体力 DESC,行为 LIMIT 5",
          "rawQueryText": "WITH e0 AS NOT MATERIALIZED (SELECT * FROM analytics_events WHERE ('__all__' IN (${client_platform:sqlstring}) OR client_platform IN (${client_platform:sqlstring})) AND ('__all__' IN (${release_version:sqlstring}) OR COALESCE(NULLIF(release_version,''),'__empty__') IN (${release_version:sqlstring})) AND ('${test_data}'='include' OR ('${test_data}'='auto' AND (client_platform='editor' OR (is_development_build=0 AND is_developer=0))) OR ('${test_data}'='exclude' AND is_development_build=0 AND is_developer=0 AND client_platform!='editor') OR ('${test_data}'='only' AND (is_development_build=1 OR is_developer=1 OR client_platform='editor')))),\n    s AS MATERIALIZED (SELECT * FROM analytics_sessions WHERE ('__all__' IN (${client_platform:sqlstring}) OR client_platform IN (${client_platform:sqlstring})) AND ('__all__' IN (${release_version:sqlstring}) OR COALESCE(NULLIF(release_version,''),'__empty__') IN (${release_version:sqlstring})) AND ('${test_data}'='include' OR ('${test_data}'='auto' AND (client_platform='editor' OR (is_development_build=0 AND is_developer=0))) OR ('${test_data}'='exclude' AND is_development_build=0 AND is_developer=0 AND client_platform!='editor') OR ('${test_data}'='only' AND (is_development_build=1 OR is_developer=1 OR client_platform='editor')))),\n    ids AS (SELECT user_id FROM s UNION SELECT user_id FROM e0),\n    states AS (SELECT *,ROW_NUMBER() OVER(PARTITION BY user_id ORDER BY julianday(occurred_at) DESC,sequence DESC,event_id DESC) rn\n        FROM e0 WHERE event_type='player_state_snapshot'),\n    legacy AS (SELECT g.*,ROW_NUMBER() OVER(PARTITION BY user_id ORDER BY julianday(real_time_started_iso) DESC,imported_at DESC) rn\n        FROM gameplay_sessions g WHERE EXISTS(SELECT 1 FROM s WHERE s.user_id=g.user_id AND s.session_id=g.session_id)),\n    days AS (SELECT user_id,COUNT(*) play_days FROM\n        (SELECT DISTINCT user_id,archive_id,game_day FROM e0 WHERE NULLIF(archive_id,'') IS NOT NULL AND game_day IS NOT NULL) GROUP BY user_id),\n    p AS (SELECT ids.user_id,COALESCE(NULLIF(json_extract(st.payload_json,'$.nickname'),''),NULLIF(u.nickname,''),NULLIF(u.player_name,''),ids.user_id) nickname,\n        (SELECT SUM(duration_sec) FROM s WHERE s.user_id=ids.user_id) play_seconds,\n        (SELECT first_entry_at FROM analytics_first_entry f WHERE f.user_id=ids.user_id AND f.cohort_scope=CASE WHEN '${test_data}' IN ('include','only') OR ('${test_data}'='auto' AND ('editor' IN (${client_platform:sqlstring}) OR '__all__' IN (${client_platform:sqlstring}))) THEN 'all' ELSE 'production' END) first_login,\n        (SELECT MAX(started_at) FROM s WHERE s.user_id=ids.user_id) latest_login,\n        d.play_days,COALESCE(json_extract(st.payload_json,'$.money'),l.money_end,l.money_start) money,\n        COALESCE(json_extract(st.payload_json,'$.island_level'),l.island_level_max) island_level,\n        json_array_length(json_extract(st.payload_json,'$.cats')) cat_count,\n        st.payload_json state,st.occurred_at state_at,\n        json_extract(st.payload_json,'$.cat_house_count') house_count,\n        json_extract(st.payload_json,'$.cat_house_capacity') house_capacity,\n        json_extract(st.payload_json,'$.cat_house_occupied') house_occupied\n        FROM ids LEFT JOIN user_sessions u USING(user_id) LEFT JOIN states st ON st.user_id=ids.user_id AND st.rn=1\n        LEFT JOIN legacy l ON l.user_id=ids.user_id AND l.rn=1 LEFT JOIN days d USING(user_id)),\n    e AS (SELECT * FROM e0 WHERE ('${behavior_period}'='all' OR julianday(occurred_at) >= julianday(${__from}/1000,'unixepoch') AND julianday(occurred_at) < julianday(${__to}/1000,'unixepoch'))\n        OR (occurred_at IS NULL AND '${behavior_period}'='all')),\n    players AS (SELECT * FROM p WHERE 1),\n    events AS (SELECT * FROM e WHERE 1) SELECT action 行为,SUM(amount) 体力,COUNT(*) 扣除次数,SUM(legacy) 旧版样本数 FROM (\n            SELECT json_extract(payload_json,'$.action') action,json_extract(payload_json,'$.amount') amount,0 legacy FROM events WHERE event_type='stamina_spent'\n            UNION ALL SELECT CASE WHEN event_type='fishing_catch' THEN 'fishing' ELSE event_type END,energy_cost,1 FROM events old\n            WHERE COALESCE(schema_version,0)<3 AND actor_is_player=1 AND energy_cost>0\n            AND NOT(event_type='building_placed' AND json_extract(payload_json,'$.building_id')=6001\n                AND json_extract(payload_json,'$.position_x') IS NOT NULL\n                AND EXISTS(SELECT 1 FROM e0 till WHERE till.user_id=old.user_id AND till.session_id=old.session_id AND till.game_day=old.game_day\n                    AND till.event_type='farming_till' AND ABS(till.sequence-old.sequence)<=1 AND till.energy_cost=old.energy_cost\n                    AND json_extract(till.payload_json,'$.position_x')=json_extract(old.payload_json,'$.position_x')\n                    AND json_extract(till.payload_json,'$.position_y')=json_extract(old.payload_json,'$.position_y')))\n        ) GROUP BY action ORDER BY 体力 DESC,行为 LIMIT 5"
        }
      ],
      "fieldConfig": {
        "defaults": {
          "unit": "short",
          "custom": {
            "filterable": true
          },
          "noValue": "未采集"
        },
        "overrides": [
          {
            "matcher": {
              "id": "byName",
              "options": "user_id"
            },
            "properties": [
              {
                "id": "links",
                "value": [
                  {
                    "title": "查看玩家旅程",
                    "url": "/d/gameplay-player-detail?${__url_time_range}&${client_platform:queryparam}&${release_version:queryparam}&${test_data:queryparam}&var-user_id=${__data.fields.user_id}&var-user_id=${__value.raw}"
                  }
                ]
              }
            ]
          },
          {
            "matcher": {
              "id": "byName",
              "options": "session_id"
            },
            "properties": [
              {
                "id": "links",
                "value": [
                  {
                    "title": "查看玩家旅程",
                    "url": "/d/gameplay-player-detail?${__url_time_range}&${client_platform:queryparam}&${release_version:queryparam}&${test_data:queryparam}&var-user_id=${__data.fields.user_id}&var-session_id=${__value.raw}"
                  }
                ]
              }
            ]
          },
          {
            "matcher": {
              "id": "byName",
              "options": "llm_request_id"
            },
            "properties": [
              {
                "id": "links",
                "value": [
                  {
                    "title": "查看玩家旅程",
                    "url": "/d/gameplay-player-detail?${__url_time_range}&${client_platform:queryparam}&${release_version:queryparam}&${test_data:queryparam}&var-user_id=${__data.fields.user_id}&var-llm_request_id=${__value.raw}"
                  }
                ]
              }
            ]
          }
        ]
      },
      "options": {
        "showHeader": true,
        "cellHeight": "sm"
      },
      "gridPos": {
        "x": 0,
        "y": 73,
        "w": 12,
        "h": 9
      }
    },
    {
      "id": 142,
      "title": "钓鱼区域与鱼影",
      "type": "table",
      "datasource": {
        "type": "frser-sqlite-datasource",
        "uid": "${DS_SQLITE}"
      },
      "description": "累计按玩家去重；当前状态取最新快照。缺失显示未采集，数值零保留。行为可切换期间，日期为北京时间。",
      "targets": [
        {
          "refId": "A",
          "queryType": "table",
          "queryText": "WITH e0 AS NOT MATERIALIZED (SELECT * FROM analytics_events WHERE ('__all__' IN (${client_platform:sqlstring}) OR client_platform IN (${client_platform:sqlstring})) AND ('__all__' IN (${release_version:sqlstring}) OR COALESCE(NULLIF(release_version,''),'__empty__') IN (${release_version:sqlstring})) AND ('${test_data}'='include' OR ('${test_data}'='auto' AND (client_platform='editor' OR (is_development_build=0 AND is_developer=0))) OR ('${test_data}'='exclude' AND is_development_build=0 AND is_developer=0 AND client_platform!='editor') OR ('${test_data}'='only' AND (is_development_build=1 OR is_developer=1 OR client_platform='editor')))),\n    s AS MATERIALIZED (SELECT * FROM analytics_sessions WHERE ('__all__' IN (${client_platform:sqlstring}) OR client_platform IN (${client_platform:sqlstring})) AND ('__all__' IN (${release_version:sqlstring}) OR COALESCE(NULLIF(release_version,''),'__empty__') IN (${release_version:sqlstring})) AND ('${test_data}'='include' OR ('${test_data}'='auto' AND (client_platform='editor' OR (is_development_build=0 AND is_developer=0))) OR ('${test_data}'='exclude' AND is_development_build=0 AND is_developer=0 AND client_platform!='editor') OR ('${test_data}'='only' AND (is_development_build=1 OR is_developer=1 OR client_platform='editor')))),\n    ids AS (SELECT user_id FROM s UNION SELECT user_id FROM e0),\n    states AS (SELECT *,ROW_NUMBER() OVER(PARTITION BY user_id ORDER BY julianday(occurred_at) DESC,sequence DESC,event_id DESC) rn\n        FROM e0 WHERE event_type='player_state_snapshot'),\n    legacy AS (SELECT g.*,ROW_NUMBER() OVER(PARTITION BY user_id ORDER BY julianday(real_time_started_iso) DESC,imported_at DESC) rn\n        FROM gameplay_sessions g WHERE EXISTS(SELECT 1 FROM s WHERE s.user_id=g.user_id AND s.session_id=g.session_id)),\n    days AS (SELECT user_id,COUNT(*) play_days FROM\n        (SELECT DISTINCT user_id,archive_id,game_day FROM e0 WHERE NULLIF(archive_id,'') IS NOT NULL AND game_day IS NOT NULL) GROUP BY user_id),\n    p AS (SELECT ids.user_id,COALESCE(NULLIF(json_extract(st.payload_json,'$.nickname'),''),NULLIF(u.nickname,''),NULLIF(u.player_name,''),ids.user_id) nickname,\n        (SELECT SUM(duration_sec) FROM s WHERE s.user_id=ids.user_id) play_seconds,\n        (SELECT first_entry_at FROM analytics_first_entry f WHERE f.user_id=ids.user_id AND f.cohort_scope=CASE WHEN '${test_data}' IN ('include','only') OR ('${test_data}'='auto' AND ('editor' IN (${client_platform:sqlstring}) OR '__all__' IN (${client_platform:sqlstring}))) THEN 'all' ELSE 'production' END) first_login,\n        (SELECT MAX(started_at) FROM s WHERE s.user_id=ids.user_id) latest_login,\n        d.play_days,COALESCE(json_extract(st.payload_json,'$.money'),l.money_end,l.money_start) money,\n        COALESCE(json_extract(st.payload_json,'$.island_level'),l.island_level_max) island_level,\n        json_array_length(json_extract(st.payload_json,'$.cats')) cat_count,\n        st.payload_json state,st.occurred_at state_at,\n        json_extract(st.payload_json,'$.cat_house_count') house_count,\n        json_extract(st.payload_json,'$.cat_house_capacity') house_capacity,\n        json_extract(st.payload_json,'$.cat_house_occupied') house_occupied\n        FROM ids LEFT JOIN user_sessions u USING(user_id) LEFT JOIN states st ON st.user_id=ids.user_id AND st.rn=1\n        LEFT JOIN legacy l ON l.user_id=ids.user_id AND l.rn=1 LEFT JOIN days d USING(user_id)),\n    e AS (SELECT * FROM e0 WHERE ('${behavior_period}'='all' OR julianday(occurred_at) >= julianday(${__from}/1000,'unixepoch') AND julianday(occurred_at) < julianday(${__to}/1000,'unixepoch'))\n        OR (occurred_at IS NULL AND '${behavior_period}'='all')),\n    players AS (SELECT * FROM p WHERE 1),\n    events AS (SELECT * FROM e WHERE 1) SELECT json_extract(payload_json,'$.region') 区域,json_extract(payload_json,'$.water_kind') 水域类型,COUNT(*) 结算次数,SUM(json_extract(payload_json,'$.outcome')='success') 成功数,SUM(json_extract(payload_json,'$.outcome')='failed') 失败数,SUM(json_extract(payload_json,'$.outcome')='cancelled') 取消数 FROM events WHERE event_type='fishing_finished' GROUP BY 1,2",
          "rawQueryText": "WITH e0 AS NOT MATERIALIZED (SELECT * FROM analytics_events WHERE ('__all__' IN (${client_platform:sqlstring}) OR client_platform IN (${client_platform:sqlstring})) AND ('__all__' IN (${release_version:sqlstring}) OR COALESCE(NULLIF(release_version,''),'__empty__') IN (${release_version:sqlstring})) AND ('${test_data}'='include' OR ('${test_data}'='auto' AND (client_platform='editor' OR (is_development_build=0 AND is_developer=0))) OR ('${test_data}'='exclude' AND is_development_build=0 AND is_developer=0 AND client_platform!='editor') OR ('${test_data}'='only' AND (is_development_build=1 OR is_developer=1 OR client_platform='editor')))),\n    s AS MATERIALIZED (SELECT * FROM analytics_sessions WHERE ('__all__' IN (${client_platform:sqlstring}) OR client_platform IN (${client_platform:sqlstring})) AND ('__all__' IN (${release_version:sqlstring}) OR COALESCE(NULLIF(release_version,''),'__empty__') IN (${release_version:sqlstring})) AND ('${test_data}'='include' OR ('${test_data}'='auto' AND (client_platform='editor' OR (is_development_build=0 AND is_developer=0))) OR ('${test_data}'='exclude' AND is_development_build=0 AND is_developer=0 AND client_platform!='editor') OR ('${test_data}'='only' AND (is_development_build=1 OR is_developer=1 OR client_platform='editor')))),\n    ids AS (SELECT user_id FROM s UNION SELECT user_id FROM e0),\n    states AS (SELECT *,ROW_NUMBER() OVER(PARTITION BY user_id ORDER BY julianday(occurred_at) DESC,sequence DESC,event_id DESC) rn\n        FROM e0 WHERE event_type='player_state_snapshot'),\n    legacy AS (SELECT g.*,ROW_NUMBER() OVER(PARTITION BY user_id ORDER BY julianday(real_time_started_iso) DESC,imported_at DESC) rn\n        FROM gameplay_sessions g WHERE EXISTS(SELECT 1 FROM s WHERE s.user_id=g.user_id AND s.session_id=g.session_id)),\n    days AS (SELECT user_id,COUNT(*) play_days FROM\n        (SELECT DISTINCT user_id,archive_id,game_day FROM e0 WHERE NULLIF(archive_id,'') IS NOT NULL AND game_day IS NOT NULL) GROUP BY user_id),\n    p AS (SELECT ids.user_id,COALESCE(NULLIF(json_extract(st.payload_json,'$.nickname'),''),NULLIF(u.nickname,''),NULLIF(u.player_name,''),ids.user_id) nickname,\n        (SELECT SUM(duration_sec) FROM s WHERE s.user_id=ids.user_id) play_seconds,\n        (SELECT first_entry_at FROM analytics_first_entry f WHERE f.user_id=ids.user_id AND f.cohort_scope=CASE WHEN '${test_data}' IN ('include','only') OR ('${test_data}'='auto' AND ('editor' IN (${client_platform:sqlstring}) OR '__all__' IN (${client_platform:sqlstring}))) THEN 'all' ELSE 'production' END) first_login,\n        (SELECT MAX(started_at) FROM s WHERE s.user_id=ids.user_id) latest_login,\n        d.play_days,COALESCE(json_extract(st.payload_json,'$.money'),l.money_end,l.money_start) money,\n        COALESCE(json_extract(st.payload_json,'$.island_level'),l.island_level_max) island_level,\n        json_array_length(json_extract(st.payload_json,'$.cats')) cat_count,\n        st.payload_json state,st.occurred_at state_at,\n        json_extract(st.payload_json,'$.cat_house_count') house_count,\n        json_extract(st.payload_json,'$.cat_house_capacity') house_capacity,\n        json_extract(st.payload_json,'$.cat_house_occupied') house_occupied\n        FROM ids LEFT JOIN user_sessions u USING(user_id) LEFT JOIN states st ON st.user_id=ids.user_id AND st.rn=1\n        LEFT JOIN legacy l ON l.user_id=ids.user_id AND l.rn=1 LEFT JOIN days d USING(user_id)),\n    e AS (SELECT * FROM e0 WHERE ('${behavior_period}'='all' OR julianday(occurred_at) >= julianday(${__from}/1000,'unixepoch') AND julianday(occurred_at) < julianday(${__to}/1000,'unixepoch'))\n        OR (occurred_at IS NULL AND '${behavior_period}'='all')),\n    players AS (SELECT * FROM p WHERE 1),\n    events AS (SELECT * FROM e WHERE 1) SELECT json_extract(payload_json,'$.region') 区域,json_extract(payload_json,'$.water_kind') 水域类型,COUNT(*) 结算次数,SUM(json_extract(payload_json,'$.outcome')='success') 成功数,SUM(json_extract(payload_json,'$.outcome')='failed') 失败数,SUM(json_extract(payload_json,'$.outcome')='cancelled') 取消数 FROM events WHERE event_type='fishing_finished' GROUP BY 1,2"
        }
      ],
      "fieldConfig": {
        "defaults": {
          "unit": "short",
          "custom": {
            "filterable": true
          },
          "noValue": "未采集"
        },
        "overrides": [
          {
            "matcher": {
              "id": "byName",
              "options": "user_id"
            },
            "properties": [
              {
                "id": "links",
                "value": [
                  {
                    "title": "查看玩家旅程",
                    "url": "/d/gameplay-player-detail?${__url_time_range}&${client_platform:queryparam}&${release_version:queryparam}&${test_data:queryparam}&var-user_id=${__data.fields.user_id}&var-user_id=${__value.raw}"
                  }
                ]
              }
            ]
          },
          {
            "matcher": {
              "id": "byName",
              "options": "session_id"
            },
            "properties": [
              {
                "id": "links",
                "value": [
                  {
                    "title": "查看玩家旅程",
                    "url": "/d/gameplay-player-detail?${__url_time_range}&${client_platform:queryparam}&${release_version:queryparam}&${test_data:queryparam}&var-user_id=${__data.fields.user_id}&var-session_id=${__value.raw}"
                  }
                ]
              }
            ]
          },
          {
            "matcher": {
              "id": "byName",
              "options": "llm_request_id"
            },
            "properties": [
              {
                "id": "links",
                "value": [
                  {
                    "title": "查看玩家旅程",
                    "url": "/d/gameplay-player-detail?${__url_time_range}&${client_platform:queryparam}&${release_version:queryparam}&${test_data:queryparam}&var-user_id=${__data.fields.user_id}&var-llm_request_id=${__value.raw}"
                  }
                ]
              }
            ]
          }
        ]
      },
      "options": {
        "showHeader": true,
        "cellHeight": "sm"
      },
      "gridPos": {
        "x": 12,
        "y": 73,
        "w": 12,
        "h": 9
      }
    },
    {
      "id": 143,
      "title": "猫工作技能点分配",
      "type": "table",
      "datasource": {
        "type": "frser-sqlite-datasource",
        "uid": "${DS_SQLITE}"
      },
      "description": "累计按玩家去重；当前状态取最新快照。缺失显示未采集，数值零保留。行为可切换期间，日期为北京时间。",
      "targets": [
        {
          "refId": "A",
          "queryType": "table",
          "queryText": "WITH e0 AS NOT MATERIALIZED (SELECT * FROM analytics_events WHERE ('__all__' IN (${client_platform:sqlstring}) OR client_platform IN (${client_platform:sqlstring})) AND ('__all__' IN (${release_version:sqlstring}) OR COALESCE(NULLIF(release_version,''),'__empty__') IN (${release_version:sqlstring})) AND ('${test_data}'='include' OR ('${test_data}'='auto' AND (client_platform='editor' OR (is_development_build=0 AND is_developer=0))) OR ('${test_data}'='exclude' AND is_development_build=0 AND is_developer=0 AND client_platform!='editor') OR ('${test_data}'='only' AND (is_development_build=1 OR is_developer=1 OR client_platform='editor')))),\n    s AS MATERIALIZED (SELECT * FROM analytics_sessions WHERE ('__all__' IN (${client_platform:sqlstring}) OR client_platform IN (${client_platform:sqlstring})) AND ('__all__' IN (${release_version:sqlstring}) OR COALESCE(NULLIF(release_version,''),'__empty__') IN (${release_version:sqlstring})) AND ('${test_data}'='include' OR ('${test_data}'='auto' AND (client_platform='editor' OR (is_development_build=0 AND is_developer=0))) OR ('${test_data}'='exclude' AND is_development_build=0 AND is_developer=0 AND client_platform!='editor') OR ('${test_data}'='only' AND (is_development_build=1 OR is_developer=1 OR client_platform='editor')))),\n    ids AS (SELECT user_id FROM s UNION SELECT user_id FROM e0),\n    states AS (SELECT *,ROW_NUMBER() OVER(PARTITION BY user_id ORDER BY julianday(occurred_at) DESC,sequence DESC,event_id DESC) rn\n        FROM e0 WHERE event_type='player_state_snapshot'),\n    legacy AS (SELECT g.*,ROW_NUMBER() OVER(PARTITION BY user_id ORDER BY julianday(real_time_started_iso) DESC,imported_at DESC) rn\n        FROM gameplay_sessions g WHERE EXISTS(SELECT 1 FROM s WHERE s.user_id=g.user_id AND s.session_id=g.session_id)),\n    days AS (SELECT user_id,COUNT(*) play_days FROM\n        (SELECT DISTINCT user_id,archive_id,game_day FROM e0 WHERE NULLIF(archive_id,'') IS NOT NULL AND game_day IS NOT NULL) GROUP BY user_id),\n    p AS (SELECT ids.user_id,COALESCE(NULLIF(json_extract(st.payload_json,'$.nickname'),''),NULLIF(u.nickname,''),NULLIF(u.player_name,''),ids.user_id) nickname,\n        (SELECT SUM(duration_sec) FROM s WHERE s.user_id=ids.user_id) play_seconds,\n        (SELECT first_entry_at FROM analytics_first_entry f WHERE f.user_id=ids.user_id AND f.cohort_scope=CASE WHEN '${test_data}' IN ('include','only') OR ('${test_data}'='auto' AND ('editor' IN (${client_platform:sqlstring}) OR '__all__' IN (${client_platform:sqlstring}))) THEN 'all' ELSE 'production' END) first_login,\n        (SELECT MAX(started_at) FROM s WHERE s.user_id=ids.user_id) latest_login,\n        d.play_days,COALESCE(json_extract(st.payload_json,'$.money'),l.money_end,l.money_start) money,\n        COALESCE(json_extract(st.payload_json,'$.island_level'),l.island_level_max) island_level,\n        json_array_length(json_extract(st.payload_json,'$.cats')) cat_count,\n        st.payload_json state,st.occurred_at state_at,\n        json_extract(st.payload_json,'$.cat_house_count') house_count,\n        json_extract(st.payload_json,'$.cat_house_capacity') house_capacity,\n        json_extract(st.payload_json,'$.cat_house_occupied') house_occupied\n        FROM ids LEFT JOIN user_sessions u USING(user_id) LEFT JOIN states st ON st.user_id=ids.user_id AND st.rn=1\n        LEFT JOIN legacy l ON l.user_id=ids.user_id AND l.rn=1 LEFT JOIN days d USING(user_id)),\n    e AS (SELECT * FROM e0 WHERE ('${behavior_period}'='all' OR julianday(occurred_at) >= julianday(${__from}/1000,'unixepoch') AND julianday(occurred_at) < julianday(${__to}/1000,'unixepoch'))\n        OR (occurred_at IS NULL AND '${behavior_period}'='all')),\n    players AS (SELECT * FROM p WHERE 1),\n    events AS (SELECT * FROM e WHERE 1) SELECT json_extract(payload_json,'$.work') 工作,COUNT(*) 投入点数,ROUND(100.0*COUNT(*)/SUM(COUNT(*)) OVER(),2) 比例 FROM events WHERE event_type='cat_skill_allocated' GROUP BY 1",
          "rawQueryText": "WITH e0 AS NOT MATERIALIZED (SELECT * FROM analytics_events WHERE ('__all__' IN (${client_platform:sqlstring}) OR client_platform IN (${client_platform:sqlstring})) AND ('__all__' IN (${release_version:sqlstring}) OR COALESCE(NULLIF(release_version,''),'__empty__') IN (${release_version:sqlstring})) AND ('${test_data}'='include' OR ('${test_data}'='auto' AND (client_platform='editor' OR (is_development_build=0 AND is_developer=0))) OR ('${test_data}'='exclude' AND is_development_build=0 AND is_developer=0 AND client_platform!='editor') OR ('${test_data}'='only' AND (is_development_build=1 OR is_developer=1 OR client_platform='editor')))),\n    s AS MATERIALIZED (SELECT * FROM analytics_sessions WHERE ('__all__' IN (${client_platform:sqlstring}) OR client_platform IN (${client_platform:sqlstring})) AND ('__all__' IN (${release_version:sqlstring}) OR COALESCE(NULLIF(release_version,''),'__empty__') IN (${release_version:sqlstring})) AND ('${test_data}'='include' OR ('${test_data}'='auto' AND (client_platform='editor' OR (is_development_build=0 AND is_developer=0))) OR ('${test_data}'='exclude' AND is_development_build=0 AND is_developer=0 AND client_platform!='editor') OR ('${test_data}'='only' AND (is_development_build=1 OR is_developer=1 OR client_platform='editor')))),\n    ids AS (SELECT user_id FROM s UNION SELECT user_id FROM e0),\n    states AS (SELECT *,ROW_NUMBER() OVER(PARTITION BY user_id ORDER BY julianday(occurred_at) DESC,sequence DESC,event_id DESC) rn\n        FROM e0 WHERE event_type='player_state_snapshot'),\n    legacy AS (SELECT g.*,ROW_NUMBER() OVER(PARTITION BY user_id ORDER BY julianday(real_time_started_iso) DESC,imported_at DESC) rn\n        FROM gameplay_sessions g WHERE EXISTS(SELECT 1 FROM s WHERE s.user_id=g.user_id AND s.session_id=g.session_id)),\n    days AS (SELECT user_id,COUNT(*) play_days FROM\n        (SELECT DISTINCT user_id,archive_id,game_day FROM e0 WHERE NULLIF(archive_id,'') IS NOT NULL AND game_day IS NOT NULL) GROUP BY user_id),\n    p AS (SELECT ids.user_id,COALESCE(NULLIF(json_extract(st.payload_json,'$.nickname'),''),NULLIF(u.nickname,''),NULLIF(u.player_name,''),ids.user_id) nickname,\n        (SELECT SUM(duration_sec) FROM s WHERE s.user_id=ids.user_id) play_seconds,\n        (SELECT first_entry_at FROM analytics_first_entry f WHERE f.user_id=ids.user_id AND f.cohort_scope=CASE WHEN '${test_data}' IN ('include','only') OR ('${test_data}'='auto' AND ('editor' IN (${client_platform:sqlstring}) OR '__all__' IN (${client_platform:sqlstring}))) THEN 'all' ELSE 'production' END) first_login,\n        (SELECT MAX(started_at) FROM s WHERE s.user_id=ids.user_id) latest_login,\n        d.play_days,COALESCE(json_extract(st.payload_json,'$.money'),l.money_end,l.money_start) money,\n        COALESCE(json_extract(st.payload_json,'$.island_level'),l.island_level_max) island_level,\n        json_array_length(json_extract(st.payload_json,'$.cats')) cat_count,\n        st.payload_json state,st.occurred_at state_at,\n        json_extract(st.payload_json,'$.cat_house_count') house_count,\n        json_extract(st.payload_json,'$.cat_house_capacity') house_capacity,\n        json_extract(st.payload_json,'$.cat_house_occupied') house_occupied\n        FROM ids LEFT JOIN user_sessions u USING(user_id) LEFT JOIN states st ON st.user_id=ids.user_id AND st.rn=1\n        LEFT JOIN legacy l ON l.user_id=ids.user_id AND l.rn=1 LEFT JOIN days d USING(user_id)),\n    e AS (SELECT * FROM e0 WHERE ('${behavior_period}'='all' OR julianday(occurred_at) >= julianday(${__from}/1000,'unixepoch') AND julianday(occurred_at) < julianday(${__to}/1000,'unixepoch'))\n        OR (occurred_at IS NULL AND '${behavior_period}'='all')),\n    players AS (SELECT * FROM p WHERE 1),\n    events AS (SELECT * FROM e WHERE 1) SELECT json_extract(payload_json,'$.work') 工作,COUNT(*) 投入点数,ROUND(100.0*COUNT(*)/SUM(COUNT(*)) OVER(),2) 比例 FROM events WHERE event_type='cat_skill_allocated' GROUP BY 1"
        }
      ],
      "fieldConfig": {
        "defaults": {
          "unit": "short",
          "custom": {
            "filterable": true
          },
          "noValue": "未采集"
        },
        "overrides": [
          {
            "matcher": {
              "id": "byName",
              "options": "user_id"
            },
            "properties": [
              {
                "id": "links",
                "value": [
                  {
                    "title": "查看玩家旅程",
                    "url": "/d/gameplay-player-detail?${__url_time_range}&${client_platform:queryparam}&${release_version:queryparam}&${test_data:queryparam}&var-user_id=${__data.fields.user_id}&var-user_id=${__value.raw}"
                  }
                ]
              }
            ]
          },
          {
            "matcher": {
              "id": "byName",
              "options": "session_id"
            },
            "properties": [
              {
                "id": "links",
                "value": [
                  {
                    "title": "查看玩家旅程",
                    "url": "/d/gameplay-player-detail?${__url_time_range}&${client_platform:queryparam}&${release_version:queryparam}&${test_data:queryparam}&var-user_id=${__data.fields.user_id}&var-session_id=${__value.raw}"
                  }
                ]
              }
            ]
          },
          {
            "matcher": {
              "id": "byName",
              "options": "llm_request_id"
            },
            "properties": [
              {
                "id": "links",
                "value": [
                  {
                    "title": "查看玩家旅程",
                    "url": "/d/gameplay-player-detail?${__url_time_range}&${client_platform:queryparam}&${release_version:queryparam}&${test_data:queryparam}&var-user_id=${__data.fields.user_id}&var-llm_request_id=${__value.raw}"
                  }
                ]
              }
            ]
          }
        ]
      },
      "options": {
        "showHeader": true,
        "cellHeight": "sm"
      },
      "gridPos": {
        "x": 0,
        "y": 82,
        "w": 12,
        "h": 9
      }
    },
    {
      "id": 144,
      "title": "升级前未使用技能点",
      "type": "table",
      "datasource": {
        "type": "frser-sqlite-datasource",
        "uid": "${DS_SQLITE}"
      },
      "description": "累计按玩家去重；当前状态取最新快照。缺失显示未采集，数值零保留。行为可切换期间，日期为北京时间。",
      "targets": [
        {
          "refId": "A",
          "queryType": "table",
          "queryText": "WITH e0 AS NOT MATERIALIZED (SELECT * FROM analytics_events WHERE ('__all__' IN (${client_platform:sqlstring}) OR client_platform IN (${client_platform:sqlstring})) AND ('__all__' IN (${release_version:sqlstring}) OR COALESCE(NULLIF(release_version,''),'__empty__') IN (${release_version:sqlstring})) AND ('${test_data}'='include' OR ('${test_data}'='auto' AND (client_platform='editor' OR (is_development_build=0 AND is_developer=0))) OR ('${test_data}'='exclude' AND is_development_build=0 AND is_developer=0 AND client_platform!='editor') OR ('${test_data}'='only' AND (is_development_build=1 OR is_developer=1 OR client_platform='editor')))),\n    s AS MATERIALIZED (SELECT * FROM analytics_sessions WHERE ('__all__' IN (${client_platform:sqlstring}) OR client_platform IN (${client_platform:sqlstring})) AND ('__all__' IN (${release_version:sqlstring}) OR COALESCE(NULLIF(release_version,''),'__empty__') IN (${release_version:sqlstring})) AND ('${test_data}'='include' OR ('${test_data}'='auto' AND (client_platform='editor' OR (is_development_build=0 AND is_developer=0))) OR ('${test_data}'='exclude' AND is_development_build=0 AND is_developer=0 AND client_platform!='editor') OR ('${test_data}'='only' AND (is_development_build=1 OR is_developer=1 OR client_platform='editor')))),\n    ids AS (SELECT user_id FROM s UNION SELECT user_id FROM e0),\n    states AS (SELECT *,ROW_NUMBER() OVER(PARTITION BY user_id ORDER BY julianday(occurred_at) DESC,sequence DESC,event_id DESC) rn\n        FROM e0 WHERE event_type='player_state_snapshot'),\n    legacy AS (SELECT g.*,ROW_NUMBER() OVER(PARTITION BY user_id ORDER BY julianday(real_time_started_iso) DESC,imported_at DESC) rn\n        FROM gameplay_sessions g WHERE EXISTS(SELECT 1 FROM s WHERE s.user_id=g.user_id AND s.session_id=g.session_id)),\n    days AS (SELECT user_id,COUNT(*) play_days FROM\n        (SELECT DISTINCT user_id,archive_id,game_day FROM e0 WHERE NULLIF(archive_id,'') IS NOT NULL AND game_day IS NOT NULL) GROUP BY user_id),\n    p AS (SELECT ids.user_id,COALESCE(NULLIF(json_extract(st.payload_json,'$.nickname'),''),NULLIF(u.nickname,''),NULLIF(u.player_name,''),ids.user_id) nickname,\n        (SELECT SUM(duration_sec) FROM s WHERE s.user_id=ids.user_id) play_seconds,\n        (SELECT first_entry_at FROM analytics_first_entry f WHERE f.user_id=ids.user_id AND f.cohort_scope=CASE WHEN '${test_data}' IN ('include','only') OR ('${test_data}'='auto' AND ('editor' IN (${client_platform:sqlstring}) OR '__all__' IN (${client_platform:sqlstring}))) THEN 'all' ELSE 'production' END) first_login,\n        (SELECT MAX(started_at) FROM s WHERE s.user_id=ids.user_id) latest_login,\n        d.play_days,COALESCE(json_extract(st.payload_json,'$.money'),l.money_end,l.money_start) money,\n        COALESCE(json_extract(st.payload_json,'$.island_level'),l.island_level_max) island_level,\n        json_array_length(json_extract(st.payload_json,'$.cats')) cat_count,\n        st.payload_json state,st.occurred_at state_at,\n        json_extract(st.payload_json,'$.cat_house_count') house_count,\n        json_extract(st.payload_json,'$.cat_house_capacity') house_capacity,\n        json_extract(st.payload_json,'$.cat_house_occupied') house_occupied\n        FROM ids LEFT JOIN user_sessions u USING(user_id) LEFT JOIN states st ON st.user_id=ids.user_id AND st.rn=1\n        LEFT JOIN legacy l ON l.user_id=ids.user_id AND l.rn=1 LEFT JOIN days d USING(user_id)),\n    e AS (SELECT * FROM e0 WHERE ('${behavior_period}'='all' OR julianday(occurred_at) >= julianday(${__from}/1000,'unixepoch') AND julianday(occurred_at) < julianday(${__to}/1000,'unixepoch'))\n        OR (occurred_at IS NULL AND '${behavior_period}'='all')),\n    players AS (SELECT * FROM p WHERE 1),\n    events AS (SELECT * FROM e WHERE 1) SELECT AVG(json_extract(payload_json,'$.remaining_before_reward')) 平均剩余点数,100.0*AVG(json_extract(payload_json,'$.remaining_before_reward')>0) 有剩余比例,COUNT(*) 升级样本数 FROM events WHERE event_type='cat_level_up'",
          "rawQueryText": "WITH e0 AS NOT MATERIALIZED (SELECT * FROM analytics_events WHERE ('__all__' IN (${client_platform:sqlstring}) OR client_platform IN (${client_platform:sqlstring})) AND ('__all__' IN (${release_version:sqlstring}) OR COALESCE(NULLIF(release_version,''),'__empty__') IN (${release_version:sqlstring})) AND ('${test_data}'='include' OR ('${test_data}'='auto' AND (client_platform='editor' OR (is_development_build=0 AND is_developer=0))) OR ('${test_data}'='exclude' AND is_development_build=0 AND is_developer=0 AND client_platform!='editor') OR ('${test_data}'='only' AND (is_development_build=1 OR is_developer=1 OR client_platform='editor')))),\n    s AS MATERIALIZED (SELECT * FROM analytics_sessions WHERE ('__all__' IN (${client_platform:sqlstring}) OR client_platform IN (${client_platform:sqlstring})) AND ('__all__' IN (${release_version:sqlstring}) OR COALESCE(NULLIF(release_version,''),'__empty__') IN (${release_version:sqlstring})) AND ('${test_data}'='include' OR ('${test_data}'='auto' AND (client_platform='editor' OR (is_development_build=0 AND is_developer=0))) OR ('${test_data}'='exclude' AND is_development_build=0 AND is_developer=0 AND client_platform!='editor') OR ('${test_data}'='only' AND (is_development_build=1 OR is_developer=1 OR client_platform='editor')))),\n    ids AS (SELECT user_id FROM s UNION SELECT user_id FROM e0),\n    states AS (SELECT *,ROW_NUMBER() OVER(PARTITION BY user_id ORDER BY julianday(occurred_at) DESC,sequence DESC,event_id DESC) rn\n        FROM e0 WHERE event_type='player_state_snapshot'),\n    legacy AS (SELECT g.*,ROW_NUMBER() OVER(PARTITION BY user_id ORDER BY julianday(real_time_started_iso) DESC,imported_at DESC) rn\n        FROM gameplay_sessions g WHERE EXISTS(SELECT 1 FROM s WHERE s.user_id=g.user_id AND s.session_id=g.session_id)),\n    days AS (SELECT user_id,COUNT(*) play_days FROM\n        (SELECT DISTINCT user_id,archive_id,game_day FROM e0 WHERE NULLIF(archive_id,'') IS NOT NULL AND game_day IS NOT NULL) GROUP BY user_id),\n    p AS (SELECT ids.user_id,COALESCE(NULLIF(json_extract(st.payload_json,'$.nickname'),''),NULLIF(u.nickname,''),NULLIF(u.player_name,''),ids.user_id) nickname,\n        (SELECT SUM(duration_sec) FROM s WHERE s.user_id=ids.user_id) play_seconds,\n        (SELECT first_entry_at FROM analytics_first_entry f WHERE f.user_id=ids.user_id AND f.cohort_scope=CASE WHEN '${test_data}' IN ('include','only') OR ('${test_data}'='auto' AND ('editor' IN (${client_platform:sqlstring}) OR '__all__' IN (${client_platform:sqlstring}))) THEN 'all' ELSE 'production' END) first_login,\n        (SELECT MAX(started_at) FROM s WHERE s.user_id=ids.user_id) latest_login,\n        d.play_days,COALESCE(json_extract(st.payload_json,'$.money'),l.money_end,l.money_start) money,\n        COALESCE(json_extract(st.payload_json,'$.island_level'),l.island_level_max) island_level,\n        json_array_length(json_extract(st.payload_json,'$.cats')) cat_count,\n        st.payload_json state,st.occurred_at state_at,\n        json_extract(st.payload_json,'$.cat_house_count') house_count,\n        json_extract(st.payload_json,'$.cat_house_capacity') house_capacity,\n        json_extract(st.payload_json,'$.cat_house_occupied') house_occupied\n        FROM ids LEFT JOIN user_sessions u USING(user_id) LEFT JOIN states st ON st.user_id=ids.user_id AND st.rn=1\n        LEFT JOIN legacy l ON l.user_id=ids.user_id AND l.rn=1 LEFT JOIN days d USING(user_id)),\n    e AS (SELECT * FROM e0 WHERE ('${behavior_period}'='all' OR julianday(occurred_at) >= julianday(${__from}/1000,'unixepoch') AND julianday(occurred_at) < julianday(${__to}/1000,'unixepoch'))\n        OR (occurred_at IS NULL AND '${behavior_period}'='all')),\n    players AS (SELECT * FROM p WHERE 1),\n    events AS (SELECT * FROM e WHERE 1) SELECT AVG(json_extract(payload_json,'$.remaining_before_reward')) 平均剩余点数,100.0*AVG(json_extract(payload_json,'$.remaining_before_reward')>0) 有剩余比例,COUNT(*) 升级样本数 FROM events WHERE event_type='cat_level_up'"
        }
      ],
      "fieldConfig": {
        "defaults": {
          "unit": "short",
          "custom": {
            "filterable": true
          },
          "noValue": "未采集"
        },
        "overrides": [
          {
            "matcher": {
              "id": "byName",
              "options": "user_id"
            },
            "properties": [
              {
                "id": "links",
                "value": [
                  {
                    "title": "查看玩家旅程",
                    "url": "/d/gameplay-player-detail?${__url_time_range}&${client_platform:queryparam}&${release_version:queryparam}&${test_data:queryparam}&var-user_id=${__data.fields.user_id}&var-user_id=${__value.raw}"
                  }
                ]
              }
            ]
          },
          {
            "matcher": {
              "id": "byName",
              "options": "session_id"
            },
            "properties": [
              {
                "id": "links",
                "value": [
                  {
                    "title": "查看玩家旅程",
                    "url": "/d/gameplay-player-detail?${__url_time_range}&${client_platform:queryparam}&${release_version:queryparam}&${test_data:queryparam}&var-user_id=${__data.fields.user_id}&var-session_id=${__value.raw}"
                  }
                ]
              }
            ]
          },
          {
            "matcher": {
              "id": "byName",
              "options": "llm_request_id"
            },
            "properties": [
              {
                "id": "links",
                "value": [
                  {
                    "title": "查看玩家旅程",
                    "url": "/d/gameplay-player-detail?${__url_time_range}&${client_platform:queryparam}&${release_version:queryparam}&${test_data:queryparam}&var-user_id=${__data.fields.user_id}&var-llm_request_id=${__value.raw}"
                  }
                ]
              }
            ]
          }
        ]
      },
      "options": {
        "showHeader": true,
        "cellHeight": "sm"
      },
      "gridPos": {
        "x": 12,
        "y": 82,
        "w": 12,
        "h": 9
      }
    },
    {
      "id": 145,
      "title": "打字前最近猫行为 Top 5",
      "type": "table",
      "datasource": {
        "type": "frser-sqlite-datasource",
        "uid": "${DS_SQLITE}"
      },
      "description": "累计按玩家去重；当前状态取最新快照。缺失显示未采集，数值零保留。行为可切换期间，日期为北京时间。",
      "targets": [
        {
          "refId": "A",
          "queryType": "table",
          "queryText": "WITH e0 AS NOT MATERIALIZED (SELECT * FROM analytics_events WHERE ('__all__' IN (${client_platform:sqlstring}) OR client_platform IN (${client_platform:sqlstring})) AND ('__all__' IN (${release_version:sqlstring}) OR COALESCE(NULLIF(release_version,''),'__empty__') IN (${release_version:sqlstring})) AND ('${test_data}'='include' OR ('${test_data}'='auto' AND (client_platform='editor' OR (is_development_build=0 AND is_developer=0))) OR ('${test_data}'='exclude' AND is_development_build=0 AND is_developer=0 AND client_platform!='editor') OR ('${test_data}'='only' AND (is_development_build=1 OR is_developer=1 OR client_platform='editor')))),\n    s AS MATERIALIZED (SELECT * FROM analytics_sessions WHERE ('__all__' IN (${client_platform:sqlstring}) OR client_platform IN (${client_platform:sqlstring})) AND ('__all__' IN (${release_version:sqlstring}) OR COALESCE(NULLIF(release_version,''),'__empty__') IN (${release_version:sqlstring})) AND ('${test_data}'='include' OR ('${test_data}'='auto' AND (client_platform='editor' OR (is_development_build=0 AND is_developer=0))) OR ('${test_data}'='exclude' AND is_development_build=0 AND is_developer=0 AND client_platform!='editor') OR ('${test_data}'='only' AND (is_development_build=1 OR is_developer=1 OR client_platform='editor')))),\n    ids AS (SELECT user_id FROM s UNION SELECT user_id FROM e0),\n    states AS (SELECT *,ROW_NUMBER() OVER(PARTITION BY user_id ORDER BY julianday(occurred_at) DESC,sequence DESC,event_id DESC) rn\n        FROM e0 WHERE event_type='player_state_snapshot'),\n    legacy AS (SELECT g.*,ROW_NUMBER() OVER(PARTITION BY user_id ORDER BY julianday(real_time_started_iso) DESC,imported_at DESC) rn\n        FROM gameplay_sessions g WHERE EXISTS(SELECT 1 FROM s WHERE s.user_id=g.user_id AND s.session_id=g.session_id)),\n    days AS (SELECT user_id,COUNT(*) play_days FROM\n        (SELECT DISTINCT user_id,archive_id,game_day FROM e0 WHERE NULLIF(archive_id,'') IS NOT NULL AND game_day IS NOT NULL) GROUP BY user_id),\n    p AS (SELECT ids.user_id,COALESCE(NULLIF(json_extract(st.payload_json,'$.nickname'),''),NULLIF(u.nickname,''),NULLIF(u.player_name,''),ids.user_id) nickname,\n        (SELECT SUM(duration_sec) FROM s WHERE s.user_id=ids.user_id) play_seconds,\n        (SELECT first_entry_at FROM analytics_first_entry f WHERE f.user_id=ids.user_id AND f.cohort_scope=CASE WHEN '${test_data}' IN ('include','only') OR ('${test_data}'='auto' AND ('editor' IN (${client_platform:sqlstring}) OR '__all__' IN (${client_platform:sqlstring}))) THEN 'all' ELSE 'production' END) first_login,\n        (SELECT MAX(started_at) FROM s WHERE s.user_id=ids.user_id) latest_login,\n        d.play_days,COALESCE(json_extract(st.payload_json,'$.money'),l.money_end,l.money_start) money,\n        COALESCE(json_extract(st.payload_json,'$.island_level'),l.island_level_max) island_level,\n        json_array_length(json_extract(st.payload_json,'$.cats')) cat_count,\n        st.payload_json state,st.occurred_at state_at,\n        json_extract(st.payload_json,'$.cat_house_count') house_count,\n        json_extract(st.payload_json,'$.cat_house_capacity') house_capacity,\n        json_extract(st.payload_json,'$.cat_house_occupied') house_occupied\n        FROM ids LEFT JOIN user_sessions u USING(user_id) LEFT JOIN states st ON st.user_id=ids.user_id AND st.rn=1\n        LEFT JOIN legacy l ON l.user_id=ids.user_id AND l.rn=1 LEFT JOIN days d USING(user_id)),\n    e AS (SELECT * FROM e0 WHERE ('${behavior_period}'='all' OR julianday(occurred_at) >= julianday(${__from}/1000,'unixepoch') AND julianday(occurred_at) < julianday(${__to}/1000,'unixepoch'))\n        OR (occurred_at IS NULL AND '${behavior_period}'='all')),\n    players AS (SELECT * FROM p WHERE 1),\n    events AS (SELECT * FROM e WHERE 1) SELECT COALESCE(json_extract(payload_json,'$.preceding.action'),'未观察到近期可见行为') 行为,COUNT(*) 开始输入次数 FROM events WHERE event_type='player_typing_started' GROUP BY 1 ORDER BY 开始输入次数 DESC,行为 LIMIT 5",
          "rawQueryText": "WITH e0 AS NOT MATERIALIZED (SELECT * FROM analytics_events WHERE ('__all__' IN (${client_platform:sqlstring}) OR client_platform IN (${client_platform:sqlstring})) AND ('__all__' IN (${release_version:sqlstring}) OR COALESCE(NULLIF(release_version,''),'__empty__') IN (${release_version:sqlstring})) AND ('${test_data}'='include' OR ('${test_data}'='auto' AND (client_platform='editor' OR (is_development_build=0 AND is_developer=0))) OR ('${test_data}'='exclude' AND is_development_build=0 AND is_developer=0 AND client_platform!='editor') OR ('${test_data}'='only' AND (is_development_build=1 OR is_developer=1 OR client_platform='editor')))),\n    s AS MATERIALIZED (SELECT * FROM analytics_sessions WHERE ('__all__' IN (${client_platform:sqlstring}) OR client_platform IN (${client_platform:sqlstring})) AND ('__all__' IN (${release_version:sqlstring}) OR COALESCE(NULLIF(release_version,''),'__empty__') IN (${release_version:sqlstring})) AND ('${test_data}'='include' OR ('${test_data}'='auto' AND (client_platform='editor' OR (is_development_build=0 AND is_developer=0))) OR ('${test_data}'='exclude' AND is_development_build=0 AND is_developer=0 AND client_platform!='editor') OR ('${test_data}'='only' AND (is_development_build=1 OR is_developer=1 OR client_platform='editor')))),\n    ids AS (SELECT user_id FROM s UNION SELECT user_id FROM e0),\n    states AS (SELECT *,ROW_NUMBER() OVER(PARTITION BY user_id ORDER BY julianday(occurred_at) DESC,sequence DESC,event_id DESC) rn\n        FROM e0 WHERE event_type='player_state_snapshot'),\n    legacy AS (SELECT g.*,ROW_NUMBER() OVER(PARTITION BY user_id ORDER BY julianday(real_time_started_iso) DESC,imported_at DESC) rn\n        FROM gameplay_sessions g WHERE EXISTS(SELECT 1 FROM s WHERE s.user_id=g.user_id AND s.session_id=g.session_id)),\n    days AS (SELECT user_id,COUNT(*) play_days FROM\n        (SELECT DISTINCT user_id,archive_id,game_day FROM e0 WHERE NULLIF(archive_id,'') IS NOT NULL AND game_day IS NOT NULL) GROUP BY user_id),\n    p AS (SELECT ids.user_id,COALESCE(NULLIF(json_extract(st.payload_json,'$.nickname'),''),NULLIF(u.nickname,''),NULLIF(u.player_name,''),ids.user_id) nickname,\n        (SELECT SUM(duration_sec) FROM s WHERE s.user_id=ids.user_id) play_seconds,\n        (SELECT first_entry_at FROM analytics_first_entry f WHERE f.user_id=ids.user_id AND f.cohort_scope=CASE WHEN '${test_data}' IN ('include','only') OR ('${test_data}'='auto' AND ('editor' IN (${client_platform:sqlstring}) OR '__all__' IN (${client_platform:sqlstring}))) THEN 'all' ELSE 'production' END) first_login,\n        (SELECT MAX(started_at) FROM s WHERE s.user_id=ids.user_id) latest_login,\n        d.play_days,COALESCE(json_extract(st.payload_json,'$.money'),l.money_end,l.money_start) money,\n        COALESCE(json_extract(st.payload_json,'$.island_level'),l.island_level_max) island_level,\n        json_array_length(json_extract(st.payload_json,'$.cats')) cat_count,\n        st.payload_json state,st.occurred_at state_at,\n        json_extract(st.payload_json,'$.cat_house_count') house_count,\n        json_extract(st.payload_json,'$.cat_house_capacity') house_capacity,\n        json_extract(st.payload_json,'$.cat_house_occupied') house_occupied\n        FROM ids LEFT JOIN user_sessions u USING(user_id) LEFT JOIN states st ON st.user_id=ids.user_id AND st.rn=1\n        LEFT JOIN legacy l ON l.user_id=ids.user_id AND l.rn=1 LEFT JOIN days d USING(user_id)),\n    e AS (SELECT * FROM e0 WHERE ('${behavior_period}'='all' OR julianday(occurred_at) >= julianday(${__from}/1000,'unixepoch') AND julianday(occurred_at) < julianday(${__to}/1000,'unixepoch'))\n        OR (occurred_at IS NULL AND '${behavior_period}'='all')),\n    players AS (SELECT * FROM p WHERE 1),\n    events AS (SELECT * FROM e WHERE 1) SELECT COALESCE(json_extract(payload_json,'$.preceding.action'),'未观察到近期可见行为') 行为,COUNT(*) 开始输入次数 FROM events WHERE event_type='player_typing_started' GROUP BY 1 ORDER BY 开始输入次数 DESC,行为 LIMIT 5"
        }
      ],
      "fieldConfig": {
        "defaults": {
          "unit": "short",
          "custom": {
            "filterable": true
          },
          "noValue": "未采集"
        },
        "overrides": [
          {
            "matcher": {
              "id": "byName",
              "options": "user_id"
            },
            "properties": [
              {
                "id": "links",
                "value": [
                  {
                    "title": "查看玩家旅程",
                    "url": "/d/gameplay-player-detail?${__url_time_range}&${client_platform:queryparam}&${release_version:queryparam}&${test_data:queryparam}&var-user_id=${__data.fields.user_id}&var-user_id=${__value.raw}"
                  }
                ]
              }
            ]
          },
          {
            "matcher": {
              "id": "byName",
              "options": "session_id"
            },
            "properties": [
              {
                "id": "links",
                "value": [
                  {
                    "title": "查看玩家旅程",
                    "url": "/d/gameplay-player-detail?${__url_time_range}&${client_platform:queryparam}&${release_version:queryparam}&${test_data:queryparam}&var-user_id=${__data.fields.user_id}&var-session_id=${__value.raw}"
                  }
                ]
              }
            ]
          },
          {
            "matcher": {
              "id": "byName",
              "options": "llm_request_id"
            },
            "properties": [
              {
                "id": "links",
                "value": [
                  {
                    "title": "查看玩家旅程",
                    "url": "/d/gameplay-player-detail?${__url_time_range}&${client_platform:queryparam}&${release_version:queryparam}&${test_data:queryparam}&var-user_id=${__data.fields.user_id}&var-llm_request_id=${__value.raw}"
                  }
                ]
              }
            ]
          }
        ]
      },
      "options": {
        "showHeader": true,
        "cellHeight": "sm"
      },
      "gridPos": {
        "x": 0,
        "y": 91,
        "w": 12,
        "h": 9
      }
    },
    {
      "id": 150,
      "title": "猫各工作等级分布",
      "type": "table",
      "datasource": {
        "type": "frser-sqlite-datasource",
        "uid": "${DS_SQLITE}"
      },
      "description": "累计按玩家去重；当前状态取最新快照。缺失显示未采集，数值零保留。行为可切换期间，日期为北京时间。",
      "targets": [
        {
          "refId": "A",
          "queryType": "table",
          "queryText": "WITH e0 AS NOT MATERIALIZED (SELECT * FROM analytics_events WHERE ('__all__' IN (${client_platform:sqlstring}) OR client_platform IN (${client_platform:sqlstring})) AND ('__all__' IN (${release_version:sqlstring}) OR COALESCE(NULLIF(release_version,''),'__empty__') IN (${release_version:sqlstring})) AND ('${test_data}'='include' OR ('${test_data}'='auto' AND (client_platform='editor' OR (is_development_build=0 AND is_developer=0))) OR ('${test_data}'='exclude' AND is_development_build=0 AND is_developer=0 AND client_platform!='editor') OR ('${test_data}'='only' AND (is_development_build=1 OR is_developer=1 OR client_platform='editor')))),\n    s AS MATERIALIZED (SELECT * FROM analytics_sessions WHERE ('__all__' IN (${client_platform:sqlstring}) OR client_platform IN (${client_platform:sqlstring})) AND ('__all__' IN (${release_version:sqlstring}) OR COALESCE(NULLIF(release_version,''),'__empty__') IN (${release_version:sqlstring})) AND ('${test_data}'='include' OR ('${test_data}'='auto' AND (client_platform='editor' OR (is_development_build=0 AND is_developer=0))) OR ('${test_data}'='exclude' AND is_development_build=0 AND is_developer=0 AND client_platform!='editor') OR ('${test_data}'='only' AND (is_development_build=1 OR is_developer=1 OR client_platform='editor')))),\n    ids AS (SELECT user_id FROM s UNION SELECT user_id FROM e0),\n    states AS (SELECT *,ROW_NUMBER() OVER(PARTITION BY user_id ORDER BY julianday(occurred_at) DESC,sequence DESC,event_id DESC) rn\n        FROM e0 WHERE event_type='player_state_snapshot'),\n    legacy AS (SELECT g.*,ROW_NUMBER() OVER(PARTITION BY user_id ORDER BY julianday(real_time_started_iso) DESC,imported_at DESC) rn\n        FROM gameplay_sessions g WHERE EXISTS(SELECT 1 FROM s WHERE s.user_id=g.user_id AND s.session_id=g.session_id)),\n    days AS (SELECT user_id,COUNT(*) play_days FROM\n        (SELECT DISTINCT user_id,archive_id,game_day FROM e0 WHERE NULLIF(archive_id,'') IS NOT NULL AND game_day IS NOT NULL) GROUP BY user_id),\n    p AS (SELECT ids.user_id,COALESCE(NULLIF(json_extract(st.payload_json,'$.nickname'),''),NULLIF(u.nickname,''),NULLIF(u.player_name,''),ids.user_id) nickname,\n        (SELECT SUM(duration_sec) FROM s WHERE s.user_id=ids.user_id) play_seconds,\n        (SELECT first_entry_at FROM analytics_first_entry f WHERE f.user_id=ids.user_id AND f.cohort_scope=CASE WHEN '${test_data}' IN ('include','only') OR ('${test_data}'='auto' AND ('editor' IN (${client_platform:sqlstring}) OR '__all__' IN (${client_platform:sqlstring}))) THEN 'all' ELSE 'production' END) first_login,\n        (SELECT MAX(started_at) FROM s WHERE s.user_id=ids.user_id) latest_login,\n        d.play_days,COALESCE(json_extract(st.payload_json,'$.money'),l.money_end,l.money_start) money,\n        COALESCE(json_extract(st.payload_json,'$.island_level'),l.island_level_max) island_level,\n        json_array_length(json_extract(st.payload_json,'$.cats')) cat_count,\n        st.payload_json state,st.occurred_at state_at,\n        json_extract(st.payload_json,'$.cat_house_count') house_count,\n        json_extract(st.payload_json,'$.cat_house_capacity') house_capacity,\n        json_extract(st.payload_json,'$.cat_house_occupied') house_occupied\n        FROM ids LEFT JOIN user_sessions u USING(user_id) LEFT JOIN states st ON st.user_id=ids.user_id AND st.rn=1\n        LEFT JOIN legacy l ON l.user_id=ids.user_id AND l.rn=1 LEFT JOIN days d USING(user_id)),\n    e AS (SELECT * FROM e0 WHERE ('${behavior_period}'='all' OR julianday(occurred_at) >= julianday(${__from}/1000,'unixepoch') AND julianday(occurred_at) < julianday(${__to}/1000,'unixepoch'))\n        OR (occurred_at IS NULL AND '${behavior_period}'='all')),\n    players AS (SELECT * FROM p WHERE 1),\n    events AS (SELECT * FROM e WHERE 1) SELECT sk.key 工作,sk.value 等级,COUNT(*) 猫数量,100.0*COUNT(*)/SUM(COUNT(*)) OVER(PARTITION BY sk.key) 比例 FROM players,json_each(state,'$.cats') c,json_each(c.value,'$.skills') sk GROUP BY 1,2",
          "rawQueryText": "WITH e0 AS NOT MATERIALIZED (SELECT * FROM analytics_events WHERE ('__all__' IN (${client_platform:sqlstring}) OR client_platform IN (${client_platform:sqlstring})) AND ('__all__' IN (${release_version:sqlstring}) OR COALESCE(NULLIF(release_version,''),'__empty__') IN (${release_version:sqlstring})) AND ('${test_data}'='include' OR ('${test_data}'='auto' AND (client_platform='editor' OR (is_development_build=0 AND is_developer=0))) OR ('${test_data}'='exclude' AND is_development_build=0 AND is_developer=0 AND client_platform!='editor') OR ('${test_data}'='only' AND (is_development_build=1 OR is_developer=1 OR client_platform='editor')))),\n    s AS MATERIALIZED (SELECT * FROM analytics_sessions WHERE ('__all__' IN (${client_platform:sqlstring}) OR client_platform IN (${client_platform:sqlstring})) AND ('__all__' IN (${release_version:sqlstring}) OR COALESCE(NULLIF(release_version,''),'__empty__') IN (${release_version:sqlstring})) AND ('${test_data}'='include' OR ('${test_data}'='auto' AND (client_platform='editor' OR (is_development_build=0 AND is_developer=0))) OR ('${test_data}'='exclude' AND is_development_build=0 AND is_developer=0 AND client_platform!='editor') OR ('${test_data}'='only' AND (is_development_build=1 OR is_developer=1 OR client_platform='editor')))),\n    ids AS (SELECT user_id FROM s UNION SELECT user_id FROM e0),\n    states AS (SELECT *,ROW_NUMBER() OVER(PARTITION BY user_id ORDER BY julianday(occurred_at) DESC,sequence DESC,event_id DESC) rn\n        FROM e0 WHERE event_type='player_state_snapshot'),\n    legacy AS (SELECT g.*,ROW_NUMBER() OVER(PARTITION BY user_id ORDER BY julianday(real_time_started_iso) DESC,imported_at DESC) rn\n        FROM gameplay_sessions g WHERE EXISTS(SELECT 1 FROM s WHERE s.user_id=g.user_id AND s.session_id=g.session_id)),\n    days AS (SELECT user_id,COUNT(*) play_days FROM\n        (SELECT DISTINCT user_id,archive_id,game_day FROM e0 WHERE NULLIF(archive_id,'') IS NOT NULL AND game_day IS NOT NULL) GROUP BY user_id),\n    p AS (SELECT ids.user_id,COALESCE(NULLIF(json_extract(st.payload_json,'$.nickname'),''),NULLIF(u.nickname,''),NULLIF(u.player_name,''),ids.user_id) nickname,\n        (SELECT SUM(duration_sec) FROM s WHERE s.user_id=ids.user_id) play_seconds,\n        (SELECT first_entry_at FROM analytics_first_entry f WHERE f.user_id=ids.user_id AND f.cohort_scope=CASE WHEN '${test_data}' IN ('include','only') OR ('${test_data}'='auto' AND ('editor' IN (${client_platform:sqlstring}) OR '__all__' IN (${client_platform:sqlstring}))) THEN 'all' ELSE 'production' END) first_login,\n        (SELECT MAX(started_at) FROM s WHERE s.user_id=ids.user_id) latest_login,\n        d.play_days,COALESCE(json_extract(st.payload_json,'$.money'),l.money_end,l.money_start) money,\n        COALESCE(json_extract(st.payload_json,'$.island_level'),l.island_level_max) island_level,\n        json_array_length(json_extract(st.payload_json,'$.cats')) cat_count,\n        st.payload_json state,st.occurred_at state_at,\n        json_extract(st.payload_json,'$.cat_house_count') house_count,\n        json_extract(st.payload_json,'$.cat_house_capacity') house_capacity,\n        json_extract(st.payload_json,'$.cat_house_occupied') house_occupied\n        FROM ids LEFT JOIN user_sessions u USING(user_id) LEFT JOIN states st ON st.user_id=ids.user_id AND st.rn=1\n        LEFT JOIN legacy l ON l.user_id=ids.user_id AND l.rn=1 LEFT JOIN days d USING(user_id)),\n    e AS (SELECT * FROM e0 WHERE ('${behavior_period}'='all' OR julianday(occurred_at) >= julianday(${__from}/1000,'unixepoch') AND julianday(occurred_at) < julianday(${__to}/1000,'unixepoch'))\n        OR (occurred_at IS NULL AND '${behavior_period}'='all')),\n    players AS (SELECT * FROM p WHERE 1),\n    events AS (SELECT * FROM e WHERE 1) SELECT sk.key 工作,sk.value 等级,COUNT(*) 猫数量,100.0*COUNT(*)/SUM(COUNT(*)) OVER(PARTITION BY sk.key) 比例 FROM players,json_each(state,'$.cats') c,json_each(c.value,'$.skills') sk GROUP BY 1,2"
        }
      ],
      "fieldConfig": {
        "defaults": {
          "unit": "short",
          "custom": {
            "filterable": true
          },
          "noValue": "未采集"
        },
        "overrides": [
          {
            "matcher": {
              "id": "byName",
              "options": "user_id"
            },
            "properties": [
              {
                "id": "links",
                "value": [
                  {
                    "title": "查看玩家旅程",
                    "url": "/d/gameplay-player-detail?${__url_time_range}&${client_platform:queryparam}&${release_version:queryparam}&${test_data:queryparam}&var-user_id=${__data.fields.user_id}&var-user_id=${__value.raw}"
                  }
                ]
              }
            ]
          },
          {
            "matcher": {
              "id": "byName",
              "options": "session_id"
            },
            "properties": [
              {
                "id": "links",
                "value": [
                  {
                    "title": "查看玩家旅程",
                    "url": "/d/gameplay-player-detail?${__url_time_range}&${client_platform:queryparam}&${release_version:queryparam}&${test_data:queryparam}&var-user_id=${__data.fields.user_id}&var-session_id=${__value.raw}"
                  }
                ]
              }
            ]
          },
          {
            "matcher": {
              "id": "byName",
              "options": "llm_request_id"
            },
            "properties": [
              {
                "id": "links",
                "value": [
                  {
                    "title": "查看玩家旅程",
                    "url": "/d/gameplay-player-detail?${__url_time_range}&${client_platform:queryparam}&${release_version:queryparam}&${test_data:queryparam}&var-user_id=${__data.fields.user_id}&var-llm_request_id=${__value.raw}"
                  }
                ]
              }
            ]
          }
        ]
      },
      "options": {
        "showHeader": true,
        "cellHeight": "sm"
      },
      "gridPos": {
        "x": 12,
        "y": 91,
        "w": 12,
        "h": 9
      }
    },
    {
      "id": 151,
      "title": "猫舍使用率与空位",
      "type": "table",
      "datasource": {
        "type": "frser-sqlite-datasource",
        "uid": "${DS_SQLITE}"
      },
      "description": "从首次有效采集起观察；未观察到不代表旧版本从未放过。当前每玩家有一个个人猫舍，升级扩容量。",
      "targets": [
        {
          "refId": "A",
          "queryType": "table",
          "queryText": "WITH e0 AS NOT MATERIALIZED (SELECT * FROM analytics_events WHERE ('__all__' IN (${client_platform:sqlstring}) OR client_platform IN (${client_platform:sqlstring})) AND ('__all__' IN (${release_version:sqlstring}) OR COALESCE(NULLIF(release_version,''),'__empty__') IN (${release_version:sqlstring})) AND ('${test_data}'='include' OR ('${test_data}'='auto' AND (client_platform='editor' OR (is_development_build=0 AND is_developer=0))) OR ('${test_data}'='exclude' AND is_development_build=0 AND is_developer=0 AND client_platform!='editor') OR ('${test_data}'='only' AND (is_development_build=1 OR is_developer=1 OR client_platform='editor')))),\n    s AS MATERIALIZED (SELECT * FROM analytics_sessions WHERE ('__all__' IN (${client_platform:sqlstring}) OR client_platform IN (${client_platform:sqlstring})) AND ('__all__' IN (${release_version:sqlstring}) OR COALESCE(NULLIF(release_version,''),'__empty__') IN (${release_version:sqlstring})) AND ('${test_data}'='include' OR ('${test_data}'='auto' AND (client_platform='editor' OR (is_development_build=0 AND is_developer=0))) OR ('${test_data}'='exclude' AND is_development_build=0 AND is_developer=0 AND client_platform!='editor') OR ('${test_data}'='only' AND (is_development_build=1 OR is_developer=1 OR client_platform='editor')))),\n    ids AS (SELECT user_id FROM s UNION SELECT user_id FROM e0),\n    states AS (SELECT *,ROW_NUMBER() OVER(PARTITION BY user_id ORDER BY julianday(occurred_at) DESC,sequence DESC,event_id DESC) rn\n        FROM e0 WHERE event_type='player_state_snapshot'),\n    legacy AS (SELECT g.*,ROW_NUMBER() OVER(PARTITION BY user_id ORDER BY julianday(real_time_started_iso) DESC,imported_at DESC) rn\n        FROM gameplay_sessions g WHERE EXISTS(SELECT 1 FROM s WHERE s.user_id=g.user_id AND s.session_id=g.session_id)),\n    days AS (SELECT user_id,COUNT(*) play_days FROM\n        (SELECT DISTINCT user_id,archive_id,game_day FROM e0 WHERE NULLIF(archive_id,'') IS NOT NULL AND game_day IS NOT NULL) GROUP BY user_id),\n    p AS (SELECT ids.user_id,COALESCE(NULLIF(json_extract(st.payload_json,'$.nickname'),''),NULLIF(u.nickname,''),NULLIF(u.player_name,''),ids.user_id) nickname,\n        (SELECT SUM(duration_sec) FROM s WHERE s.user_id=ids.user_id) play_seconds,\n        (SELECT first_entry_at FROM analytics_first_entry f WHERE f.user_id=ids.user_id AND f.cohort_scope=CASE WHEN '${test_data}' IN ('include','only') OR ('${test_data}'='auto' AND ('editor' IN (${client_platform:sqlstring}) OR '__all__' IN (${client_platform:sqlstring}))) THEN 'all' ELSE 'production' END) first_login,\n        (SELECT MAX(started_at) FROM s WHERE s.user_id=ids.user_id) latest_login,\n        d.play_days,COALESCE(json_extract(st.payload_json,'$.money'),l.money_end,l.money_start) money,\n        COALESCE(json_extract(st.payload_json,'$.island_level'),l.island_level_max) island_level,\n        json_array_length(json_extract(st.payload_json,'$.cats')) cat_count,\n        st.payload_json state,st.occurred_at state_at,\n        json_extract(st.payload_json,'$.cat_house_count') house_count,\n        json_extract(st.payload_json,'$.cat_house_capacity') house_capacity,\n        json_extract(st.payload_json,'$.cat_house_occupied') house_occupied\n        FROM ids LEFT JOIN user_sessions u USING(user_id) LEFT JOIN states st ON st.user_id=ids.user_id AND st.rn=1\n        LEFT JOIN legacy l ON l.user_id=ids.user_id AND l.rn=1 LEFT JOIN days d USING(user_id)),\n    e AS (SELECT * FROM e0 WHERE ('${behavior_period}'='all' OR julianday(occurred_at) >= julianday(${__from}/1000,'unixepoch') AND julianday(occurred_at) < julianday(${__to}/1000,'unixepoch'))\n        OR (occurred_at IS NULL AND '${behavior_period}'='all')),\n    players AS (SELECT * FROM p WHERE 1),\n    events AS (SELECT * FROM e WHERE 1) SELECT SUM(used=1) 放过猫玩家数,SUM(used=0) 未观察到放猫玩家数,\n            100.0*AVG(used) 观察期使用率,AVG(house_capacity-house_occupied) 每玩家平均空位,\n            AVG(CASE WHEN house_count>0 THEN house_capacity-house_occupied END) 有猫舍玩家平均空位,COUNT(house_capacity) 有效快照数\n            FROM (SELECT p.*,EXISTS(SELECT 1 FROM e0 WHERE e0.user_id=p.user_id AND event_type='cat_housing_changed' AND json_extract(payload_json,'$.location')='cat_house') OR house_occupied>0 used FROM players p WHERE house_capacity IS NOT NULL)",
          "rawQueryText": "WITH e0 AS NOT MATERIALIZED (SELECT * FROM analytics_events WHERE ('__all__' IN (${client_platform:sqlstring}) OR client_platform IN (${client_platform:sqlstring})) AND ('__all__' IN (${release_version:sqlstring}) OR COALESCE(NULLIF(release_version,''),'__empty__') IN (${release_version:sqlstring})) AND ('${test_data}'='include' OR ('${test_data}'='auto' AND (client_platform='editor' OR (is_development_build=0 AND is_developer=0))) OR ('${test_data}'='exclude' AND is_development_build=0 AND is_developer=0 AND client_platform!='editor') OR ('${test_data}'='only' AND (is_development_build=1 OR is_developer=1 OR client_platform='editor')))),\n    s AS MATERIALIZED (SELECT * FROM analytics_sessions WHERE ('__all__' IN (${client_platform:sqlstring}) OR client_platform IN (${client_platform:sqlstring})) AND ('__all__' IN (${release_version:sqlstring}) OR COALESCE(NULLIF(release_version,''),'__empty__') IN (${release_version:sqlstring})) AND ('${test_data}'='include' OR ('${test_data}'='auto' AND (client_platform='editor' OR (is_development_build=0 AND is_developer=0))) OR ('${test_data}'='exclude' AND is_development_build=0 AND is_developer=0 AND client_platform!='editor') OR ('${test_data}'='only' AND (is_development_build=1 OR is_developer=1 OR client_platform='editor')))),\n    ids AS (SELECT user_id FROM s UNION SELECT user_id FROM e0),\n    states AS (SELECT *,ROW_NUMBER() OVER(PARTITION BY user_id ORDER BY julianday(occurred_at) DESC,sequence DESC,event_id DESC) rn\n        FROM e0 WHERE event_type='player_state_snapshot'),\n    legacy AS (SELECT g.*,ROW_NUMBER() OVER(PARTITION BY user_id ORDER BY julianday(real_time_started_iso) DESC,imported_at DESC) rn\n        FROM gameplay_sessions g WHERE EXISTS(SELECT 1 FROM s WHERE s.user_id=g.user_id AND s.session_id=g.session_id)),\n    days AS (SELECT user_id,COUNT(*) play_days FROM\n        (SELECT DISTINCT user_id,archive_id,game_day FROM e0 WHERE NULLIF(archive_id,'') IS NOT NULL AND game_day IS NOT NULL) GROUP BY user_id),\n    p AS (SELECT ids.user_id,COALESCE(NULLIF(json_extract(st.payload_json,'$.nickname'),''),NULLIF(u.nickname,''),NULLIF(u.player_name,''),ids.user_id) nickname,\n        (SELECT SUM(duration_sec) FROM s WHERE s.user_id=ids.user_id) play_seconds,\n        (SELECT first_entry_at FROM analytics_first_entry f WHERE f.user_id=ids.user_id AND f.cohort_scope=CASE WHEN '${test_data}' IN ('include','only') OR ('${test_data}'='auto' AND ('editor' IN (${client_platform:sqlstring}) OR '__all__' IN (${client_platform:sqlstring}))) THEN 'all' ELSE 'production' END) first_login,\n        (SELECT MAX(started_at) FROM s WHERE s.user_id=ids.user_id) latest_login,\n        d.play_days,COALESCE(json_extract(st.payload_json,'$.money'),l.money_end,l.money_start) money,\n        COALESCE(json_extract(st.payload_json,'$.island_level'),l.island_level_max) island_level,\n        json_array_length(json_extract(st.payload_json,'$.cats')) cat_count,\n        st.payload_json state,st.occurred_at state_at,\n        json_extract(st.payload_json,'$.cat_house_count') house_count,\n        json_extract(st.payload_json,'$.cat_house_capacity') house_capacity,\n        json_extract(st.payload_json,'$.cat_house_occupied') house_occupied\n        FROM ids LEFT JOIN user_sessions u USING(user_id) LEFT JOIN states st ON st.user_id=ids.user_id AND st.rn=1\n        LEFT JOIN legacy l ON l.user_id=ids.user_id AND l.rn=1 LEFT JOIN days d USING(user_id)),\n    e AS (SELECT * FROM e0 WHERE ('${behavior_period}'='all' OR julianday(occurred_at) >= julianday(${__from}/1000,'unixepoch') AND julianday(occurred_at) < julianday(${__to}/1000,'unixepoch'))\n        OR (occurred_at IS NULL AND '${behavior_period}'='all')),\n    players AS (SELECT * FROM p WHERE 1),\n    events AS (SELECT * FROM e WHERE 1) SELECT SUM(used=1) 放过猫玩家数,SUM(used=0) 未观察到放猫玩家数,\n            100.0*AVG(used) 观察期使用率,AVG(house_capacity-house_occupied) 每玩家平均空位,\n            AVG(CASE WHEN house_count>0 THEN house_capacity-house_occupied END) 有猫舍玩家平均空位,COUNT(house_capacity) 有效快照数\n            FROM (SELECT p.*,EXISTS(SELECT 1 FROM e0 WHERE e0.user_id=p.user_id AND event_type='cat_housing_changed' AND json_extract(payload_json,'$.location')='cat_house') OR house_occupied>0 used FROM players p WHERE house_capacity IS NOT NULL)"
        }
      ],
      "fieldConfig": {
        "defaults": {
          "unit": "short",
          "custom": {
            "filterable": true
          },
          "noValue": "未采集"
        },
        "overrides": [
          {
            "matcher": {
              "id": "byName",
              "options": "user_id"
            },
            "properties": [
              {
                "id": "links",
                "value": [
                  {
                    "title": "查看玩家旅程",
                    "url": "/d/gameplay-player-detail?${__url_time_range}&${client_platform:queryparam}&${release_version:queryparam}&${test_data:queryparam}&var-user_id=${__data.fields.user_id}&var-user_id=${__value.raw}"
                  }
                ]
              }
            ]
          },
          {
            "matcher": {
              "id": "byName",
              "options": "session_id"
            },
            "properties": [
              {
                "id": "links",
                "value": [
                  {
                    "title": "查看玩家旅程",
                    "url": "/d/gameplay-player-detail?${__url_time_range}&${client_platform:queryparam}&${release_version:queryparam}&${test_data:queryparam}&var-user_id=${__data.fields.user_id}&var-session_id=${__value.raw}"
                  }
                ]
              }
            ]
          },
          {
            "matcher": {
              "id": "byName",
              "options": "llm_request_id"
            },
            "properties": [
              {
                "id": "links",
                "value": [
                  {
                    "title": "查看玩家旅程",
                    "url": "/d/gameplay-player-detail?${__url_time_range}&${client_platform:queryparam}&${release_version:queryparam}&${test_data:queryparam}&var-user_id=${__data.fields.user_id}&var-llm_request_id=${__value.raw}"
                  }
                ]
              }
            ]
          }
        ]
      },
      "options": {
        "showHeader": true,
        "cellHeight": "sm"
      },
      "gridPos": {
        "x": 0,
        "y": 100,
        "w": 12,
        "h": 9
      }
    },
    {
      "id": 152,
      "title": "各部位穿搭 Top 5",
      "type": "table",
      "datasource": {
        "type": "frser-sqlite-datasource",
        "uid": "${DS_SQLITE}"
      },
      "description": "累计按玩家去重；当前状态取最新快照。缺失显示未采集，数值零保留。行为可切换期间，日期为北京时间。",
      "targets": [
        {
          "refId": "A",
          "queryType": "table",
          "queryText": "WITH e0 AS NOT MATERIALIZED (SELECT * FROM analytics_events WHERE ('__all__' IN (${client_platform:sqlstring}) OR client_platform IN (${client_platform:sqlstring})) AND ('__all__' IN (${release_version:sqlstring}) OR COALESCE(NULLIF(release_version,''),'__empty__') IN (${release_version:sqlstring})) AND ('${test_data}'='include' OR ('${test_data}'='auto' AND (client_platform='editor' OR (is_development_build=0 AND is_developer=0))) OR ('${test_data}'='exclude' AND is_development_build=0 AND is_developer=0 AND client_platform!='editor') OR ('${test_data}'='only' AND (is_development_build=1 OR is_developer=1 OR client_platform='editor')))),\n    s AS MATERIALIZED (SELECT * FROM analytics_sessions WHERE ('__all__' IN (${client_platform:sqlstring}) OR client_platform IN (${client_platform:sqlstring})) AND ('__all__' IN (${release_version:sqlstring}) OR COALESCE(NULLIF(release_version,''),'__empty__') IN (${release_version:sqlstring})) AND ('${test_data}'='include' OR ('${test_data}'='auto' AND (client_platform='editor' OR (is_development_build=0 AND is_developer=0))) OR ('${test_data}'='exclude' AND is_development_build=0 AND is_developer=0 AND client_platform!='editor') OR ('${test_data}'='only' AND (is_development_build=1 OR is_developer=1 OR client_platform='editor')))),\n    ids AS (SELECT user_id FROM s UNION SELECT user_id FROM e0),\n    states AS (SELECT *,ROW_NUMBER() OVER(PARTITION BY user_id ORDER BY julianday(occurred_at) DESC,sequence DESC,event_id DESC) rn\n        FROM e0 WHERE event_type='player_state_snapshot'),\n    legacy AS (SELECT g.*,ROW_NUMBER() OVER(PARTITION BY user_id ORDER BY julianday(real_time_started_iso) DESC,imported_at DESC) rn\n        FROM gameplay_sessions g WHERE EXISTS(SELECT 1 FROM s WHERE s.user_id=g.user_id AND s.session_id=g.session_id)),\n    days AS (SELECT user_id,COUNT(*) play_days FROM\n        (SELECT DISTINCT user_id,archive_id,game_day FROM e0 WHERE NULLIF(archive_id,'') IS NOT NULL AND game_day IS NOT NULL) GROUP BY user_id),\n    p AS (SELECT ids.user_id,COALESCE(NULLIF(json_extract(st.payload_json,'$.nickname'),''),NULLIF(u.nickname,''),NULLIF(u.player_name,''),ids.user_id) nickname,\n        (SELECT SUM(duration_sec) FROM s WHERE s.user_id=ids.user_id) play_seconds,\n        (SELECT first_entry_at FROM analytics_first_entry f WHERE f.user_id=ids.user_id AND f.cohort_scope=CASE WHEN '${test_data}' IN ('include','only') OR ('${test_data}'='auto' AND ('editor' IN (${client_platform:sqlstring}) OR '__all__' IN (${client_platform:sqlstring}))) THEN 'all' ELSE 'production' END) first_login,\n        (SELECT MAX(started_at) FROM s WHERE s.user_id=ids.user_id) latest_login,\n        d.play_days,COALESCE(json_extract(st.payload_json,'$.money'),l.money_end,l.money_start) money,\n        COALESCE(json_extract(st.payload_json,'$.island_level'),l.island_level_max) island_level,\n        json_array_length(json_extract(st.payload_json,'$.cats')) cat_count,\n        st.payload_json state,st.occurred_at state_at,\n        json_extract(st.payload_json,'$.cat_house_count') house_count,\n        json_extract(st.payload_json,'$.cat_house_capacity') house_capacity,\n        json_extract(st.payload_json,'$.cat_house_occupied') house_occupied\n        FROM ids LEFT JOIN user_sessions u USING(user_id) LEFT JOIN states st ON st.user_id=ids.user_id AND st.rn=1\n        LEFT JOIN legacy l ON l.user_id=ids.user_id AND l.rn=1 LEFT JOIN days d USING(user_id)),\n    e AS (SELECT * FROM e0 WHERE ('${behavior_period}'='all' OR julianday(occurred_at) >= julianday(${__from}/1000,'unixepoch') AND julianday(occurred_at) < julianday(${__to}/1000,'unixepoch'))\n        OR (occurred_at IS NULL AND '${behavior_period}'='all')),\n    players AS (SELECT * FROM p WHERE 1),\n    events AS (SELECT * FROM e WHERE 1) SELECT 部位,服装,玩家数 FROM (SELECT json_extract(c.value,'$.slot') 部位,json_extract(c.value,'$.item_name') 服装,COUNT(*) 玩家数,\n            ROW_NUMBER() OVER(PARTITION BY json_extract(c.value,'$.slot_id') ORDER BY COUNT(*) DESC,json_extract(c.value,'$.item_id')) n\n            FROM players,json_each(state,'$.outfits') c GROUP BY json_extract(c.value,'$.slot_id'),json_extract(c.value,'$.item_id')) WHERE n<=5 ORDER BY 部位,n",
          "rawQueryText": "WITH e0 AS NOT MATERIALIZED (SELECT * FROM analytics_events WHERE ('__all__' IN (${client_platform:sqlstring}) OR client_platform IN (${client_platform:sqlstring})) AND ('__all__' IN (${release_version:sqlstring}) OR COALESCE(NULLIF(release_version,''),'__empty__') IN (${release_version:sqlstring})) AND ('${test_data}'='include' OR ('${test_data}'='auto' AND (client_platform='editor' OR (is_development_build=0 AND is_developer=0))) OR ('${test_data}'='exclude' AND is_development_build=0 AND is_developer=0 AND client_platform!='editor') OR ('${test_data}'='only' AND (is_development_build=1 OR is_developer=1 OR client_platform='editor')))),\n    s AS MATERIALIZED (SELECT * FROM analytics_sessions WHERE ('__all__' IN (${client_platform:sqlstring}) OR client_platform IN (${client_platform:sqlstring})) AND ('__all__' IN (${release_version:sqlstring}) OR COALESCE(NULLIF(release_version,''),'__empty__') IN (${release_version:sqlstring})) AND ('${test_data}'='include' OR ('${test_data}'='auto' AND (client_platform='editor' OR (is_development_build=0 AND is_developer=0))) OR ('${test_data}'='exclude' AND is_development_build=0 AND is_developer=0 AND client_platform!='editor') OR ('${test_data}'='only' AND (is_development_build=1 OR is_developer=1 OR client_platform='editor')))),\n    ids AS (SELECT user_id FROM s UNION SELECT user_id FROM e0),\n    states AS (SELECT *,ROW_NUMBER() OVER(PARTITION BY user_id ORDER BY julianday(occurred_at) DESC,sequence DESC,event_id DESC) rn\n        FROM e0 WHERE event_type='player_state_snapshot'),\n    legacy AS (SELECT g.*,ROW_NUMBER() OVER(PARTITION BY user_id ORDER BY julianday(real_time_started_iso) DESC,imported_at DESC) rn\n        FROM gameplay_sessions g WHERE EXISTS(SELECT 1 FROM s WHERE s.user_id=g.user_id AND s.session_id=g.session_id)),\n    days AS (SELECT user_id,COUNT(*) play_days FROM\n        (SELECT DISTINCT user_id,archive_id,game_day FROM e0 WHERE NULLIF(archive_id,'') IS NOT NULL AND game_day IS NOT NULL) GROUP BY user_id),\n    p AS (SELECT ids.user_id,COALESCE(NULLIF(json_extract(st.payload_json,'$.nickname'),''),NULLIF(u.nickname,''),NULLIF(u.player_name,''),ids.user_id) nickname,\n        (SELECT SUM(duration_sec) FROM s WHERE s.user_id=ids.user_id) play_seconds,\n        (SELECT first_entry_at FROM analytics_first_entry f WHERE f.user_id=ids.user_id AND f.cohort_scope=CASE WHEN '${test_data}' IN ('include','only') OR ('${test_data}'='auto' AND ('editor' IN (${client_platform:sqlstring}) OR '__all__' IN (${client_platform:sqlstring}))) THEN 'all' ELSE 'production' END) first_login,\n        (SELECT MAX(started_at) FROM s WHERE s.user_id=ids.user_id) latest_login,\n        d.play_days,COALESCE(json_extract(st.payload_json,'$.money'),l.money_end,l.money_start) money,\n        COALESCE(json_extract(st.payload_json,'$.island_level'),l.island_level_max) island_level,\n        json_array_length(json_extract(st.payload_json,'$.cats')) cat_count,\n        st.payload_json state,st.occurred_at state_at,\n        json_extract(st.payload_json,'$.cat_house_count') house_count,\n        json_extract(st.payload_json,'$.cat_house_capacity') house_capacity,\n        json_extract(st.payload_json,'$.cat_house_occupied') house_occupied\n        FROM ids LEFT JOIN user_sessions u USING(user_id) LEFT JOIN states st ON st.user_id=ids.user_id AND st.rn=1\n        LEFT JOIN legacy l ON l.user_id=ids.user_id AND l.rn=1 LEFT JOIN days d USING(user_id)),\n    e AS (SELECT * FROM e0 WHERE ('${behavior_period}'='all' OR julianday(occurred_at) >= julianday(${__from}/1000,'unixepoch') AND julianday(occurred_at) < julianday(${__to}/1000,'unixepoch'))\n        OR (occurred_at IS NULL AND '${behavior_period}'='all')),\n    players AS (SELECT * FROM p WHERE 1),\n    events AS (SELECT * FROM e WHERE 1) SELECT 部位,服装,玩家数 FROM (SELECT json_extract(c.value,'$.slot') 部位,json_extract(c.value,'$.item_name') 服装,COUNT(*) 玩家数,\n            ROW_NUMBER() OVER(PARTITION BY json_extract(c.value,'$.slot_id') ORDER BY COUNT(*) DESC,json_extract(c.value,'$.item_id')) n\n            FROM players,json_each(state,'$.outfits') c GROUP BY json_extract(c.value,'$.slot_id'),json_extract(c.value,'$.item_id')) WHERE n<=5 ORDER BY 部位,n"
        }
      ],
      "fieldConfig": {
        "defaults": {
          "unit": "short",
          "custom": {
            "filterable": true
          },
          "noValue": "未采集"
        },
        "overrides": [
          {
            "matcher": {
              "id": "byName",
              "options": "user_id"
            },
            "properties": [
              {
                "id": "links",
                "value": [
                  {
                    "title": "查看玩家旅程",
                    "url": "/d/gameplay-player-detail?${__url_time_range}&${client_platform:queryparam}&${release_version:queryparam}&${test_data:queryparam}&var-user_id=${__data.fields.user_id}&var-user_id=${__value.raw}"
                  }
                ]
              }
            ]
          },
          {
            "matcher": {
              "id": "byName",
              "options": "session_id"
            },
            "properties": [
              {
                "id": "links",
                "value": [
                  {
                    "title": "查看玩家旅程",
                    "url": "/d/gameplay-player-detail?${__url_time_range}&${client_platform:queryparam}&${release_version:queryparam}&${test_data:queryparam}&var-user_id=${__data.fields.user_id}&var-session_id=${__value.raw}"
                  }
                ]
              }
            ]
          },
          {
            "matcher": {
              "id": "byName",
              "options": "llm_request_id"
            },
            "properties": [
              {
                "id": "links",
                "value": [
                  {
                    "title": "查看玩家旅程",
                    "url": "/d/gameplay-player-detail?${__url_time_range}&${client_platform:queryparam}&${release_version:queryparam}&${test_data:queryparam}&var-user_id=${__data.fields.user_id}&var-llm_request_id=${__value.raw}"
                  }
                ]
              }
            ]
          }
        ]
      },
      "options": {
        "showHeader": true,
        "cellHeight": "sm"
      },
      "gridPos": {
        "x": 12,
        "y": 100,
        "w": 12,
        "h": 9
      }
    },
    {
      "id": 153,
      "title": "采集覆盖与历史限制",
      "type": "table",
      "datasource": {
        "type": "frser-sqlite-datasource",
        "uid": "${DS_SQLITE}"
      },
      "description": "累计按玩家去重；当前状态取最新快照。缺失显示未采集，数值零保留。行为可切换期间，日期为北京时间。",
      "targets": [
        {
          "refId": "A",
          "queryType": "table",
          "queryText": "WITH e0 AS NOT MATERIALIZED (SELECT * FROM analytics_events WHERE ('__all__' IN (${client_platform:sqlstring}) OR client_platform IN (${client_platform:sqlstring})) AND ('__all__' IN (${release_version:sqlstring}) OR COALESCE(NULLIF(release_version,''),'__empty__') IN (${release_version:sqlstring})) AND ('${test_data}'='include' OR ('${test_data}'='auto' AND (client_platform='editor' OR (is_development_build=0 AND is_developer=0))) OR ('${test_data}'='exclude' AND is_development_build=0 AND is_developer=0 AND client_platform!='editor') OR ('${test_data}'='only' AND (is_development_build=1 OR is_developer=1 OR client_platform='editor')))),\n    s AS MATERIALIZED (SELECT * FROM analytics_sessions WHERE ('__all__' IN (${client_platform:sqlstring}) OR client_platform IN (${client_platform:sqlstring})) AND ('__all__' IN (${release_version:sqlstring}) OR COALESCE(NULLIF(release_version,''),'__empty__') IN (${release_version:sqlstring})) AND ('${test_data}'='include' OR ('${test_data}'='auto' AND (client_platform='editor' OR (is_development_build=0 AND is_developer=0))) OR ('${test_data}'='exclude' AND is_development_build=0 AND is_developer=0 AND client_platform!='editor') OR ('${test_data}'='only' AND (is_development_build=1 OR is_developer=1 OR client_platform='editor')))),\n    ids AS (SELECT user_id FROM s UNION SELECT user_id FROM e0),\n    states AS (SELECT *,ROW_NUMBER() OVER(PARTITION BY user_id ORDER BY julianday(occurred_at) DESC,sequence DESC,event_id DESC) rn\n        FROM e0 WHERE event_type='player_state_snapshot'),\n    legacy AS (SELECT g.*,ROW_NUMBER() OVER(PARTITION BY user_id ORDER BY julianday(real_time_started_iso) DESC,imported_at DESC) rn\n        FROM gameplay_sessions g WHERE EXISTS(SELECT 1 FROM s WHERE s.user_id=g.user_id AND s.session_id=g.session_id)),\n    days AS (SELECT user_id,COUNT(*) play_days FROM\n        (SELECT DISTINCT user_id,archive_id,game_day FROM e0 WHERE NULLIF(archive_id,'') IS NOT NULL AND game_day IS NOT NULL) GROUP BY user_id),\n    p AS (SELECT ids.user_id,COALESCE(NULLIF(json_extract(st.payload_json,'$.nickname'),''),NULLIF(u.nickname,''),NULLIF(u.player_name,''),ids.user_id) nickname,\n        (SELECT SUM(duration_sec) FROM s WHERE s.user_id=ids.user_id) play_seconds,\n        (SELECT first_entry_at FROM analytics_first_entry f WHERE f.user_id=ids.user_id AND f.cohort_scope=CASE WHEN '${test_data}' IN ('include','only') OR ('${test_data}'='auto' AND ('editor' IN (${client_platform:sqlstring}) OR '__all__' IN (${client_platform:sqlstring}))) THEN 'all' ELSE 'production' END) first_login,\n        (SELECT MAX(started_at) FROM s WHERE s.user_id=ids.user_id) latest_login,\n        d.play_days,COALESCE(json_extract(st.payload_json,'$.money'),l.money_end,l.money_start) money,\n        COALESCE(json_extract(st.payload_json,'$.island_level'),l.island_level_max) island_level,\n        json_array_length(json_extract(st.payload_json,'$.cats')) cat_count,\n        st.payload_json state,st.occurred_at state_at,\n        json_extract(st.payload_json,'$.cat_house_count') house_count,\n        json_extract(st.payload_json,'$.cat_house_capacity') house_capacity,\n        json_extract(st.payload_json,'$.cat_house_occupied') house_occupied\n        FROM ids LEFT JOIN user_sessions u USING(user_id) LEFT JOIN states st ON st.user_id=ids.user_id AND st.rn=1\n        LEFT JOIN legacy l ON l.user_id=ids.user_id AND l.rn=1 LEFT JOIN days d USING(user_id)),\n    e AS (SELECT * FROM e0 WHERE ('${behavior_period}'='all' OR julianday(occurred_at) >= julianday(${__from}/1000,'unixepoch') AND julianday(occurred_at) < julianday(${__to}/1000,'unixepoch'))\n        OR (occurred_at IS NULL AND '${behavior_period}'='all')),\n    players AS (SELECT * FROM p WHERE 1),\n    events AS (SELECT * FROM e WHERE 1) SELECT COUNT(*) 玩家数,COUNT(state) 完整状态玩家数,COUNT(play_days) 可去重游戏日玩家数,(SELECT COUNT(*) FROM e0 WHERE occurred_at IS NULL) 缺真实时间事件数 FROM players",
          "rawQueryText": "WITH e0 AS NOT MATERIALIZED (SELECT * FROM analytics_events WHERE ('__all__' IN (${client_platform:sqlstring}) OR client_platform IN (${client_platform:sqlstring})) AND ('__all__' IN (${release_version:sqlstring}) OR COALESCE(NULLIF(release_version,''),'__empty__') IN (${release_version:sqlstring})) AND ('${test_data}'='include' OR ('${test_data}'='auto' AND (client_platform='editor' OR (is_development_build=0 AND is_developer=0))) OR ('${test_data}'='exclude' AND is_development_build=0 AND is_developer=0 AND client_platform!='editor') OR ('${test_data}'='only' AND (is_development_build=1 OR is_developer=1 OR client_platform='editor')))),\n    s AS MATERIALIZED (SELECT * FROM analytics_sessions WHERE ('__all__' IN (${client_platform:sqlstring}) OR client_platform IN (${client_platform:sqlstring})) AND ('__all__' IN (${release_version:sqlstring}) OR COALESCE(NULLIF(release_version,''),'__empty__') IN (${release_version:sqlstring})) AND ('${test_data}'='include' OR ('${test_data}'='auto' AND (client_platform='editor' OR (is_development_build=0 AND is_developer=0))) OR ('${test_data}'='exclude' AND is_development_build=0 AND is_developer=0 AND client_platform!='editor') OR ('${test_data}'='only' AND (is_development_build=1 OR is_developer=1 OR client_platform='editor')))),\n    ids AS (SELECT user_id FROM s UNION SELECT user_id FROM e0),\n    states AS (SELECT *,ROW_NUMBER() OVER(PARTITION BY user_id ORDER BY julianday(occurred_at) DESC,sequence DESC,event_id DESC) rn\n        FROM e0 WHERE event_type='player_state_snapshot'),\n    legacy AS (SELECT g.*,ROW_NUMBER() OVER(PARTITION BY user_id ORDER BY julianday(real_time_started_iso) DESC,imported_at DESC) rn\n        FROM gameplay_sessions g WHERE EXISTS(SELECT 1 FROM s WHERE s.user_id=g.user_id AND s.session_id=g.session_id)),\n    days AS (SELECT user_id,COUNT(*) play_days FROM\n        (SELECT DISTINCT user_id,archive_id,game_day FROM e0 WHERE NULLIF(archive_id,'') IS NOT NULL AND game_day IS NOT NULL) GROUP BY user_id),\n    p AS (SELECT ids.user_id,COALESCE(NULLIF(json_extract(st.payload_json,'$.nickname'),''),NULLIF(u.nickname,''),NULLIF(u.player_name,''),ids.user_id) nickname,\n        (SELECT SUM(duration_sec) FROM s WHERE s.user_id=ids.user_id) play_seconds,\n        (SELECT first_entry_at FROM analytics_first_entry f WHERE f.user_id=ids.user_id AND f.cohort_scope=CASE WHEN '${test_data}' IN ('include','only') OR ('${test_data}'='auto' AND ('editor' IN (${client_platform:sqlstring}) OR '__all__' IN (${client_platform:sqlstring}))) THEN 'all' ELSE 'production' END) first_login,\n        (SELECT MAX(started_at) FROM s WHERE s.user_id=ids.user_id) latest_login,\n        d.play_days,COALESCE(json_extract(st.payload_json,'$.money'),l.money_end,l.money_start) money,\n        COALESCE(json_extract(st.payload_json,'$.island_level'),l.island_level_max) island_level,\n        json_array_length(json_extract(st.payload_json,'$.cats')) cat_count,\n        st.payload_json state,st.occurred_at state_at,\n        json_extract(st.payload_json,'$.cat_house_count') house_count,\n        json_extract(st.payload_json,'$.cat_house_capacity') house_capacity,\n        json_extract(st.payload_json,'$.cat_house_occupied') house_occupied\n        FROM ids LEFT JOIN user_sessions u USING(user_id) LEFT JOIN states st ON st.user_id=ids.user_id AND st.rn=1\n        LEFT JOIN legacy l ON l.user_id=ids.user_id AND l.rn=1 LEFT JOIN days d USING(user_id)),\n    e AS (SELECT * FROM e0 WHERE ('${behavior_period}'='all' OR julianday(occurred_at) >= julianday(${__from}/1000,'unixepoch') AND julianday(occurred_at) < julianday(${__to}/1000,'unixepoch'))\n        OR (occurred_at IS NULL AND '${behavior_period}'='all')),\n    players AS (SELECT * FROM p WHERE 1),\n    events AS (SELECT * FROM e WHERE 1) SELECT COUNT(*) 玩家数,COUNT(state) 完整状态玩家数,COUNT(play_days) 可去重游戏日玩家数,(SELECT COUNT(*) FROM e0 WHERE occurred_at IS NULL) 缺真实时间事件数 FROM players"
        }
      ],
      "fieldConfig": {
        "defaults": {
          "unit": "short",
          "custom": {
            "filterable": true
          },
          "noValue": "未采集"
        },
        "overrides": [
          {
            "matcher": {
              "id": "byName",
              "options": "user_id"
            },
            "properties": [
              {
                "id": "links",
                "value": [
                  {
                    "title": "查看玩家旅程",
                    "url": "/d/gameplay-player-detail?${__url_time_range}&${client_platform:queryparam}&${release_version:queryparam}&${test_data:queryparam}&var-user_id=${__data.fields.user_id}&var-user_id=${__value.raw}"
                  }
                ]
              }
            ]
          },
          {
            "matcher": {
              "id": "byName",
              "options": "session_id"
            },
            "properties": [
              {
                "id": "links",
                "value": [
                  {
                    "title": "查看玩家旅程",
                    "url": "/d/gameplay-player-detail?${__url_time_range}&${client_platform:queryparam}&${release_version:queryparam}&${test_data:queryparam}&var-user_id=${__data.fields.user_id}&var-session_id=${__value.raw}"
                  }
                ]
              }
            ]
          },
          {
            "matcher": {
              "id": "byName",
              "options": "llm_request_id"
            },
            "properties": [
              {
                "id": "links",
                "value": [
                  {
                    "title": "查看玩家旅程",
                    "url": "/d/gameplay-player-detail?${__url_time_range}&${client_platform:queryparam}&${release_version:queryparam}&${test_data:queryparam}&var-user_id=${__data.fields.user_id}&var-llm_request_id=${__value.raw}"
                  }
                ]
              }
            ]
          }
        ]
      },
      "options": {
        "showHeader": true,
        "cellHeight": "sm"
      },
      "gridPos": {
        "x": 0,
        "y": 109,
        "w": 24,
        "h": 9
      }
    },
    {
      "id": 154,
      "title": "钓鱼尝试覆盖（含未观察到结算）",
      "type": "table",
      "datasource": {
        "type": "frser-sqlite-datasource",
        "uid": "${DS_SQLITE}"
      },
      "description": "累计按玩家去重；当前状态取最新快照。缺失显示未采集，数值零保留。行为可切换期间，日期为北京时间。",
      "targets": [
        {
          "refId": "A",
          "queryType": "table",
          "queryText": "WITH e0 AS NOT MATERIALIZED (SELECT * FROM analytics_events WHERE ('__all__' IN (${client_platform:sqlstring}) OR client_platform IN (${client_platform:sqlstring})) AND ('__all__' IN (${release_version:sqlstring}) OR COALESCE(NULLIF(release_version,''),'__empty__') IN (${release_version:sqlstring})) AND ('${test_data}'='include' OR ('${test_data}'='auto' AND (client_platform='editor' OR (is_development_build=0 AND is_developer=0))) OR ('${test_data}'='exclude' AND is_development_build=0 AND is_developer=0 AND client_platform!='editor') OR ('${test_data}'='only' AND (is_development_build=1 OR is_developer=1 OR client_platform='editor')))),\n    s AS MATERIALIZED (SELECT * FROM analytics_sessions WHERE ('__all__' IN (${client_platform:sqlstring}) OR client_platform IN (${client_platform:sqlstring})) AND ('__all__' IN (${release_version:sqlstring}) OR COALESCE(NULLIF(release_version,''),'__empty__') IN (${release_version:sqlstring})) AND ('${test_data}'='include' OR ('${test_data}'='auto' AND (client_platform='editor' OR (is_development_build=0 AND is_developer=0))) OR ('${test_data}'='exclude' AND is_development_build=0 AND is_developer=0 AND client_platform!='editor') OR ('${test_data}'='only' AND (is_development_build=1 OR is_developer=1 OR client_platform='editor')))),\n    ids AS (SELECT user_id FROM s UNION SELECT user_id FROM e0),\n    states AS (SELECT *,ROW_NUMBER() OVER(PARTITION BY user_id ORDER BY julianday(occurred_at) DESC,sequence DESC,event_id DESC) rn\n        FROM e0 WHERE event_type='player_state_snapshot'),\n    legacy AS (SELECT g.*,ROW_NUMBER() OVER(PARTITION BY user_id ORDER BY julianday(real_time_started_iso) DESC,imported_at DESC) rn\n        FROM gameplay_sessions g WHERE EXISTS(SELECT 1 FROM s WHERE s.user_id=g.user_id AND s.session_id=g.session_id)),\n    days AS (SELECT user_id,COUNT(*) play_days FROM\n        (SELECT DISTINCT user_id,archive_id,game_day FROM e0 WHERE NULLIF(archive_id,'') IS NOT NULL AND game_day IS NOT NULL) GROUP BY user_id),\n    p AS (SELECT ids.user_id,COALESCE(NULLIF(json_extract(st.payload_json,'$.nickname'),''),NULLIF(u.nickname,''),NULLIF(u.player_name,''),ids.user_id) nickname,\n        (SELECT SUM(duration_sec) FROM s WHERE s.user_id=ids.user_id) play_seconds,\n        (SELECT first_entry_at FROM analytics_first_entry f WHERE f.user_id=ids.user_id AND f.cohort_scope=CASE WHEN '${test_data}' IN ('include','only') OR ('${test_data}'='auto' AND ('editor' IN (${client_platform:sqlstring}) OR '__all__' IN (${client_platform:sqlstring}))) THEN 'all' ELSE 'production' END) first_login,\n        (SELECT MAX(started_at) FROM s WHERE s.user_id=ids.user_id) latest_login,\n        d.play_days,COALESCE(json_extract(st.payload_json,'$.money'),l.money_end,l.money_start) money,\n        COALESCE(json_extract(st.payload_json,'$.island_level'),l.island_level_max) island_level,\n        json_array_length(json_extract(st.payload_json,'$.cats')) cat_count,\n        st.payload_json state,st.occurred_at state_at,\n        json_extract(st.payload_json,'$.cat_house_count') house_count,\n        json_extract(st.payload_json,'$.cat_house_capacity') house_capacity,\n        json_extract(st.payload_json,'$.cat_house_occupied') house_occupied\n        FROM ids LEFT JOIN user_sessions u USING(user_id) LEFT JOIN states st ON st.user_id=ids.user_id AND st.rn=1\n        LEFT JOIN legacy l ON l.user_id=ids.user_id AND l.rn=1 LEFT JOIN days d USING(user_id)),\n    e AS (SELECT * FROM e0 WHERE ('${behavior_period}'='all' OR julianday(occurred_at) >= julianday(${__from}/1000,'unixepoch') AND julianday(occurred_at) < julianday(${__to}/1000,'unixepoch'))\n        OR (occurred_at IS NULL AND '${behavior_period}'='all')),\n    players AS (SELECT * FROM p WHERE 1),\n    events AS (SELECT * FROM e WHERE 1) SELECT json_extract(payload_json,'$.region') 区域,COUNT(*) 抛竿次数,SUM(EXISTS(SELECT 1 FROM e0 f WHERE f.user_id=events.user_id AND f.session_id=events.session_id AND f.event_type='fishing_finished' AND json_extract(f.payload_json,'$.operation_id')=json_extract(events.payload_json,'$.operation_id'))) 已观察到结算 FROM events WHERE event_type='fishing_started' GROUP BY 1",
          "rawQueryText": "WITH e0 AS NOT MATERIALIZED (SELECT * FROM analytics_events WHERE ('__all__' IN (${client_platform:sqlstring}) OR client_platform IN (${client_platform:sqlstring})) AND ('__all__' IN (${release_version:sqlstring}) OR COALESCE(NULLIF(release_version,''),'__empty__') IN (${release_version:sqlstring})) AND ('${test_data}'='include' OR ('${test_data}'='auto' AND (client_platform='editor' OR (is_development_build=0 AND is_developer=0))) OR ('${test_data}'='exclude' AND is_development_build=0 AND is_developer=0 AND client_platform!='editor') OR ('${test_data}'='only' AND (is_development_build=1 OR is_developer=1 OR client_platform='editor')))),\n    s AS MATERIALIZED (SELECT * FROM analytics_sessions WHERE ('__all__' IN (${client_platform:sqlstring}) OR client_platform IN (${client_platform:sqlstring})) AND ('__all__' IN (${release_version:sqlstring}) OR COALESCE(NULLIF(release_version,''),'__empty__') IN (${release_version:sqlstring})) AND ('${test_data}'='include' OR ('${test_data}'='auto' AND (client_platform='editor' OR (is_development_build=0 AND is_developer=0))) OR ('${test_data}'='exclude' AND is_development_build=0 AND is_developer=0 AND client_platform!='editor') OR ('${test_data}'='only' AND (is_development_build=1 OR is_developer=1 OR client_platform='editor')))),\n    ids AS (SELECT user_id FROM s UNION SELECT user_id FROM e0),\n    states AS (SELECT *,ROW_NUMBER() OVER(PARTITION BY user_id ORDER BY julianday(occurred_at) DESC,sequence DESC,event_id DESC) rn\n        FROM e0 WHERE event_type='player_state_snapshot'),\n    legacy AS (SELECT g.*,ROW_NUMBER() OVER(PARTITION BY user_id ORDER BY julianday(real_time_started_iso) DESC,imported_at DESC) rn\n        FROM gameplay_sessions g WHERE EXISTS(SELECT 1 FROM s WHERE s.user_id=g.user_id AND s.session_id=g.session_id)),\n    days AS (SELECT user_id,COUNT(*) play_days FROM\n        (SELECT DISTINCT user_id,archive_id,game_day FROM e0 WHERE NULLIF(archive_id,'') IS NOT NULL AND game_day IS NOT NULL) GROUP BY user_id),\n    p AS (SELECT ids.user_id,COALESCE(NULLIF(json_extract(st.payload_json,'$.nickname'),''),NULLIF(u.nickname,''),NULLIF(u.player_name,''),ids.user_id) nickname,\n        (SELECT SUM(duration_sec) FROM s WHERE s.user_id=ids.user_id) play_seconds,\n        (SELECT first_entry_at FROM analytics_first_entry f WHERE f.user_id=ids.user_id AND f.cohort_scope=CASE WHEN '${test_data}' IN ('include','only') OR ('${test_data}'='auto' AND ('editor' IN (${client_platform:sqlstring}) OR '__all__' IN (${client_platform:sqlstring}))) THEN 'all' ELSE 'production' END) first_login,\n        (SELECT MAX(started_at) FROM s WHERE s.user_id=ids.user_id) latest_login,\n        d.play_days,COALESCE(json_extract(st.payload_json,'$.money'),l.money_end,l.money_start) money,\n        COALESCE(json_extract(st.payload_json,'$.island_level'),l.island_level_max) island_level,\n        json_array_length(json_extract(st.payload_json,'$.cats')) cat_count,\n        st.payload_json state,st.occurred_at state_at,\n        json_extract(st.payload_json,'$.cat_house_count') house_count,\n        json_extract(st.payload_json,'$.cat_house_capacity') house_capacity,\n        json_extract(st.payload_json,'$.cat_house_occupied') house_occupied\n        FROM ids LEFT JOIN user_sessions u USING(user_id) LEFT JOIN states st ON st.user_id=ids.user_id AND st.rn=1\n        LEFT JOIN legacy l ON l.user_id=ids.user_id AND l.rn=1 LEFT JOIN days d USING(user_id)),\n    e AS (SELECT * FROM e0 WHERE ('${behavior_period}'='all' OR julianday(occurred_at) >= julianday(${__from}/1000,'unixepoch') AND julianday(occurred_at) < julianday(${__to}/1000,'unixepoch'))\n        OR (occurred_at IS NULL AND '${behavior_period}'='all')),\n    players AS (SELECT * FROM p WHERE 1),\n    events AS (SELECT * FROM e WHERE 1) SELECT json_extract(payload_json,'$.region') 区域,COUNT(*) 抛竿次数,SUM(EXISTS(SELECT 1 FROM e0 f WHERE f.user_id=events.user_id AND f.session_id=events.session_id AND f.event_type='fishing_finished' AND json_extract(f.payload_json,'$.operation_id')=json_extract(events.payload_json,'$.operation_id'))) 已观察到结算 FROM events WHERE event_type='fishing_started' GROUP BY 1"
        }
      ],
      "fieldConfig": {
        "defaults": {
          "unit": "short",
          "custom": {
            "filterable": true
          },
          "noValue": "未采集"
        },
        "overrides": [
          {
            "matcher": {
              "id": "byName",
              "options": "user_id"
            },
            "properties": [
              {
                "id": "links",
                "value": [
                  {
                    "title": "查看玩家旅程",
                    "url": "/d/gameplay-player-detail?${__url_time_range}&${client_platform:queryparam}&${release_version:queryparam}&${test_data:queryparam}&var-user_id=${__data.fields.user_id}&var-user_id=${__value.raw}"
                  }
                ]
              }
            ]
          },
          {
            "matcher": {
              "id": "byName",
              "options": "session_id"
            },
            "properties": [
              {
                "id": "links",
                "value": [
                  {
                    "title": "查看玩家旅程",
                    "url": "/d/gameplay-player-detail?${__url_time_range}&${client_platform:queryparam}&${release_version:queryparam}&${test_data:queryparam}&var-user_id=${__data.fields.user_id}&var-session_id=${__value.raw}"
                  }
                ]
              }
            ]
          },
          {
            "matcher": {
              "id": "byName",
              "options": "llm_request_id"
            },
            "properties": [
              {
                "id": "links",
                "value": [
                  {
                    "title": "查看玩家旅程",
                    "url": "/d/gameplay-player-detail?${__url_time_range}&${client_platform:queryparam}&${release_version:queryparam}&${test_data:queryparam}&var-user_id=${__data.fields.user_id}&var-llm_request_id=${__value.raw}"
                  }
                ]
              }
            ]
          }
        ]
      },
      "options": {
        "showHeader": true,
        "cellHeight": "sm"
      },
      "gridPos": {
        "x": 0,
        "y": 118,
        "w": 12,
        "h": 9
      }
    },
    {
      "id": 226,
      "title": "最愿意继续的 5 种小剧场",
      "type": "table",
      "datasource": {
        "type": "frser-sqlite-datasource",
        "uid": "${DS_SQLITE}"
      },
      "description": "按玩家、会话和本次观看去重，只取首次邀请及其首次点击；返回邀请页不会再次计入。继续率=选择继续/邀请展示数；未选择、Esc、超时均不推算为拒绝。记录点击意图，不表示服务端成功开演。 默认至少 10 次邀请；可调整样本门槛。",
      "targets": [
        {
          "refId": "A",
          "queryType": "table",
          "queryText": "WITH e AS (SELECT * FROM analytics_events WHERE ('__all__' IN (${client_platform:sqlstring}) OR client_platform IN (${client_platform:sqlstring})) AND ('__all__' IN (${release_version:sqlstring}) OR COALESCE(NULLIF(release_version,''),'__empty__') IN (${release_version:sqlstring})) AND ('${test_data}'='include' OR ('${test_data}'='auto' AND (client_platform='editor' OR (is_development_build=0 AND is_developer=0))) OR ('${test_data}'='exclude' AND is_development_build=0 AND is_developer=0 AND client_platform!='editor') OR ('${test_data}'='only' AND (is_development_build=1 OR is_developer=1 OR client_platform='editor'))) AND ('${behavior_period}'='all' OR (julianday(occurred_at) >= julianday(${__from}/1000,'unixepoch') AND julianday(occurred_at) < julianday(${__to}/1000,'unixepoch'))) ), t AS (\n SELECT *,json_extract(payload_json,'$.theater_event_id') theater_event_id,\n json_extract(payload_json,'$.theater_type') theater_type,\n json_extract(payload_json,'$.view_id') view_id,\n json_extract(payload_json,'$.decision_id') decision_id,\n json_extract(payload_json,'$.phase') phase,\n json_extract(payload_json,'$.choice_class') choice_class,\n json_extract(payload_json,'$.reason') reason\n FROM e WHERE event_type IN ('theater_view','theater_choice','theater_line','theater_exit')\n AND 1 AND 1 AND (COALESCE(json_extract(json_array(${theater_event_id:sqlstring}), '$[0]'),'')='' OR json_extract(payload_json,'$.theater_event_id')=COALESCE(json_extract(json_array(${theater_event_id:sqlstring}), '$[0]'),''))\n) , invitations AS (\n SELECT *,ROW_NUMBER() OVER (PARTITION BY user_id,session_id,view_id ORDER BY julianday(occurred_at),sequence,event_id) rn\n FROM t WHERE event_type='theater_view' AND phase='invitation'\n), choices AS (\n SELECT *,ROW_NUMBER() OVER (PARTITION BY user_id,session_id,view_id,decision_id ORDER BY julianday(occurred_at),sequence,event_id) rn\n FROM t WHERE event_type='theater_choice'\n), attempts AS (\n SELECT i.user_id,i.session_id,i.view_id,i.theater_type,json_extract(i.payload_json,'$.title') title,\n c.choice_class, MAX(CASE WHEN x.reason='escape' AND json_extract(x.payload_json,'$.playback_started')=1 THEN 1 ELSE 0 END) escape_midway\n FROM invitations i LEFT JOIN choices c ON c.user_id=i.user_id AND c.session_id=i.session_id\n AND c.view_id=i.view_id AND c.decision_id=i.decision_id AND c.rn=1\n LEFT JOIN t x ON x.user_id=i.user_id AND x.session_id=i.session_id AND x.view_id=i.view_id AND x.event_type='theater_exit'\n WHERE i.rn=1 GROUP BY i.user_id,i.session_id,i.view_id\n), rates AS (\n SELECT theater_type,MAX(title) 小剧场,COUNT(*) 邀请展示数,COUNT(DISTINCT user_id) 玩家数,\n SUM(CASE WHEN choice_class='continue' THEN 1 ELSE 0 END) 选择继续数,SUM(CASE WHEN choice_class='decline' THEN 1 ELSE 0 END) 直接拒绝数,\n SUM(choice_class IS NULL) 未观察到选择数,SUM(escape_midway) 开演后Esc退出数,\n ROUND(100.0*SUM(CASE WHEN choice_class='continue' THEN 1 ELSE 0 END)/COUNT(*),2) 继续率,\n ROUND(100.0*SUM(CASE WHEN choice_class='decline' THEN 1 ELSE 0 END)/COUNT(*),2) 直接拒绝率\n FROM attempts GROUP BY theater_type\n) SELECT * FROM rates WHERE 邀请展示数>=MAX(1,CAST(COALESCE(json_extract(json_array(${min_invitations:sqlstring}),'$[0]'),'10') AS INTEGER)) ORDER BY 继续率 DESC,选择继续数 DESC,邀请展示数 DESC,theater_type LIMIT 5",
          "rawQueryText": "WITH e AS (SELECT * FROM analytics_events WHERE ('__all__' IN (${client_platform:sqlstring}) OR client_platform IN (${client_platform:sqlstring})) AND ('__all__' IN (${release_version:sqlstring}) OR COALESCE(NULLIF(release_version,''),'__empty__') IN (${release_version:sqlstring})) AND ('${test_data}'='include' OR ('${test_data}'='auto' AND (client_platform='editor' OR (is_development_build=0 AND is_developer=0))) OR ('${test_data}'='exclude' AND is_development_build=0 AND is_developer=0 AND client_platform!='editor') OR ('${test_data}'='only' AND (is_development_build=1 OR is_developer=1 OR client_platform='editor'))) AND ('${behavior_period}'='all' OR (julianday(occurred_at) >= julianday(${__from}/1000,'unixepoch') AND julianday(occurred_at) < julianday(${__to}/1000,'unixepoch'))) ), t AS (\n SELECT *,json_extract(payload_json,'$.theater_event_id') theater_event_id,\n json_extract(payload_json,'$.theater_type') theater_type,\n json_extract(payload_json,'$.view_id') view_id,\n json_extract(payload_json,'$.decision_id') decision_id,\n json_extract(payload_json,'$.phase') phase,\n json_extract(payload_json,'$.choice_class') choice_class,\n json_extract(payload_json,'$.reason') reason\n FROM e WHERE event_type IN ('theater_view','theater_choice','theater_line','theater_exit')\n AND 1 AND 1 AND (COALESCE(json_extract(json_array(${theater_event_id:sqlstring}), '$[0]'),'')='' OR json_extract(payload_json,'$.theater_event_id')=COALESCE(json_extract(json_array(${theater_event_id:sqlstring}), '$[0]'),''))\n) , invitations AS (\n SELECT *,ROW_NUMBER() OVER (PARTITION BY user_id,session_id,view_id ORDER BY julianday(occurred_at),sequence,event_id) rn\n FROM t WHERE event_type='theater_view' AND phase='invitation'\n), choices AS (\n SELECT *,ROW_NUMBER() OVER (PARTITION BY user_id,session_id,view_id,decision_id ORDER BY julianday(occurred_at),sequence,event_id) rn\n FROM t WHERE event_type='theater_choice'\n), attempts AS (\n SELECT i.user_id,i.session_id,i.view_id,i.theater_type,json_extract(i.payload_json,'$.title') title,\n c.choice_class, MAX(CASE WHEN x.reason='escape' AND json_extract(x.payload_json,'$.playback_started')=1 THEN 1 ELSE 0 END) escape_midway\n FROM invitations i LEFT JOIN choices c ON c.user_id=i.user_id AND c.session_id=i.session_id\n AND c.view_id=i.view_id AND c.decision_id=i.decision_id AND c.rn=1\n LEFT JOIN t x ON x.user_id=i.user_id AND x.session_id=i.session_id AND x.view_id=i.view_id AND x.event_type='theater_exit'\n WHERE i.rn=1 GROUP BY i.user_id,i.session_id,i.view_id\n), rates AS (\n SELECT theater_type,MAX(title) 小剧场,COUNT(*) 邀请展示数,COUNT(DISTINCT user_id) 玩家数,\n SUM(CASE WHEN choice_class='continue' THEN 1 ELSE 0 END) 选择继续数,SUM(CASE WHEN choice_class='decline' THEN 1 ELSE 0 END) 直接拒绝数,\n SUM(choice_class IS NULL) 未观察到选择数,SUM(escape_midway) 开演后Esc退出数,\n ROUND(100.0*SUM(CASE WHEN choice_class='continue' THEN 1 ELSE 0 END)/COUNT(*),2) 继续率,\n ROUND(100.0*SUM(CASE WHEN choice_class='decline' THEN 1 ELSE 0 END)/COUNT(*),2) 直接拒绝率\n FROM attempts GROUP BY theater_type\n) SELECT * FROM rates WHERE 邀请展示数>=MAX(1,CAST(COALESCE(json_extract(json_array(${min_invitations:sqlstring}),'$[0]'),'10') AS INTEGER)) ORDER BY 继续率 DESC,选择继续数 DESC,邀请展示数 DESC,theater_type LIMIT 5"
        }
      ],
      "fieldConfig": {
        "defaults": {
          "unit": "short",
          "custom": {
            "filterable": true
          },
          "noValue": "未采集"
        },
        "overrides": [
          {
            "matcher": {
              "id": "byName",
              "options": "user_id"
            },
            "properties": [
              {
                "id": "links",
                "value": [
                  {
                    "title": "查看玩家旅程",
                    "url": "/d/gameplay-player-detail?${__url_time_range}&${client_platform:queryparam}&${release_version:queryparam}&${test_data:queryparam}&var-user_id=${__data.fields.user_id}&var-user_id=${__value.raw}"
                  }
                ]
              }
            ]
          },
          {
            "matcher": {
              "id": "byName",
              "options": "session_id"
            },
            "properties": [
              {
                "id": "links",
                "value": [
                  {
                    "title": "查看玩家旅程",
                    "url": "/d/gameplay-player-detail?${__url_time_range}&${client_platform:queryparam}&${release_version:queryparam}&${test_data:queryparam}&var-user_id=${__data.fields.user_id}&var-session_id=${__value.raw}"
                  }
                ]
              }
            ]
          },
          {
            "matcher": {
              "id": "byName",
              "options": "llm_request_id"
            },
            "properties": [
              {
                "id": "links",
                "value": [
                  {
                    "title": "查看玩家旅程",
                    "url": "/d/gameplay-player-detail?${__url_time_range}&${client_platform:queryparam}&${release_version:queryparam}&${test_data:queryparam}&var-user_id=${__data.fields.user_id}&var-llm_request_id=${__value.raw}"
                  }
                ]
              }
            ]
          }
        ]
      },
      "options": {
        "showHeader": true,
        "cellHeight": "sm"
      },
      "gridPos": {
        "x": 12,
        "y": 118,
        "w": 12,
        "h": 9
      }
    },
    {
      "id": 30,
      "title": "历史累计：缺时间事件与玩家（不受时间筛选）",
      "type": "table",
      "datasource": {
        "type": "frser-sqlite-datasource",
        "uid": "${DS_SQLITE}"
      },
      "description": "历史缺时间事件的累计值，不受右上角时间范围影响；仍遵守平台、版本和测试数据筛选。不能用于日活或留存。",
      "targets": [
        {
          "refId": "A",
          "queryType": "table",
          "queryText": "WITH h AS (SELECT g.*,COALESCE(u.is_developer,0) is_developer FROM gameplay_events g LEFT JOIN user_sessions u ON g.user_id=u.user_id WHERE g.event_real_time_iso IS NULL OR g.event_real_time_iso=''), e AS (SELECT * FROM h WHERE ('__all__' IN (${client_platform:sqlstring}) OR client_platform IN (${client_platform:sqlstring})) AND ('__all__' IN (${release_version:sqlstring}) OR COALESCE(NULLIF(release_version,''),'__empty__') IN (${release_version:sqlstring})) AND ('${test_data}'='include' OR ('${test_data}'='auto' AND (client_platform='editor' OR (is_development_build=0 AND is_developer=0))) OR ('${test_data}'='exclude' AND is_development_build=0 AND is_developer=0 AND client_platform!='editor') OR ('${test_data}'='only' AND (is_development_build=1 OR is_developer=1 OR client_platform='editor')))) SELECT COUNT(*) 历史事件数,COUNT(DISTINCT user_id) 玩家数 FROM e",
          "rawQueryText": "WITH h AS (SELECT g.*,COALESCE(u.is_developer,0) is_developer FROM gameplay_events g LEFT JOIN user_sessions u ON g.user_id=u.user_id WHERE g.event_real_time_iso IS NULL OR g.event_real_time_iso=''), e AS (SELECT * FROM h WHERE ('__all__' IN (${client_platform:sqlstring}) OR client_platform IN (${client_platform:sqlstring})) AND ('__all__' IN (${release_version:sqlstring}) OR COALESCE(NULLIF(release_version,''),'__empty__') IN (${release_version:sqlstring})) AND ('${test_data}'='include' OR ('${test_data}'='auto' AND (client_platform='editor' OR (is_development_build=0 AND is_developer=0))) OR ('${test_data}'='exclude' AND is_development_build=0 AND is_developer=0 AND client_platform!='editor') OR ('${test_data}'='only' AND (is_development_build=1 OR is_developer=1 OR client_platform='editor')))) SELECT COUNT(*) 历史事件数,COUNT(DISTINCT user_id) 玩家数 FROM e"
        }
      ],
      "fieldConfig": {
        "defaults": {
          "unit": "short",
          "custom": {
            "filterable": true
          },
          "noValue": "未采集"
        },
        "overrides": [
          {
            "matcher": {
              "id": "byName",
              "options": "user_id"
            },
            "properties": [
              {
                "id": "links",
                "value": [
                  {
                    "title": "查看玩家旅程",
                    "url": "/d/gameplay-player-detail?${__url_time_range}&${client_platform:queryparam}&${release_version:queryparam}&${test_data:queryparam}&var-user_id=${__data.fields.user_id}&var-user_id=${__value.raw}"
                  }
                ]
              }
            ]
          },
          {
            "matcher": {
              "id": "byName",
              "options": "session_id"
            },
            "properties": [
              {
                "id": "links",
                "value": [
                  {
                    "title": "查看玩家旅程",
                    "url": "/d/gameplay-player-detail?${__url_time_range}&${client_platform:queryparam}&${release_version:queryparam}&${test_data:queryparam}&var-user_id=${__data.fields.user_id}&var-session_id=${__value.raw}"
                  }
                ]
              }
            ]
          },
          {
            "matcher": {
              "id": "byName",
              "options": "llm_request_id"
            },
            "properties": [
              {
                "id": "links",
                "value": [
                  {
                    "title": "查看玩家旅程",
                    "url": "/d/gameplay-player-detail?${__url_time_range}&${client_platform:queryparam}&${release_version:queryparam}&${test_data:queryparam}&var-user_id=${__data.fields.user_id}&var-llm_request_id=${__value.raw}"
                  }
                ]
              }
            ]
          }
        ]
      },
      "options": {
        "showHeader": true,
        "cellHeight": "sm"
      },
      "gridPos": {
        "x": 0,
        "y": 127,
        "w": 24,
        "h": 6
      }
    },
    {
      "id": 31,
      "title": "历史累计：行为次数与参与人数（不受时间筛选）",
      "type": "table",
      "datasource": {
        "type": "frser-sqlite-datasource",
        "uid": "${DS_SQLITE}"
      },
      "description": "历史缺时间事件的累计值，不受右上角时间范围影响；仍遵守平台、版本和测试数据筛选。不能用于日活或留存。",
      "targets": [
        {
          "refId": "A",
          "queryType": "table",
          "queryText": "WITH h AS (SELECT g.*,COALESCE(u.is_developer,0) is_developer FROM gameplay_events g LEFT JOIN user_sessions u ON g.user_id=u.user_id WHERE g.event_real_time_iso IS NULL OR g.event_real_time_iso=''), e AS (SELECT * FROM h WHERE ('__all__' IN (${client_platform:sqlstring}) OR client_platform IN (${client_platform:sqlstring})) AND ('__all__' IN (${release_version:sqlstring}) OR COALESCE(NULLIF(release_version,''),'__empty__') IN (${release_version:sqlstring})) AND ('${test_data}'='include' OR ('${test_data}'='auto' AND (client_platform='editor' OR (is_development_build=0 AND is_developer=0))) OR ('${test_data}'='exclude' AND is_development_build=0 AND is_developer=0 AND client_platform!='editor') OR ('${test_data}'='only' AND (is_development_build=1 OR is_developer=1 OR client_platform='editor')))) SELECT event_type 行为,COUNT(*) 次数,COUNT(DISTINCT user_id) 参与人数 FROM e GROUP BY event_type ORDER BY 次数 DESC LIMIT 100",
          "rawQueryText": "WITH h AS (SELECT g.*,COALESCE(u.is_developer,0) is_developer FROM gameplay_events g LEFT JOIN user_sessions u ON g.user_id=u.user_id WHERE g.event_real_time_iso IS NULL OR g.event_real_time_iso=''), e AS (SELECT * FROM h WHERE ('__all__' IN (${client_platform:sqlstring}) OR client_platform IN (${client_platform:sqlstring})) AND ('__all__' IN (${release_version:sqlstring}) OR COALESCE(NULLIF(release_version,''),'__empty__') IN (${release_version:sqlstring})) AND ('${test_data}'='include' OR ('${test_data}'='auto' AND (client_platform='editor' OR (is_development_build=0 AND is_developer=0))) OR ('${test_data}'='exclude' AND is_development_build=0 AND is_developer=0 AND client_platform!='editor') OR ('${test_data}'='only' AND (is_development_build=1 OR is_developer=1 OR client_platform='editor')))) SELECT event_type 行为,COUNT(*) 次数,COUNT(DISTINCT user_id) 参与人数 FROM e GROUP BY event_type ORDER BY 次数 DESC LIMIT 100"
        }
      ],
      "fieldConfig": {
        "defaults": {
          "unit": "short",
          "custom": {
            "filterable": true
          },
          "noValue": "未采集"
        },
        "overrides": [
          {
            "matcher": {
              "id": "byName",
              "options": "user_id"
            },
            "properties": [
              {
                "id": "links",
                "value": [
                  {
                    "title": "查看玩家旅程",
                    "url": "/d/gameplay-player-detail?${__url_time_range}&${client_platform:queryparam}&${release_version:queryparam}&${test_data:queryparam}&var-user_id=${__data.fields.user_id}&var-user_id=${__value.raw}"
                  }
                ]
              }
            ]
          },
          {
            "matcher": {
              "id": "byName",
              "options": "session_id"
            },
            "properties": [
              {
                "id": "links",
                "value": [
                  {
                    "title": "查看玩家旅程",
                    "url": "/d/gameplay-player-detail?${__url_time_range}&${client_platform:queryparam}&${release_version:queryparam}&${test_data:queryparam}&var-user_id=${__data.fields.user_id}&var-session_id=${__value.raw}"
                  }
                ]
              }
            ]
          },
          {
            "matcher": {
              "id": "byName",
              "options": "llm_request_id"
            },
            "properties": [
              {
                "id": "links",
                "value": [
                  {
                    "title": "查看玩家旅程",
                    "url": "/d/gameplay-player-detail?${__url_time_range}&${client_platform:queryparam}&${release_version:queryparam}&${test_data:queryparam}&var-user_id=${__data.fields.user_id}&var-llm_request_id=${__value.raw}"
                  }
                ]
              }
            ]
          }
        ]
      },
      "options": {
        "showHeader": true,
        "cellHeight": "sm"
      },
      "gridPos": {
        "x": 0,
        "y": 133,
        "w": 24,
        "h": 6
      }
    }
  ]
}
